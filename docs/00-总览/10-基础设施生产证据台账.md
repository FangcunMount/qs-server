# 基础设施生产证据台账

本文是基础设施生产证据的人工阅读入口；机器真值由 [`infrastructure-production-evidence.json`](../infrastructure-production-evidence.json) 维护。
每条记录必须保留观测时间、环境、部署 SHA、源码基线、有效配置摘要或具名缺失、命令、结果、Owner、过期日、取代关系、原始证据与局限。

> 当前结论：台账中的五条记录全部是历史时点证据。它们不覆盖当前 `git:HEAD`（本轮主体文档提交为 `df09c14c...`），也不覆盖剩余本地门禁差异；由于没有当前精确部署 SHA、有效配置摘要和真实环境复验，基础设施 production 维度不得签署为 ready。

## 1. 历史记录索引

| ID | 适用 topics | 观测时间（CST） | 环境 | 执行/部署 SHA | 源码基线 SHA | 结果 | 当前资格与主要局限 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `infra-2026-08-15-interpretation-ledger-migration` | `data_migration_transaction`、`oneoff_recovery_production_evidence` | 2026-08-15 15:58:20 | production/serverA-serverD | `0cc80ae037e0...` | `df15c6070d91...` | 131,273 条 Attention、1 条 Catalog checkpoint 导入回读通过；三个 Mongo 源集合删除，服务恢复 | historical；有效配置摘要未留存，只证明该维护窗口 |
| `infra-2026-08-16-production-deploy-8f663383` | `deployment_cd_network` | 2026-08-16 00:59:33 | production/serverA-serverB-serverD | `8f663383e117...` | `e9f085e30800...` | apiserver 1、collection 2、worker 3 个实例通过精确 SHA 部署门禁 | historical；不证明完整业务链、数据一致性或后续健康 |
| `infra-2026-08-16-mongo-consistency-oneoff` | `data_migration_transaction`、`event_outbox_mq`、`oneoff_recovery_production_evidence` | 2026-08-16 01:13:35 | production/MongoDB 只读 one-off | `8f663383e117...` | `e9f085e30800...` | 扫描 347,494 个 anchor，退出码 2；唯一 finding 为 `outbox_missing_answersheet=269` | historical/findings；准确命令参数、二进制和有效配置摘要未独立留存；未修复，scheduler 仍关闭 |
| `infra-2026-08-18-compatibility-observation` | `observability_probes` | 2026-08-18 02:40:00 | production/Prometheus 只读观察 | `8f663383e117...` | `e9f085e30800...` | 失败；三个 current series 均无 anchor，legacy 指标缺失 | historical/failed；不能解释为兼容流量为零或回升 |
| `infra-2026-08-18-production-probe` | `deployment_cd_network`、`observability_probes` | 2026-08-18 08:45:08 | production/serverA-serverB-serverD | `8f663383e117...` | `e9f085e30800...` | apiserver health、collection 2/2、worker 3/3 与 K6 观测反查通过 | historical；未重新证明容器 SHA、全业务链、数据一致性和容量 |

所有记录的 `expires_on` 都是当前状态可引用资格的截止日，不是删除历史记录的日期。完整测量值、原始链接、topics 和限制以机器台账为准。
`runtime_lifecycle`、`config_secrets`、`cache_redis_signal`、`concurrency_resilience`、`security_acl_resource_ownership`
当前没有与自身语义相符的生产记录，因此 manifest 中的空引用是有意的证据边界，不是漏填：部署或浅探针不得冒充 SIGTERM/drain、有效配置、Redis/Signal、lease 或 ACL/资源授权证据。

## 2. 证据链如何读取

生产结论必须按以下链条分层，任何一层通过都不能替代下一层：

1. 源码与配置事实说明仓库声明了什么；
2. 非缓存测试说明指定源码基线在本地或 CI 中通过了哪些行为；
3. 环境测试说明目标网络、依赖、权限和有效配置下实际通过了什么；
4. 精确 SHA 部署说明哪份构建产物运行在什么环境；
5. 生产证据说明某一时间、某一范围内观测到了什么结果及限制。

因此，2026-08-18 的 probe 不能刷新 2026-08-16 的数据审计，失败的 compatibility observation 也不能被成功部署覆盖。版本级判断仍从[当前版本定档验收台账](./09-当前版本定档验收台账.md)进入；
基础设施专题签署由 [`document-closure.json`](../document-closure.json) 的 `infrastructure_signoff` 管理。

## 3. 新增和更新规则

新增生产证据时必须：

1. 使用不可复用的稳定 ID，并记录 RFC 3339 观测时间和具名环境；
2. 记录真实部署 SHA 与稳定源码基线 SHA，二者不得用 `git:HEAD` 代替；
3. 保存有效配置的 `sha256` 摘要；若历史运行确实未记录，只能使用 `unknown_not_recorded` 并解释缺失影响，`current` 记录不得使用这一豁免；
4. 保存可审查命令、结构化结果、Owner、过期日、原始证据和限制；`current` 记录的 `expires_on` 最迟不得超过 `observed_at` 后 30 天；
5. `current` 记录必须至少引用一条不可变的 `github_run` 证据，使用直接的 `/actions/runs/<run_id>` URL；`repository_record` 只能补充上下文，不能独立替代运行证据；
6. 若新记录取代旧记录，在 `supersedes` 中引用旧 ID，但不得删除旧记录；
7. 只有未过期、结果 passed、topics 相关、`deployed_sha` 精确等于当前 checkout、`source_baseline_sha` 等于当前机器基线并保存有效配置哈希的记录，才有资格支持 production ready；签署仍需责任人独立复核。

该台账只记录证据，不授权迁移、删除、修复、回滚或重跑 one-off。高风险动作必须从
[Migration、dirty 与回退边界](../03-基础设施/migration-recovery/10-Migration、dirty与回退边界.md) 和
[One-off、恢复与生产证据](../03-基础设施/migration-recovery/20-One-off、恢复与生产证据.md) 的独立审批链进入。
