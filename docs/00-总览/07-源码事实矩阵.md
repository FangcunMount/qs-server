# 源码事实矩阵

## 1. 用途

当文档与代码冲突时，用本表快速找到应核对的入口。

## 2. 矩阵

| 结论 | 首要事实源 | 补充证据 |
| --- | --- | --- |
| 业务模块清单 | `internal/apiserver/container/modules/registry.go` | `modules/*/wire.go`、`install.go` |
| 三进程入口 | `cmd/*` | 各进程 `internal/*/app.go` 与配置 |
| REST 路由与 schema | `api/rest/*.yaml` | handler 注解、生成 Swagger |
| internal gRPC | `api/grpc/proto` | 生成代码、service 实现、worker client |
| 事件名与投递语义 | `configs/events.yaml` | event catalog、handler 注册 |
| Redis 一次性信令 | `configs/signals.yaml` | `internal/pkg/signalcatalog` 契约测试 |
| 模型运行时边界 | modelcatalog runtime ports 与发布快照存储 | publication/release 服务和测试 |
| Evaluation/Interpretation 分界 | 两个 domain `doc.go` | application、container、worker 定向测试 |
| 数据访问护栏 | repository/UoW/Outbox 适配 | `internal/pkg/architecture` 测试 |
| 运维与质量入口 | `Makefile` | `.github/workflows` |

## 3. 使用规则

文档只给读者路径，不替代上述事实源。任何跨模块结论至少应能回到注册表/组合根、机器契约或一条可执行测试链。
