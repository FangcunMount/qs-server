# 当前版本定档验收台账

> 当前结论：**qs-server 当前代码、生产部署与已授权数据治理已达到本版定档门槛，无开放 P1。** Interpretation 历史 Presentation、Template、ModelCatalog 与 Outcome 路由治理已完成；Statistics 六条陈旧 running ledger、Attention 前后两批 33+91 个历史缺口、医学量表 14 个分类缺口与 2 个目录元数据污染、MySQL 5 张遗留表和 MongoDB 11 个遗留集合均已按各自授权闭环。17 个 active 医学量表现均使用 canonical 分类，公开 collection 接口缺口与 MBTI 污染均为 0。5 个具名兼容候选继续观察至 2026-09-05，跨仓 DefinitionV2 契约仍按 P2 跟踪，ServerD 三个 Runner 已收敛到 `2.336.0`。验收口径见[项目完成画像与验收标准](./08-项目完成画像与验收标准.md)。

> 本文只记录当前有效结论。绿色 workflow、候选构建、静态搜索和本地测试均不能替代原始 job 日志、实际 `DEPLOY_SHA`、运行进程指纹和生产证据。Attention 33 条与 91 条、Statistics 6 行、医学量表目录 15 个治理目标、数据库 5+11 对象和 Interpretation manifest 的授权均已一次性使用并闭环，不延伸为其他生产破坏性操作授权。

## 1. 基线、证据与当前结论

| 项目 | 当前事实 | 判定 |
| --- | --- | --- |
| 分支 / 验收输入 | 生产运行代码 `5c38faf7c48d76c163078f5de8cc87c6e3fe3566`；严格路由 `f63e273a`；AdmissionFailure 持久化修复 `b6407f40`；91 条 apply `29b452ff`；一次性入口退役 `5c38faf7` | 数据治理 manifest 已闭环；主干保留通用但默认关闭的精确清单安全能力，不保留固定对象清单或可重复执行的一次性入口 |
| 当前基线 CI | [CI 31114159630](https://github.com/FangcunMount/qs-server/actions/runs/31114159630)、[CodeQL 31114157038](https://github.com/FangcunMount/qs-server/actions/runs/31114157038) 与 [SonarQube 31114158960](https://github.com/FangcunMount/qs-server/actions/runs/31114158960) 完整成功 | E5 已取得；全量 Go 测试、integration-tag 编译、当前运行时闭环 E2E、lint、边界、安全、部署契约和三制品全部通过 |
| 当前严格版本部署 | [Production Deploy 31115112723](https://github.com/FangcunMount/qs-server/actions/runs/31115112723) 将 `5c38faf7c48d76c163078f5de8cc87c6e3fe3566` 部署到三服务 | E6 已取得；apiserver healthy、collection 2/2 healthy、worker 3/3 治理端点 ready；生产恢复开关关闭且固定 91 清单、指纹和一次性 apply workflow 均已退役 |
| 本地门禁 | `actionlint`、`go test ./internal/pkg/configcontract ./internal/pkg/migration` 与 `git diff --check` 通过 | 与当前 CI 相互独立的 E2 证据 |
| API 文档 | apiserver 174 paths、collection 45 paths，与当前生成结果一致 | 当前静态契约一致 |
| 部署后生产巡检 | [Ping 31115858816](https://github.com/FangcunMount/qs-server/actions/runs/31115858816) 成功：API health、collection 2/2 serving-ready、Nginx/DNS/HTTPS、ServerD 三个 Runner 与 worker 3/3 反向治理均通过；[Database Status 31115866722](https://github.com/FangcunMount/qs-server/actions/runs/31115866722) 只读巡检成功 | 退役入口后的严格版本运行闭环成立；三个 `Runner.Listener` 均运行，job 原始日志显示版本 `2.336.0` |
| Attention 恢复闭环 | 原 33 条由 31065753675 / 31065753703 闭环；新 91 条在 [Apply Audit 31113670701](https://github.com/FangcunMount/qs-server/actions/runs/31113670701) 确认精确扫描/创建 `91/91`、错误 0、mismatch 0，[DB Status 31113758030](https://github.com/FangcunMount/qs-server/actions/runs/31113758030) 首次独立归零，[DB Status 31115866722](https://github.com/FangcunMount/qs-server/actions/runs/31115866722) 在入口退役后再次确认归零 | 两批授权均已一次性使用；`5c38faf7` 已关闭生产任务、清空固定清单和指纹并删除一次性 apply workflow |
| 医学量表目录闭环 | [Audit 31156147186](https://github.com/FangcunMount/qs-server/actions/runs/31156147186) 冻结 15 个目标与 manifest；[Apply 31156236560](https://github.com/FangcunMount/qs-server/actions/runs/31156236560) 激活 15 个不可变 v2 release 并修正 2 个 head；[Verify 31156323262](https://github.com/FangcunMount/qs-server/actions/runs/31156323262) 确认 15/15、active 17、分类缺口 0、MBTI 标签污染 0；[Cache 31156574498](https://github.com/FangcunMount/qs-server/actions/runs/31156574498) 精确推进目录版本缓存 | 公开 `GET /api/v1/assessment-models?kind=scale` 实测返回 17/17 有效分类；一次性脚本和 workflow 操作已由 `116b9ffc` 退役，永久状态审计保留 |
| 当前生产数据盘点 | [Data Status 31156928663](https://github.com/FangcunMount/qs-server/actions/runs/31156928663)：ModelCatalog 103 条、25 个 head、78 个 snapshot、24 个 active，canonical identity/DefinitionV2/active duplicate 缺口均为 0；17 个 active scale 与对应 head 的分类缺口均为 0 | 一次性量表治理入口退役后的独立 E7 成立；retired Interpretation/ModelCatalog 集合残留仍为 0 |
| 兼容接口与数据回退观察 | [Compatibility Observation 31062020491](https://github.com/FangcunMount/qs-server/actions/runs/31062020491) 成功；三个公开接口、AnswerSheet 旧幂等 lookup/hit 和 Evaluation 无冻结 admission 回退共六个指标均存在，可见窗口增量均为 0，但 30 天前锚点均不存在 | E7 时点证据；公开接口窗口从 2026-08-06 09:04 CST 起算，数据回退窗口从 09:12 起算，均不能回溯宣称 30 天零命中；lookup 仅表示查询流量，只有旧集合 hit 才表示旧数据依赖 |
| Mongo 备份恢复预检 | [Backup Validate 31045715549](https://github.com/FangcunMount/qs-server/actions/runs/31045715549) 第二次执行成功；28 个归档集合均被读取，dry-run 0 restored / 0 failed | 证明归档可读，不等于实际恢复演练 |
| 生产物理退役 | [Backup 31070267072](https://github.com/FangcunMount/qs-server/actions/runs/31070267072) 创建固定备份集并对 11 个 Mongo 归档逐一 dry-run；[Apply 31071105270](https://github.com/FangcunMount/qs-server/actions/runs/31071105270) 删除 MySQL 5 表与 MongoDB 11 集合；[DB Status 31071330424](https://github.com/FangcunMount/qs-server/actions/runs/31071330424) 独立复核 | E7 已取得；MySQL 退役目标 0、共 28 表、migration `67/0`；MongoDB 退役列表空、共 18 集合、migration `21/false` |

当前严格版本已取得 E5、E6，Interpretation 与 Attention 数据治理取得 E7；入口退役后独立 [Ping 31115858816](https://github.com/FangcunMount/qs-server/actions/runs/31115858816) 与 [Database Status 31115866722](https://github.com/FangcunMount/qs-server/actions/runs/31115866722) 均成功。当前无开放 P1；兼容观察与跨仓 DefinitionV2 契约继续作为具名 P2，不影响 qs-server 本版定档。

## 2. 七个业务模块完成画像

| 类型 | 模块 | 状态 | 当前成立事实 | 未关闭项 / 退出条件 |
| --- | --- | --- | --- | --- |
| 核心 | Survey | 有条件通过 | 问卷发布、精确版本答卷、隐藏题拒绝、可靠提交、资源归属和摘要列表主链成立；填写人展示身份已补齐；`answersheet_submit_idempotency` 已由 migration 22 退役 | 历史无 admission、`task_id` 等兼容需指标和观察窗；published-head fallback 因 6 个 head version 缺 snapshot 必须保留 |
| 核心 | ModelCatalog | 有条件通过 | 25 个 head、24 个 active snapshot；其中 17 个 active 医学量表分类完整，canonical identity、DefinitionV2 与 active 唯一性检查均通过 | 历史记录仍持久化旧 identity 字段；`valid/errors` 外部响应兼容仍有调用方 |
| 核心 | Evaluation | 有条件通过 | Assessment、Run、Outcome、checkpoint 与 durable event 主链成立；五项生产一致性查询为 0；非 canonical run identity 为 0 | 保留历史读归一化与治理入口；补长期观察窗和真实并发运行证据 |
| 核心 | Interpretation | 有条件通过 | 历史展示治理已完成：36,860 个 profile 已等价物化，10 个 Template release、24 个 active ModelCatalog 路由和当前 Outcome 路由均显式；91 份 Attention 缺口已精确恢复并两次独立确认归零；动态猜测与一次性恢复入口均已退役 | 具名兼容候选继续按指标观察至 2026-09-05；命中则对应项重新计时 |
| 辅助 | Actor | 有条件通过 | IAM、Operator、Clinician、Testee、ProfileLink 责任链已装配；旧角色投影、旧受试者统计代码已退出 | 三个 Testee 旧物理列仍存在；`tags`、`org_id` 和默认组织仍是活跃线协议/单租户契约 |
| 辅助 | Plan | 有条件通过 | 286,552 个 task 均有 revisioned schedule fact；legacy fulfillment fallback 为 0；计划、入组、任务链可回放；当前 HEAD 三进程和治理巡检通过 | 缓存、通知、失败恢复与容量仍缺长期观测和故障演练 |
| 辅助 | Statistics | 通过 | 849,692 条 assessment fact 已 frozen；最新 publish 成功且 snapshot 对齐；v2 Redis key 已出现 | 六条未提交数据的陈旧 running ledger 已按授权精确结算为 `failed`，未删除任何幂等 Fact；独立对账无 running 遗留 |

## 3. 十个技术专题

| 技术专题 | 状态 | 当前判定 |
| --- | --- | --- |
| Event / Outbox / MQ | 黄 | 审计窗内 Mongo `answersheet.submitted`、`interpretation.report.generated` 各 768 条 published；MySQL `evaluation.requested`、`evaluation.outcome.committed` 各 768 条 published；无未知活动类型；当前 HEAD 消费端健康，仍缺长期窗口 |
| Cache / Signal / Warmup | 黄 | 当前 key Builder、Signal、Warmup 责任链成立；医学量表目录治理后已通过全局 version token 精确失效 list cache；Statistics v2 已有真实 key，Assessment List v2 尚无真实 data key，旧 token 和兼容 key 不能提前清理 |
| Rate limit / Gate / Backpressure | 黄 | 限流、队列、payload gate 和 Outbox relay 均有边界与测试；生产 inflight、等待时长和下游慢场景证据不足 |
| LockLease / Leader / Duplicate suppression | 黄 | Attention 91 条 apply 在共享租约下仅一实例执行，精确创建 91、mismatch/错误为 0；退役后重复记录仍为 0。FactReconciler 保留为默认关闭的通用恢复能力，新批次仍需新清单、指纹、dry-run 与明确授权 |
| Retry / Manual governance | 黄 | 业务重试、Outbox、MQ NACK、hold 与人工动作已分层；当前窗口无 hold/dead-letter，仍缺长期窗口和人工恢复演练 |
| Data / Migration / Retirement | 黄 | MySQL migration 68、Mongo migration 23 均 clean；三个 Interpretation 运行账本已从 MongoDB 迁移到 MySQL 并退役源集合；已授权的 5+11 物理退役对象也已备份、恢复预检、删除并独立对账归零；Testee 三个旧列仍需单独批准与迁移 |
| IAM / Security / Tenant | 黄 | JWT 校验、authz snapshot、角色投影和资源归属链唯一；外部 IAM capability fallback 与当前单租户默认组织仍需保留 |
| Runtime / Scheduler / Shutdown | 黄 | 严格版本本地与 CI 运行时闭环已通过，`5c38faf7` 已部署到三服务并通过 apiserver、collection 2/2、worker 3/3 健康核验；Statistics 台账已收敛，Attention 临时恢复任务已关闭且固定清单为空 |
| Observability / Governance | 黄 | cache、resilience、outbox、worker governance 与兼容命中均有指标入口；三个公开兼容接口已进入每日 Prometheus 观察，首次生产运行因缺 30 天锚点判定窗口未满；其他退出项仍需分批固化 |
| Delivery / Performance | 黄 | 当前基线的 CI、三制品、实际全服务部署、运行指纹和部署后巡检已闭环；当前 k6 主入口可解析，但本轮未重新执行容量与故障压测 |

## 4. 十二角度统一矩阵

颜色含义：绿为当前基线主体成立；黄为有 owner 或退出条件但仍需补证；红为定档阻断。矩阵只做导航，具体结论以本台账其他章节为准。

| 验收角度 | Survey | ModelCatalog | Evaluation | Interpretation | Actor | Plan | Statistics |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1. 职责与边界 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 2. 代码与装配 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 3. 领域与不变量 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 4. REST / gRPC / Event | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 5. 数据与迁移 | 黄 | 黄 | 黄 | 红 | 黄 | 绿 | 绿 |
| 6. 状态与一致性 | 绿 | 绿 | 绿 | 红 | 绿 | 绿 | 绿 |
| 7. 安全与隐私 | 绿 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 8. 缓存与并发 | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 9. 测试与质量门禁 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 10. 可观测与运维 | 黄 | 黄 | 黄 | 绿 | 黄 | 黄 | 绿 |
| 11. 性能与容量 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 12. 遗留与兼容 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |

## 5. 遗留代码与中间态兼容分类

### 5.1 已确认并删除的代码中间态

| 批次 | 代表提交 | 已关闭内容 |
| --- | --- | --- |
| 数据退役审计 | `0d7249f9` | 固化退役 MySQL 字段、表和索引只读审计 |
| 部署环境 | `63dbf946` | 删除已失效的部署环境变量兼容 |
| 路由契约 | `98b707bf` | `/practitioners` 降为仅运行时兼容，移出当前 OpenAPI |
| 接口 facade | `6c092b67` | 删除零调用 facade 中间层 |
| 管理接口 | `51e73892` | 删除永久 501 占位管理接口和空 registrar |
| 上下文语义 | `dbb39911` | 删除生产代码中的 `context.TODO()` 中间态 |
| 答卷展示 | `08bd5e30` | 补齐活跃消费方所需的 `filler_name` 身份解析，失败时回退用户 ID |
| 领域契约 | `b760116b` | 删除未生效的停用原因参数、虚假未来事件 TODO 和过期重构注释 |
| Attention 演练 | `b6a01ba2` | 为生产 worker 启用只读 reconcile dry-run，固定起始时间与配置契约 |
| 脚本入口 | `d2482f27` | 删除旧 Swagger 路径 fallback 与 k6 兼容包装脚本 |
| Statistics 结算 | `17f25ff7` | 精确结算 6 条陈旧 running ledger 后退役一次性 SQL、脚本与 workflow |
| 部署镜像导出 | `0a850833` | 串行化共享 Docker Desktop 的 pull/save 并加入可重现的并发/重试契约 |
| Attention 收敛 | `c82d4a9b` / `1532b30a` | 精确修复 33 个历史缺口，对账归零后停用临时扫描并删除固定数量的一次性 apply workflow |
| 物理对象退役 | `6f08e511` / `84b61b30` | 在固定备份、逐对象恢复预检和确认口令保护下删除 MySQL 5 表与 MongoDB 11 集合，独立对账后移除一次性入口 |
| Interpretation 路由收敛 | `a58050a6` / `f63e273a` / `b6407f40` | 历史 Outcome 路由显式冻结；删除动态 presentation、默认模板和 `legacy-v1` 猜测；准入失败持久化使用互不重叠的 Mongo operator 字段 |
| 医学量表目录收敛 | `cd85b83c` / `116b9ffc` | 以不可变 v2 release 修复 14 个分类缺口和 2 个元数据污染；独立验证、缓存失效和公开接口复核后删除一次性治理脚本与 workflow 操作 |

当前生产 Go 代码已无 `TODO`、`FIXME`、`XXX`、`HACK`。静态遗留关键词和零调用搜索已经饱和：剩余命中均能映射到历史读、当前线协议、外部 IAM 能力、指标观察窗、迁移审计或部署回滚边界；不能仅凭 `legacy`、`v1`、`deprecated` 命名删除。

### 5.2 当前契约或数据兼容，不得批量删除

下表不是“17 段都应在本版删除的过渡代码”。`wait-report`、`task_id/origin_ref` 在 qs-collection-system 和 seeddata-runner 中仍有直接生产调用，ModelCatalog `valid/errors` 仍有 qs-operating-system 读取回退；published-head、历史 identity、IAM、Redis 项则有已存数据或外部能力依赖。将这些整体直接删除会破坏当前客户端、历史读或运行安全，因此本轮明确否决“不兼容、全部直删”。只有 5 个具名退役候选进入 2026-09-05 观察线，其余项必须先完成消费方或数据迁移。

| 兼容项 | 保留理由 | 退出证据 |
| --- | --- | --- |
| `/practitioners` | `/clinicians` 已是 canonical，但旧路由仍有运行时计量入口 | 指标 `qs_actor_deprecated_practitioner_route_total` 连续 30 天为 0，调用方确认后删除 |
| ModelCatalog `valid/errors` | qs-operating-system 仍会从 `passed/issues` 回退读取 | 前端发布、观察窗归零后退出 |
| Statistics REST `validate_only` | 仓内 CLI 已迁移 `mode`，外部调用仍需指标证明 | deprecated 参数命中 30 天为 0 |
| AnswerSheet `task_id` / `origin_ref` | collection 与 seeddata-runner 等调用链仍使用 | 跨仓迁移、流量归零、历史事件回放通过 |
| Testee `tags`、Actor/Clinician `org_id` | 当前线协议仍公开且有消费者 | 调用方版本窗口与契约升级完成 |
| Collection `wait-report` / `interpreted` | 当前产品流程仍使用 | 新状态协议完成迁移并经过观察窗 |
| `GenerateReportFromAssessment` | 静态跨仓无消费者，但生产观察窗不足 | deprecated RPC 指标连续 30 天为 0 |
| `PayloadGateLegacyIncomplete` | 历史事件可能缺少新字段 | 历史积压清空、兼容命中归零 |
| Evaluation / Interpretation 历史 identity 归一化 | 不可变历史事实读取仍需要 | 历史事实回填或生命周期自然退出 |
| Questionnaire published-head / exact-version fallback | 生产有 6 个 head version 缺 snapshot | 完整性查询归零且 fallback 指标连续归零 |
| IAM 通用 claim key 与 service-token fallback | 外部 IAM 契约与能力协商仍活跃 | IAM 统一升级并完成跨服务验收 |
| 默认 `org_id=1` | 当前单租户运行契约，不是未实现 TODO | 多租户契约落地后显式迁移 |
| ModelCatalog 旧 identity 持久化字段 | 103 条生产记录仍包含旧字段 | 新写切断、回填/归档、读指标归零 |
| Redis Statistics 旧 12 键 | v2 仅有 3 个真实键，旧代实例退出未证明 | 旧实例退出、最长 TTL 过去、单独批准 |
| Redis Assessment List 39,656 个旧 token | v2 data key 尚未真实出现，旧 token 无 TTL | v2 真实命中、旧实例退出、单独清理批准 |
| `qs_cache_lock_*` 指标 | 基础设施 Grafana 仍依赖 | 仪表盘迁移并完成观察窗 |

#### 5.2.1 兼容接口与数据回退正式观察窗

仓库尚无可用的发布 tag，因此不虚构 SemVer；5 个退役候选的 6 个指标统一进入具名退出基线 `compat-retirement-2026-09`。生产 run 31062020491 对三个公开接口和两个 AnswerSheet 指标各返回 1 个当前 series，Evaluation 回退因结果标签预初始化返回 4 个当前 series；六项均为 `window_increase=0`、`anchor_series=absent`。因此观察窗分别从首次成功运行起算，不借用不完整的历史数据，也不把“series 存在”解释为发生过兼容命中。

| 兼容项 | 指标 | Owner | 观察窗 | 退出基线与条件 |
| --- | --- | --- | --- | --- |
| `/practitioners` | `qs_actor_deprecated_practitioner_route_total` | Actor | 2026-08-06 09:04–2026-09-05 09:04 CST | `compat-retirement-2026-09`；每日 workflow 连续成功、30 天增量为 0、跨仓调用方确认后单独删除 |
| Statistics `validate_only` | `qs_statistics_deprecated_validate_only_total` | Statistics | 2026-08-06 09:04–2026-09-05 09:04 CST | `compat-retirement-2026-09`；每日 workflow 连续成功、30 天增量为 0、外部调用方确认后删除参数兼容 |
| `GenerateReportFromAssessment` | `qs_interpretation_deprecated_generate_report_from_assessment_total` | Interpretation | 2026-08-06 09:04–2026-09-05 09:04 CST | `compat-retirement-2026-09`；每日 workflow 连续成功、30 天增量为 0、所有 gRPC 消费方确认后删除旧 RPC |
| AnswerSheet 旧幂等集合 | `qs_answersheet_legacy_idempotency_fallback_lookup_total` / `qs_answersheet_legacy_idempotency_fallback_hit_total` | Survey | 2026-08-06 09:12–2026-09-05 09:12 CST | `compat-retirement-2026-09`；lookup 只观察回退查询量，不要求归零；旧集合 hit 30 天增量为 0、集合仍为 0、删除演练通过后，另行批准删除集合与 fallback |
| Evaluation 无冻结 admission 回退 | `qs_evaluation_assessment_intake_legacy_binding_total` | Evaluation | 2026-08-06 09:12–2026-09-05 09:12 CST | `compat-retirement-2026-09`；各结果标签合计 30 天增量为 0、历史数据 Owner 确认后再收敛回退 |

三个公开兼容指标、AnswerSheet hit 或 Evaluation 回退出现增量时，对应项立即回到“活跃兼容”，观察窗从最后一次命中重新计时；AnswerSheet lookup 增量不触发重计时，因为当前新幂等键也会走该查询。workflow 失败、指标缺失或历史锚点不足不计入连续窗口。窗口完成只是删除的必要条件，不是自动删除授权。

### 5.3 已完成生产物理退役

| 对象 | 生产证据 | 当前代码责任链 | 当前动作 |
| --- | --- | --- | --- |
| MySQL 4 个 `seed_orphan_*_bak_hist_v1*` 表 | 删除前精确计数均为 0 | 无运行时读写 | 31071105270 已 DROP；31071330424 独立复核无残留 |
| MySQL `analytics_scan_watermarks` | 删除前精确计数为 0 | migration 67 统一退役，无运行时 owner | 31071105270 已 DROP；migration `67/0` 防止环境回漂 |
| Mongo `assessment_models_legacy_20260717_084636` | 删除前 28 个文档 | 无当前运行时 owner | 31070267072 归档可恢复预检；31071105270 已删除 |
| Mongo 10 个 `__seed_orphan_backup_hist_v1...` 集合 | 删除前每个 269 个文档 | 无当前运行时 owner | 10 个归档逐一通过 dry-run；31071105270 已删除，31071330424 退役列表为空 |

### 5.4 高置信但生产观察不足

| 对象 | 当前证据 | 仍缺证据 |
| --- | --- | --- |
| MySQL `testee.total_assessments` / `last_assessment_at` / `last_risk_level` | active testee 的三个旧统计列全部为默认值，且大量与 canonical summary 不一致；当前代码不再读写 | 单独的列级 schema 备份、DROP COLUMN 批准、migration 与回滚验证；本次 5+11 授权不包含它们 |
| deprecated `GenerateReportFromAssessment` | 本仓及已检跨仓静态零调用；31061609088 当前增量为 0，已启动正式窗口 | 连续 30 天生产指标与所有外部消费者确认 |
| `/practitioners` | canonical `/clinicians` 已被当前前端和 seeddata-runner 使用；31061609088 当前增量为 0，已启动正式窗口 | 连续 30 天旧路由指标与其他调用方确认 |
| 兼容字段与 fallback | 均有明确 current owner 或历史读入口 | 独立指标、Owner、观察窗和退出版本 |

## 6. 数据库与缓存当前画像

### 6.1 MySQL

- MySQL 8.0.36，隔离级别 READ-COMMITTED，migration 68，`dirty=false`；共 31 张正式表。
- Interpretation 运行账本已完成迁移：Attention 共 131,273 条且全部为 `succeeded`，Admission 为 0 条，Catalog checkpoint `report_catalog` 存在且处于 `catalog_entries`；其 revision 是持续推进的运行时 CAS token，不作为定档常量。
- 四个 seed backup 表和 `analytics_scan_watermarks` 已删除；31071330424 的 `unexpected_retirement_table` 查询返回空。
- 64,991 个 active testee 的三个旧统计列全部为默认值，且各有 35,608 条与 canonical summary 不一致；这些列不能作为回滚数据源。
- Evaluation 五项一致性检查均为 0，非 canonical run identity 为 0。
- Statistics 最新 publish run `631524273684492846` 成功，`as_of=2026-08-05`，generation 15，snapshot 对齐。
- 六条陈旧 running ledger 在写入前反复确认 `data_committed_at IS NULL`、标准 rebuild 进程/容器为 0、Redis DB 6 的 `cache:lock:statistics:1` TTL 为 -2；经负责人批准后由 31063053544 精确更新 6 行为 `failed/stale_run_reconciled`，eligible/updated/verified=`6/6/6`。独立 MySQL 只读运行 31063110773 确认原 6 行不再属于 running 分组。

### 6.2 MongoDB

- MongoDB 7.0.31，migration 23，`dirty=false`；共 13 个集合。
- 13 个集合精确分类为：11 个当前业务/运维集合、1 个 `schema_migrations`、1 个系统集合 `system.profile`；已退役集合不会由运行时重新创建。
- `answersheet_submit_idempotency` 与 `archived_reports` 已在生产确认均为 0 文档，并由 Mongo migration 22 完成物理删除；运行时不会重新创建。
- `interpretation_admission_failures`、`interpretation_attention_projections` 与 `interpretation_catalog_audit_checkpoints` 已由维护运行 31872956357 迁移并删除，Mongo migration 23 固化空源校验和物理退役。
- ModelCatalog 共 103 条：25 个 head、78 个 snapshot、24 个 active snapshot、54 个 archived snapshot；103 条均仍持久化旧 identity 字段。canonical identity、DefinitionV2、release status 和 active duplicate 检查均无异常。医学量表共 17 个 active snapshot，active/head 的空或非法分类均为 0；分类分布为 ADHD 5、ASD 2、情绪 4、压力 1、感觉统合 1、睡眠 2、抽动 2。
- Questionnaire 共 136 条：42 个 head、94 个 snapshot、38 个 active snapshot；角色、状态和重复检查无异常，但有 6 个 head version 缺 snapshot。
- Interpretation 历史展示治理已完成：36,860 个缺失 `presentation_profile` 全部由 Artifact 自身 dimensions 等价物化；最新状态运行 31115866722 确认 active artifact 213,250，缺 profile/builder/content schema 均为 0；source 为 `frozen` 176,390、`legacy_artifact_dimensions/v1` 36,860。
- Template 发布目录现为 5 个 TemplateID × 历史版/当前版，共 10 条；24 个 active ModelCatalog snapshot 均显式冻结 TemplateID/TemplateVersion。
- Outcome 历史 manifest 核验 31103985240：212,586 个历史候选逐条验证通过；最新状态运行 31115866722 确认 total=213,250、缺 TemplateID/TemplateVersion=0，新增 664 份均使用当前 `2026-08-v1`。运行时已删除默认版本和动态目录猜测；专项执行台账已在签署后退出 active truth layer。
- 31107140357 在 `audit_from=2026-08-01` 的只读复核发现 165 份 high/severe artifact 中有 91 份缺 Attention projection，涉及 82 个 Testee 和 9 个重复 Testee 分组；跨库主体、Assessment、组织与关系检查均无异常。清单指纹 `75bc40d2…a09` 经 31111298011 dry-run 验证后，由 31113670701 精确创建 91 条、错误和 mismatch 为 0；31113758030 首次对账归零。入口退役后 31115866722 再次确认 168 份 high/severe artifact 缺口 0、projection 重复 0、跨库异常 0。
- Interpretation 治理前全库归档 `qs_mongodb_backup_20260806_140515.archive.gz` 约 253 MiB，31086706136 验证成功；物理退役备份集仍位于 `/opt/backups/qs-server/legacy-database-retirement/legacy-db-retirement-20260806-v1`。

### 6.3 Redis

- Redis 7.0.15，used memory 约 178.48 MiB，maxmemory 256 MiB，策略 `allkeys-lru`。
- 旧候选计数器在 DB 0–8 均为 0；这只能证明采样时点，不等于观察窗完成。
- 最新只读时点，DB 2 有 74 个 questionnaire、19 个 model 当前 key；DB 3 扫描到 32,134 个 assessment 当前 key。缓存计数会随 TTL、流量和 SCAN 并发变化，不能当成持久数据总量。
- DB 4 有 3 个 Statistics 当前 key 和 12 个兼容 key；DB 7 当时采样到的 39,656 个 assessment-list token 现已归类为 `retired_assessment_list_version_candidate`，仅在旧 apiserver 全部退出并稳定至少 24 小时后才可另行审批清理；DB 7 另有 2 个当前 hotset，DB 8 有 321,868 个当前 rank/other family key。
- Redis 是运行时状态，不是迁移表；任何清理必须精确到 DB、family、namespace、TTL、实例退出和审批。

## 7. 当前结论与目标队列

### 7.1 本轮已关闭门禁

| ID | 已关闭目标 | 证据 |
| --- | --- | --- |
| FINAL-P1-001 | 当前基线完整 CI | 31105453450、31105452791、31105456102 均成功；全量测试、integration-tag 编译、运行时闭环 E2E、lint、边界、安全、部署契约和三制品通过 |
| FINAL-P1-002 | 当前基线实际部署与运行指纹 | 31106231761 attempt 2 实际部署 `b6407f40` 全部三服务并通过 worker 治理反向验证；31107140396 独立巡检通过 |
| FINAL-P1-003A | Attention dry-run 演练 | 31051404012 验证三实例目标 SHA、有效 dry-run 配置和三个串行轮次，均扫描 41、缺失 33、创建 0、失败 0；31051481975 复核 projection 仍为 117 |
| FINAL-P1-003B | 处置 33 个 Attention 高风险缺口 | 31065753675 确认精确创建 33、错误 0、mismatch 0；31065753703 确认 184 条 projection 全成功、42 份 high/severe artifact 缺口 0、重复 0；`1532b30a` 关闭临时任务 |
| FINAL-P1-004 | 结算六条 Statistics running ledger | 31063053544 在无进程、无容器、无 Redis 租约的前提下精确结算 6 行，写后自检为 `6/6/6/6/6`；31063110773 独立只读对账通过，未删除幂等 Fact |
| FINAL-P1-005 | 固化兼容分类与观察窗 | 17 项已拆分为当前真实契约/数据依赖与 5 个具名退役候选；31062020491 确认 6 个指标当前增量为 0，Owner、观察窗、重计时规则和退出门槛已记录；整体直删决策已被真实调用/数据证据否决 |
| FINAL-P1-006 | 升级 ServerD 自托管 Runner 集群 | 31074245885、31074245928、31074246185 并发成功；原始 job 日志分别确认 `serverD-runner3`、`serverD-runner2`、`serverD-runner1` 均为 `2.336.0` |
| FINAL-P1-007 | 处置新发现的 91 个 Attention 高风险投影缺口 | 31109084517 冻结 91 条清单与指纹；31111298011 三副本 dry-run 一致；31113670701 精确创建 91、错误/mismatch 为 0；31113758030 / 31115866722 两次独立确认缺口与重复为 0；`5c38faf7` 已退役固定清单和一次性入口 |
| FINAL-P1-008 | 收敛医学量表分类与目录污染 | 31156147186 冻结 15 个目标；31156236560 激活 15 个不可变 v2 release、更新 2 个 head；31156323262 确认 15/15、active 17、分类缺口 0、MBTI 污染 0；31156574498 精确失效缓存，公开接口返回 17/17；`116b9ffc` 已退役一次性入口 |
| FINAL-P2-002 | 生产物理遗留对象退役 | 31070267072 完成 5+11 固定备份及恢复预检；31071105270 完成精确删除；31071330424 确认 MySQL `67/0`、28 表、退役表 0，MongoDB `21/false`、18 集合、退役列表空 |
| FINAL-P2-003 | 退出 GitHub Actions Node 20 强制兼容并恢复仓内覆盖率制品 | `286010dd` 删除无有效消费链的 Codecov 外部上传；CI 31061044971 完整成功，`Run Tests` 生成并通过 `upload-artifact@v7` 上传 `coverage.out`，三应用制品也全部成功 |
| FINAL-P2-001 | Interpretation presentation/template 收敛 | 36,860 个 profile、10 个 Template release、24 个 active ModelCatalog route、当前 213,250 个 Outcome route 全部显式；CI/CodeQL/Sonar 全绿；31115112723 部署 `5c38faf7` 三服务，31115858816 / 31115866722 复核通过 |

### 7.2 仍开放目标

| ID | 级别 | 目标 | 当前证据 | 退出条件 | Owner / 期限 |
| --- | --- | --- | --- | --- | --- |
| FINAL-P2-004 | P2 | 执行兼容退役观察 | 5 个具名候选已进入 `compat-retirement-2026-09`；首次观察的 6 个指标可见增量均为 0，但无 30 天锚点 | 每日 workflow 连续成功到 2026-09-05；命中则对应项重新计时；窗口完成后仍需按项批准删除 | Actor / Statistics / Interpretation / Survey / Evaluation / 2026-09-05 |
| FINAL-P2-005 | P2 | 补齐 qs-operating-system DefinitionV2 模板路由契约 | qs-server 两个跨仓 characterization fixture 缺显式 TemplateID/TemplateVersion；后端严格路由已拒绝默认值 | 前端 DefinitionV2 类型、编辑/发布映射和 fixture 均携带显式版本；跨仓 characterization 门禁通过 | qs-operating-system / 待独立任务 |

当前无开放 P1。Interpretation 历史展示与 Attention 数据治理已经关闭；兼容观察和跨仓前端契约是独立 P2。任何观察窗完成都不会自动产生新的删除或数据修复授权。

## 8. 验证入口与证据缺口

| 门禁 | 当前结果 |
| --- | --- |
| 本地 Go 门禁 | 非 characterization 全仓测试、聚焦 Mongo 测试、`actionlint` 和 `git diff --check` 通过；全仓、integration-tag 编译与三制品结果见 31105453450 |
| `make lint-boundaries` | 通过，0 issues |
| `make docs-verify` | 通过；apiserver 174、collection 45 paths |
| `make docs-hygiene` / `make docs-facts` | 通过；150 篇现行 Markdown、7 个业务模块、6 个核心事件 |
| `k6 inspect scripts/perf/k6/mixed.js` | 通过，当前主压测入口可解析 |
| 当前基线 CI | 31114159630、31114157038、31114158960 完整成功；全量测试、integration-tag 编译、当前运行时闭环 E2E、部署契约和三制品全绿 |
| 当前严格版本 Deploy | 31115112723 完整成功；原始日志确认三服务运行退役入口后的 `5c38faf7`，apiserver healthy、collection 2/2 healthy、worker 3/3 治理端点 ready |
| 部署后巡检 | 31115858816 成功：API、Nginx/DNS/HTTPS、collection 2/2、worker 3/3 和 ServerD 三个 Runner 健康；31115866722 确认 Attention 与跨库对账继续为 0 |
| Attention dry-run | 原 33 条由 31051404012 验证；新 91 条由 31111298011 验证三副本目标 SHA、allowlist、指纹和 `dry_run=true` 一致，成功轮次 5、created/mismatched/error 均为 0 |
| Attention 跨库只读分类 | 31109084517 确认新清单 gap reports/Testees = 91/82，缺失/删除主体、跨组织、关系和 Assessment mismatch 均为 0；82 个 Testee 均需置重点关注，全程只读 |
| Attention apply 与收敛后对账 | 31113670701 成功：3 worker、精确 created=91、error=0、mismatched=0。31113758030 首次确认缺口/重复为 0；入口退役后的 31115866722 再次确认 projection 仅有 succeeded、168 份 high/severe 缺口 0、跨库异常 0 |
| 兼容接口与数据回退观察 | `go test ./internal/pkg/configcontract`、actionlint 和脚本正反例通过；31062020491 生产只读运行成功，六个指标均有 current series、可见增量均为 0、30 天 anchor 均缺失；公开接口窗口从 2026-08-06 09:04 CST 开始，数据回退窗口从 09:12 开始；AnswerSheet lookup 仅作流量观测，hit 才是旧数据依赖信号 |
| Interpretation 历史展示治理 | Presentation 31089462135；Template 31096561755；ModelCatalog 31098459999；Outcome manifest 31103985240 均成功。31115866722 确认 profile/builder/content schema 缺口 0、10 个 release、24 个显式 active route、213,250 个显式 Outcome route |
| 医学量表目录治理 | 31156147186 audit、31156236560 apply、31156323262 verify、31156574498 cache invalidation 均成功；17 个 active scale 的 category 均属于 8 个 canonical 值，空/非法分类与 MBTI 污染为 0；公开 collection API 同步返回 17/17 |
| 治理入口退役后独立数据盘点 | 31156928663 成功：ModelCatalog `103/25/78/24/54`（总数/head/snapshot/active/archived），snapshot canonical identity、DefinitionV2、release status、active duplicate 缺口均为 0；active/head 医学量表分类缺口均为 0；retired 集合残留为 0 |
| 生产数据盘点 | Statistics 结算 31063053544 与独立 MySQL 对账 31063110773 均成功；31115866722 更新 Attention 事实为 projection `succeeded=1068`、168 份 high/severe 缺口 0、重复和跨库异常均为 0 |
| Mongo 备份验证 | 31045715549 第二次成功；28 个归档集合 dry-run 0 failed |
| 物理退役备份 / 执行 / 独立对账 | 31070267072 / 31071105270 / 31071330424 均成功；5 张 MySQL 表和 11 个 MongoDB 集合已退役，migration 清洁且残留清单为空 |
| 静态 deadcode | 工具不可用；未下载未固定版本的 latest 工具，候选改由责任链与零调用搜索逐项确认 |

## 9. 破坏性操作边界

本台账不授权任何新的生产 `DROP`、`DELETE`、Mongo collection 删除、Redis 批量清理或未列明的 ledger 状态修改。Attention 33 条与 91 条、医学量表目录 15 个目标的 apply 授权及本次 5+11 物理退役授权均已一次性使用并闭环，不得复用或扩展。

1. MySQL 物理删除前必须具备当前 schema/row count、备份、恢复命令、审批、执行窗口和执行后 migration/schema 核验。
2. 本次退役备份集必须按运维保留策略保管；`mongorestore --dryRun` 证明归档可解析，不等于新库实际导入的完整灾备演练。
3. Redis 清理必须精确到 DB、family、namespace 和 key pattern；先证明旧实例退出并等待最长 data TTL。无 TTL token 只能单独审批，禁止宽泛通配删除。
4. Statistics 六条结算授权已在 31063053544 一次性使用并闭环；不得复用为其他 Run 的批量状态修改授权。
5. Attention 91 条 reconcile 授权已在 31113670701 使用，31113758030 对账后由 `5c38faf7` 停用恢复任务、清空对象清单与 fingerprint 并删除一次性 workflow；任何重新启用都需新的对象清单、fingerprint、dry-run 和明确授权。

## 10. 定档签署结果

当前版本的代码、生产部署与已授权数据治理达到定档门槛，当前无开放 P1：

- 当前生产运行代码 `5c38faf7` 的全仓测试、必跑 CI、三制品和运行时闭环 E2E 已通过；31115112723 已将同一 SHA 部署到三服务，31115858816 完成健康与治理反向验证。
- Statistics 六条陈旧 running ledger 已精确结算并独立对账；Attention 前后两批 33+91 个缺口均已精确收敛、跨库归零并停用临时任务。
- Interpretation 历史展示治理完成：36,860 个 profile、10 个 Template release、24 个 active ModelCatalog 路由和当前 213,250 个 Outcome 路由均已显式，运行时动态猜测已删除。
- 医学量表目录治理完成：17 个 active scale 均有 canonical category，14 个分类缺口和 2 个 MBTI 元数据污染已归零；公开接口返回 17/17，空分类兼容逻辑与一次性治理入口均已删除。
- 31115866722 在一次性入口退役后确认 Attention projection 仅有 succeeded、缺口/重复/跨库异常均为 0，数据完整性红项已关闭。
- 兼容项已分为当前真实契约/数据依赖与具名退役候选，不进行整体直删；`legacy-v1` 是历史版本身份，不是待删默认别名。
- 已批准的 MySQL 5 表与 MongoDB 11 集合已备份、删除并独立对账；该授权已结束，其他字段、集合或缓存不因本次定档自动获得删除授权。

后续只需按 P2 跟踪：5 个具名兼容候选按日观察至 2026-09-05，以及 qs-operating-system DefinitionV2 显式模板版本契约。兼容指标出现命中、巡检中断或新数据红项时，台账必须即时更新。

目标模式继续按“只读验收 → 小批修复 → 聚焦测试 → Conventional Commit 类型前缀 + 中文主题 → push → CI/CD → 运行证据 → 更新台账”维护。每个可独立验收阶段立即提交，不跨阶段积攒；commit 或 push 本身不提升验收状态。
