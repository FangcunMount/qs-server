# 当前版本定档验收台账

> 当前结论：**有条件通过，已达到代码定档、部署确认与本轮生产物理退役目标。** 当前生产运行基线为 `main@6f08e51198bc06d6c503fd8ce8668d2eacc9c7cf`，一次性清理入口已由 `84b61b305779d69696b88f31322724aacd61246f` 移除，事实刷新时间为 2026-08-06 12:32 CST。Statistics 六条陈旧 running ledger、Attention 33 个历史投影缺口、MySQL 5 张遗留表和 MongoDB 11 个遗留集合均已按授权闭环；Runner 已升级至 `2.336.0`。当前保留的兼容包含真实跨仓调用、历史数据读取和外部能力协商，不能整体删除；5 个具名兼容候选继续观察至 2026-09-05。验收口径见[项目完成画像与验收标准](./08-项目完成画像与验收标准.md)。

> 本文只记录当前有效结论。绿色 workflow、候选构建、静态搜索和本地测试均不能替代原始 job 日志、实际 `DEPLOY_SHA`、运行进程指纹和生产证据。本次 5+11 对象的删除授权已一次性使用并闭环，不延伸为其他生产数据库或 Redis 破坏性操作授权。

## 1. 基线、证据与当前结论

| 项目 | 当前事实 | 判定 |
| --- | --- | --- |
| 分支 / 验收输入 | 生产运行 `main@6f08e51198bc06d6c503fd8ce8668d2eacc9c7cf`；一次性入口退役 `84b61b305779d69696b88f31322724aacd61246f` | 业务代码、migration 67、长期巡检与生产运行已验证；主干不保留可重复执行的一次性删除入口 |
| 当前基线 CI | [CI 31070641073](https://github.com/FangcunMount/qs-server/actions/runs/31070641073)、[Code Quality 31070640718](https://github.com/FangcunMount/qs-server/actions/runs/31070640718) 与 [SonarQube 31070641067](https://github.com/FangcunMount/qs-server/actions/runs/31070641067) 完整成功 | E5 已取得；全量 Go 测试、integration-tag、迁移/E2E、lint、边界、安全、部署契约和三制品全部通过 |
| 当前实际全服务部署 | [Production Deploy 31071074962](https://github.com/FangcunMount/qs-server/actions/runs/31071074962) 将 `6f08e511` 的 apiserver、collection 与 worker 全部换版成功，worker 治理端点反向验证成功 | E6 已取得；MySQL migration 67 在部署后为 clean |
| 本地门禁 | `actionlint`、`go test ./internal/pkg/configcontract ./internal/pkg/migration` 与 `git diff --check` 通过 | 与当前 CI 相互独立的 E2 证据 |
| API 文档 | apiserver 174 paths、collection 45 paths，与当前生成结果一致 | 当前静态契约一致 |
| 部署后生产巡检 | [Ping Runner 31071529902](https://github.com/FangcunMount/qs-server/actions/runs/31071529902) 覆盖 ServerA API/Nginx/DNS、collection 2/2、ServerD worker 3/3；当前 Actions job 均由 Runner `2.336.0` 执行 | Runner 升级黄项已闭环 |
| Attention 恢复闭环 | [Apply Audit 31065753675](https://github.com/FangcunMount/qs-server/actions/runs/31065753675) 在 `0a850833` 三 worker 上确认 `dry_run=false`：首轮扫描 42、已有 9、历史缺口 33、精确创建 33、错误 0、mismatch 0 | 已授权写入仅作用于事前分类的 33 个缺口；`1532b30a` 已停用临时任务并退役含固定 33 常量的一次性 apply workflow |
| 生产数据盘点 | [Data Status 31065753703](https://github.com/FangcunMount/qs-server/actions/runs/31065753703) 成功：Attention projection 184 条全为 `succeeded`，`missing_report_id=0`，重复组/记录均为 0；切点后 42 份 high/severe artifact 的 projection 缺口为 0；MySQL 和 Redis 只读状态也通过 | E7 已取得；Attention 主体、投影状态、唯一性与跨库缺口均收敛 |
| 兼容接口与数据回退观察 | [Compatibility Observation 31062020491](https://github.com/FangcunMount/qs-server/actions/runs/31062020491) 成功；三个公开接口、AnswerSheet 旧幂等 lookup/hit 和 Evaluation 无冻结 admission 回退共六个指标均存在，可见窗口增量均为 0，但 30 天前锚点均不存在 | E7 时点证据；公开接口窗口从 2026-08-06 09:04 CST 起算，数据回退窗口从 09:12 起算，均不能回溯宣称 30 天零命中；lookup 仅表示查询流量，只有旧集合 hit 才表示旧数据依赖 |
| Mongo 备份恢复预检 | [Backup Validate 31045715549](https://github.com/FangcunMount/qs-server/actions/runs/31045715549) 第二次执行成功；28 个归档集合均被读取，dry-run 0 restored / 0 failed | 证明归档可读，不等于实际恢复演练 |
| 生产物理退役 | [Backup 31070267072](https://github.com/FangcunMount/qs-server/actions/runs/31070267072) 创建固定备份集并对 11 个 Mongo 归档逐一 dry-run；[Apply 31071105270](https://github.com/FangcunMount/qs-server/actions/runs/31071105270) 删除 MySQL 5 表与 MongoDB 11 集合；[DB Status 31071330424](https://github.com/FangcunMount/qs-server/actions/runs/31071330424) 独立复核 | E7 已取得；MySQL 退役目标 0、共 28 表、migration `67/0`；MongoDB 退役列表空、共 18 集合、migration `21/false` |

当前基线已取得 E5、E6、部署后独立巡检与 E7，已无代码、接口或数据红项。本版因此达到“代码定档 + 部署确认 + 本轮生产物理退役”；兼容退役观察与长期容量/故障样本仍是黄色项，不回退当前定档结论。

## 2. 七个业务模块完成画像

| 类型 | 模块 | 状态 | 当前成立事实 | 未关闭项 / 退出条件 |
| --- | --- | --- | --- | --- |
| 核心 | Survey | 有条件通过 | 问卷发布、精确版本答卷、隐藏题拒绝、可靠提交、资源归属和摘要列表主链成立；填写人展示身份已补齐 | `answersheet_submit_idempotency`、历史无 admission、`task_id` 等兼容需指标和观察窗；published-head fallback 因 6 个 head version 缺 snapshot 必须保留 |
| 核心 | ModelCatalog | 有条件通过 | 25 个 head、24 个 active snapshot；canonical identity、DefinitionV2 与 active 唯一性检查均通过 | 49 条记录仍持久化旧 identity 字段；`valid/errors` 外部响应兼容仍有调用方 |
| 核心 | Evaluation | 有条件通过 | Assessment、Run、Outcome、checkpoint 与 durable event 主链成立；五项生产一致性查询为 0；非 canonical run identity 为 0 | 保留历史读归一化与治理入口；补长期观察窗和真实并发运行证据 |
| 核心 | Interpretation | 有条件通过 | 212,457 个 generation/run/artifact 生命周期一致；Attention projection 184 条全为 `succeeded`，42 份 high/severe artifact 缺口为 0，重复为 0；退役集合不存在 | 临时 FactReconciler 已关闭；36,860 个历史 artifact 缺 presentation profile 与 `legacy-v1` 模板属已知治理项，不是主链阻断 |
| 辅助 | Actor | 有条件通过 | IAM、Operator、Clinician、Testee、ProfileLink 责任链已装配；旧角色投影、旧受试者统计代码已退出 | 三个 Testee 旧物理列仍存在；`tags`、`org_id` 和默认组织仍是活跃线协议/单租户契约 |
| 辅助 | Plan | 有条件通过 | 286,552 个 task 均有 revisioned schedule fact；legacy fulfillment fallback 为 0；计划、入组、任务链可回放；当前 HEAD 三进程和治理巡检通过 | 缓存、通知、失败恢复与容量仍缺长期观测和故障演练 |
| 辅助 | Statistics | 通过 | 849,692 条 assessment fact 已 frozen；最新 publish 成功且 snapshot 对齐；v2 Redis key 已出现 | 六条未提交数据的陈旧 running ledger 已按授权精确结算为 `failed`，未删除任何幂等 Fact；独立对账无 running 遗留 |

## 3. 十个技术专题

| 技术专题 | 状态 | 当前判定 |
| --- | --- | --- |
| Event / Outbox / MQ | 黄 | 审计窗内 Mongo `answersheet.submitted`、`interpretation.report.generated` 各 768 条 published；MySQL `evaluation.requested`、`evaluation.outcome.committed` 各 768 条 published；无未知活动类型；当前 HEAD 消费端健康，仍缺长期窗口 |
| Cache / Signal / Warmup | 黄 | 当前 key Builder、Signal、Warmup 责任链成立；Statistics v2 已有真实 key，Assessment List v2 尚无真实 data key，旧 token 和兼容 key 不能提前清理 |
| Rate limit / Gate / Backpressure | 黄 | 限流、队列、payload gate 和 Outbox relay 均有边界与测试；生产 inflight、等待时长和下游慢场景证据不足 |
| LockLease / Leader / Duplicate suppression | 黄 | 三 worker 治理能力健康，Attention apply 在共享租约下仅一实例执行，精确创建 33、失败 0、mismatch 0，收敛后临时扫描任务已关闭。FactReconciler 保留为需明确授权才启用的恢复能力，不是常驻后台任务 |
| Retry / Manual governance | 黄 | 业务重试、Outbox、MQ NACK、hold 与人工动作已分层；当前窗口无 hold/dead-letter，仍缺长期窗口和人工恢复演练 |
| Data / Migration / Retirement | 黄 | MySQL migration 67、Mongo migration 21 均 clean；已授权的 5+11 物理退役对象已备份、恢复预检、删除并独立对账归零；Testee 三个旧列仍需单独批准与迁移 |
| IAM / Security / Tenant | 黄 | JWT 校验、authz snapshot、角色投影和资源归属链唯一；外部 IAM capability fallback 与当前单租户默认组织仍需保留 |
| Runtime / Scheduler / Shutdown | 黄 | 当前基线三进程已部署，apiserver、collection 2/2、worker 3/3 和治理端点均健康；Statistics 台账与 Attention 临时恢复任务均已收敛；Actions Runner 已升级至 `2.336.0` |
| Observability / Governance | 黄 | cache、resilience、outbox、worker governance 与兼容命中均有指标入口；三个公开兼容接口已进入每日 Prometheus 观察，首次生产运行因缺 30 天锚点判定窗口未满；其他退出项仍需分批固化 |
| Delivery / Performance | 黄 | 当前基线的 CI、三制品、实际全服务部署、运行指纹和部署后巡检已闭环；当前 k6 主入口可解析，但本轮未重新执行容量与故障压测 |

## 4. 十二角度统一矩阵

颜色含义：绿为当前基线主体成立；黄为有 owner 或退出条件但仍需补证；红为定档阻断。矩阵只做导航，具体结论以本台账其他章节为准。

| 验收角度 | Survey | ModelCatalog | Evaluation | Interpretation | Actor | Plan | Statistics |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1. 职责与边界 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 2. 代码与装配 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 3. 领域与不变量 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 4. REST / gRPC / Event | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 5. 数据与迁移 | 黄 | 黄 | 黄 | 黄 | 黄 | 绿 | 绿 |
| 6. 状态与一致性 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 7. 安全与隐私 | 绿 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 8. 缓存与并发 | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 9. 测试与质量门禁 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 10. 可观测与运维 | 黄 | 黄 | 黄 | 绿 | 黄 | 黄 | 绿 |
| 11. 性能与容量 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 12. 遗留与兼容 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |

## 5. 遗留代码与中间态兼容分类

### 5.1 已确认并删除的代码中间态

| 批次 | 代表提交 | 已关闭内容 |
| --- | --- | --- |
| 数据退役审计 | `0d7249f9` | 固化退役 MySQL 字段、表和索引只读审计 |
| 部署环境 | `63dbf946` | 删除已失效的部署环境变量兼容 |
| 路由契约 | `98b707bf` | `/practitioners` 降为仅运行时兼容，移出当前 OpenAPI |
| 接口 facade | `6c092b67` | 删除零调用 facade 中间层 |
| 管理接口 | `51e73892` | 删除永久 501 占位管理接口和空 registrar |
| 上下文语义 | `dbb39911` | 删除生产代码中的 `context.TODO()` 中间态 |
| 答卷展示 | `08bd5e30` | 补齐活跃消费方所需的 `filler_name` 身份解析，失败时回退用户 ID |
| 领域契约 | `b760116b` | 删除未生效的停用原因参数、虚假未来事件 TODO 和过期重构注释 |
| Attention 演练 | `b6a01ba2` | 为生产 worker 启用只读 reconcile dry-run，固定起始时间与配置契约 |
| 脚本入口 | `d2482f27` | 删除旧 Swagger 路径 fallback 与 k6 兼容包装脚本 |
| Statistics 结算 | `17f25ff7` | 精确结算 6 条陈旧 running ledger 后退役一次性 SQL、脚本与 workflow |
| 部署镜像导出 | `0a850833` | 串行化共享 Docker Desktop 的 pull/save 并加入可重现的并发/重试契约 |
| Attention 收敛 | `c82d4a9b` / `1532b30a` | 精确修复 33 个历史缺口，对账归零后停用临时扫描并删除固定数量的一次性 apply workflow |
| 物理对象退役 | `6f08e511` / `84b61b30` | 在固定备份、逐对象恢复预检和确认口令保护下删除 MySQL 5 表与 MongoDB 11 集合，独立对账后移除一次性入口 |

当前生产 Go 代码已无 `TODO`、`FIXME`、`XXX`、`HACK`。静态遗留关键词和零调用搜索已经饱和：剩余命中均能映射到历史读、当前线协议、外部 IAM 能力、指标观察窗、迁移审计或部署回滚边界；不能仅凭 `legacy`、`v1`、`deprecated` 命名删除。

### 5.2 当前契约或数据兼容，不得批量删除

下表不是“17 段都应在本版删除的过渡代码”。`wait-report`、`task_id/origin_ref` 在 qs-collection-system 和 seeddata-runner 中仍有直接生产调用，ModelCatalog `valid/errors` 仍有 qs-operating-system 读取回退；published-head、历史 identity、IAM、Redis 项则有已存数据或外部能力依赖。将这些整体直接删除会破坏当前客户端、历史读或运行安全，因此本轮明确否决“不兼容、全部直删”。只有 5 个具名退役候选进入 2026-09-05 观察线，其余项必须先完成消费方或数据迁移。

| 兼容项 | 保留理由 | 退出证据 |
| --- | --- | --- |
| `/practitioners` | `/clinicians` 已是 canonical，但旧路由仍有运行时计量入口 | 指标 `qs_actor_deprecated_practitioner_route_total` 连续 30 天为 0，调用方确认后删除 |
| ModelCatalog `valid/errors` | qs-operating-system 仍会从 `passed/issues` 回退读取 | 前端发布、观察窗归零后退出 |
| Statistics REST `validate_only` | 仓内 CLI 已迁移 `mode`，外部调用仍需指标证明 | deprecated 参数命中 30 天为 0 |
| AnswerSheet `task_id` / `origin_ref` | collection 与 seeddata-runner 等调用链仍使用 | 跨仓迁移、流量归零、历史事件回放通过 |
| Testee `tags`、Actor/Clinician `org_id` | 当前线协议仍公开且有消费者 | 调用方版本窗口与契约升级完成 |
| Collection `wait-report` / `interpreted` | 当前产品流程仍使用 | 新状态协议完成迁移并经过观察窗 |
| `GenerateReportFromAssessment` | 静态跨仓无消费者，但生产观察窗不足 | deprecated RPC 指标连续 30 天为 0 |
| `PayloadGateLegacyIncomplete` | 历史事件可能缺少新字段 | 历史积压清空、兼容命中归零 |
| Evaluation / Interpretation 历史 identity 归一化 | 不可变历史事实读取仍需要 | 历史事实回填或生命周期自然退出 |
| Questionnaire published-head / exact-version fallback | 生产有 6 个 head version 缺 snapshot | 完整性查询归零且 fallback 指标连续归零 |
| IAM 通用 claim key 与 service-token fallback | 外部 IAM 契约与能力协商仍活跃 | IAM 统一升级并完成跨服务验收 |
| 默认 `org_id=1` | 当前单租户运行契约，不是未实现 TODO | 多租户契约落地后显式迁移 |
| ModelCatalog 旧 identity 持久化字段 | 49 条生产记录仍包含旧字段 | 新写切断、回填/归档、读指标归零 |
| `answersheet_submit_idempotency` | 当前 0 文档，但嵌入幂等 miss 时仍有 fallback 读取；新幂等键也会触发 lookup | 旧集合 hit 连续 30 天为 0、集合仍为 0、删除演练通过；lookup 不能作为归零门槛 |
| Redis Statistics 旧 12 键 | v2 仅有 3 个真实键，旧代实例退出未证明 | 旧实例退出、最长 TTL 过去、单独批准 |
| Redis Assessment List 39,656 个旧 token | v2 data key 尚未真实出现，旧 token 无 TTL | v2 真实命中、旧实例退出、单独清理批准 |
| `qs_cache_lock_*` 指标 | 基础设施 Grafana 仍依赖 | 仪表盘迁移并完成观察窗 |

#### 5.2.1 兼容接口与数据回退正式观察窗

仓库尚无可用的发布 tag，因此不虚构 SemVer；5 个退役候选的 6 个指标统一进入具名退出基线 `compat-retirement-2026-09`。生产 run 31062020491 对三个公开接口和两个 AnswerSheet 指标各返回 1 个当前 series，Evaluation 回退因结果标签预初始化返回 4 个当前 series；六项均为 `window_increase=0`、`anchor_series=absent`。因此观察窗分别从首次成功运行起算，不借用不完整的历史数据，也不把“series 存在”解释为发生过兼容命中。

| 兼容项 | 指标 | Owner | 观察窗 | 退出基线与条件 |
| --- | --- | --- | --- | --- |
| `/practitioners` | `qs_actor_deprecated_practitioner_route_total` | Actor | 2026-08-06 09:04–2026-09-05 09:04 CST | `compat-retirement-2026-09`；每日 workflow 连续成功、30 天增量为 0、跨仓调用方确认后单独删除 |
| Statistics `validate_only` | `qs_statistics_deprecated_validate_only_total` | Statistics | 2026-08-06 09:04–2026-09-05 09:04 CST | `compat-retirement-2026-09`；每日 workflow 连续成功、30 天增量为 0、外部调用方确认后删除参数兼容 |
| `GenerateReportFromAssessment` | `qs_interpretation_deprecated_generate_report_from_assessment_total` | Interpretation | 2026-08-06 09:04–2026-09-05 09:04 CST | `compat-retirement-2026-09`；每日 workflow 连续成功、30 天增量为 0、所有 gRPC 消费方确认后删除旧 RPC |
| AnswerSheet 旧幂等集合 | `qs_answersheet_legacy_idempotency_fallback_lookup_total` / `qs_answersheet_legacy_idempotency_fallback_hit_total` | Survey | 2026-08-06 09:12–2026-09-05 09:12 CST | `compat-retirement-2026-09`；lookup 只观察回退查询量，不要求归零；旧集合 hit 30 天增量为 0、集合仍为 0、删除演练通过后，另行批准删除集合与 fallback |
| Evaluation 无冻结 admission 回退 | `qs_evaluation_assessment_intake_legacy_binding_total` | Evaluation | 2026-08-06 09:12–2026-09-05 09:12 CST | `compat-retirement-2026-09`；各结果标签合计 30 天增量为 0、历史数据 Owner 确认后再收敛回退 |

三个公开兼容指标、AnswerSheet hit 或 Evaluation 回退出现增量时，对应项立即回到“活跃兼容”，观察窗从最后一次命中重新计时；AnswerSheet lookup 增量不触发重计时，因为当前新幂等键也会走该查询。workflow 失败、指标缺失或历史锚点不足不计入连续窗口。窗口完成只是删除的必要条件，不是自动删除授权。

### 5.3 已完成生产物理退役

| 对象 | 生产证据 | 当前代码责任链 | 当前动作 |
| --- | --- | --- | --- |
| MySQL 4 个 `seed_orphan_*_bak_hist_v1*` 表 | 删除前精确计数均为 0 | 无运行时读写 | 31071105270 已 DROP；31071330424 独立复核无残留 |
| MySQL `analytics_scan_watermarks` | 删除前精确计数为 0 | migration 67 统一退役，无运行时 owner | 31071105270 已 DROP；migration `67/0` 防止环境回漂 |
| Mongo `assessment_models_legacy_20260717_084636` | 删除前 28 个文档 | 无当前运行时 owner | 31070267072 归档可恢复预检；31071105270 已删除 |
| Mongo 10 个 `__seed_orphan_backup_hist_v1...` 集合 | 删除前每个 269 个文档 | 无当前运行时 owner | 10 个归档逐一通过 dry-run；31071105270 已删除，31071330424 退役列表为空 |

### 5.4 高置信但生产观察不足

| 对象 | 当前证据 | 仍缺证据 |
| --- | --- | --- |
| MySQL `testee.total_assessments` / `last_assessment_at` / `last_risk_level` | active testee 的三个旧统计列全部为默认值，且大量与 canonical summary 不一致；当前代码不再读写 | 单独的列级 schema 备份、DROP COLUMN 批准、migration 与回滚验证；本次 5+11 授权不包含它们 |
| deprecated `GenerateReportFromAssessment` | 本仓及已检跨仓静态零调用；31061609088 当前增量为 0，已启动正式窗口 | 连续 30 天生产指标与所有外部消费者确认 |
| `/practitioners` | canonical `/clinicians` 已被当前前端和 seeddata-runner 使用；31061609088 当前增量为 0，已启动正式窗口 | 连续 30 天旧路由指标与其他调用方确认 |
| 兼容字段与 fallback | 均有明确 current owner 或历史读入口 | 独立指标、Owner、观察窗和退出版本 |

## 6. 数据库与缓存当前画像

### 6.1 MySQL

- MySQL 8.0.36，隔离级别 READ-COMMITTED，migration 67，`dirty=false`；共 28 张正式表。
- 四个 seed backup 表和 `analytics_scan_watermarks` 已删除；31071330424 的 `unexpected_retirement_table` 查询返回空。
- 64,991 个 active testee 的三个旧统计列全部为默认值，且各有 35,608 条与 canonical summary 不一致；这些列不能作为回滚数据源。
- Evaluation 五项一致性检查均为 0，非 canonical run identity 为 0。
- Statistics 最新 publish run `631524273684492846` 成功，`as_of=2026-08-05`，generation 15，snapshot 对齐。
- 六条陈旧 running ledger 在写入前反复确认 `data_committed_at IS NULL`、标准 rebuild 进程/容器为 0、Redis DB 6 的 `cache:lock:statistics:1` TTL 为 -2；经负责人批准后由 31063053544 精确更新 6 行为 `failed/stale_run_reconciled`，eligible/updated/verified=`6/6/6`。独立 MySQL 只读运行 31063110773 确认原 6 行不再属于 running 分组。

### 6.2 MongoDB

- MongoDB 7.0.31，migration 21，`dirty=false`；共 18 个集合、1,488,184 个对象、逻辑数据约 2.782 GB。
- 18 个集合精确分类为：17 个当前运行时/兼容/迁移集合、1 个系统集合 `system.profile`。原 11 个退役集合已全部删除，`system.profile` 不是遗留业务集合。
- `answersheet_submit_idempotency` 为 0 文档但存在活跃 fallback owner，不能按空集合删除。
- ModelCatalog 共 49 条：25 个 head、24 个 snapshot、24 个 active snapshot；canonical identity、DefinitionV2 和 active duplicate 检查均无异常。
- Questionnaire 共 136 条：42 个 head、94 个 snapshot、38 个 active snapshot；角色、状态和重复检查无异常，但有 6 个 head version 缺 snapshot。
- Interpretation 十个当前集合均存在；generation、run、artifact 各 212,457 条，生命周期一致性检查均为 0；Attention projection 184 条全为 `succeeded`、缺口 0、重复 0；四个已退役集合均不存在。
- 36,860 个 active artifact 缺 `presentation_profile`，4 个 published template 均为 `legacy-v1`，属于已知后续治理项，不等同于可删除遗留集合。
- 全库归档 `qs_mongodb_backup_20260806_021432.archive.gz` 约 254 MiB；本次固定退役备份集位于 `/opt/backups/qs-server/legacy-database-retirement/legacy-db-retirement-20260806-v1`，包含 MySQL 5 表 DDL 和 MongoDB 11 个独立归档，删除前已全部通过恢复预检。

### 6.3 Redis

- Redis 7.0.15，used memory 约 178.48 MiB，maxmemory 256 MiB，策略 `allkeys-lru`。
- 旧候选计数器在 DB 0–8 均为 0；这只能证明采样时点，不等于观察窗完成。
- 最新只读时点，DB 2 有 74 个 questionnaire、19 个 model 当前 key；DB 3 扫描到 32,134 个 assessment 当前 key。缓存计数会随 TTL、流量和 SCAN 并发变化，不能当成持久数据总量。
- DB 4 有 3 个 Statistics 当前 key 和 12 个兼容 key；DB 7 有 39,656 个 assessment-list 兼容 token 与 2 个 hotset；DB 8 有 321,868 个当前 rank/other family key。
- Redis 是运行时状态，不是迁移表；任何清理必须精确到 DB、family、namespace、TTL、实例退出和审批。

## 7. 当前结论与目标队列

### 7.1 本轮已关闭门禁

| ID | 已关闭目标 | 证据 |
| --- | --- | --- |
| FINAL-P1-001 | 当前基线完整 CI | 31070641073、31070640718、31070641067 均成功；全量测试、integration-tag、迁移/E2E、lint、边界、安全、部署契约和三制品通过 |
| FINAL-P1-002 | 当前基线实际部署与运行指纹 | 31071074962 实际部署 `6f08e511` 全部三服务并通过 worker 治理反向验证；31071529902 独立巡检通过 |
| FINAL-P1-003A | Attention dry-run 演练 | 31051404012 验证三实例目标 SHA、有效 dry-run 配置和三个串行轮次，均扫描 41、缺失 33、创建 0、失败 0；31051481975 复核 projection 仍为 117 |
| FINAL-P1-003B | 处置 33 个 Attention 高风险缺口 | 31065753675 确认精确创建 33、错误 0、mismatch 0；31065753703 确认 184 条 projection 全成功、42 份 high/severe artifact 缺口 0、重复 0；`1532b30a` 关闭临时任务 |
| FINAL-P1-004 | 结算六条 Statistics running ledger | 31063053544 在无进程、无容器、无 Redis 租约的前提下精确结算 6 行，写后自检为 `6/6/6/6/6`；31063110773 独立只读对账通过，未删除幂等 Fact |
| FINAL-P1-005 | 固化兼容分类与观察窗 | 17 项已拆分为当前真实契约/数据依赖与 5 个具名退役候选；31062020491 确认 6 个指标当前增量为 0，Owner、观察窗、重计时规则和退出门槛已记录；整体直删决策已被真实调用/数据证据否决 |
| FINAL-P1-006 | 升级 ServerD 自托管 Runner | 当前 workflow 原始日志确认 Runner `2.336.0` 正常接单；31071529902 完成升级后独立生产巡检 |
| FINAL-P2-002 | 生产物理遗留对象退役 | 31070267072 完成 5+11 固定备份及恢复预检；31071105270 完成精确删除；31071330424 确认 MySQL `67/0`、28 表、退役表 0，MongoDB `21/false`、18 集合、退役列表空 |
| FINAL-P2-003 | 退出 GitHub Actions Node 20 强制兼容并恢复仓内覆盖率制品 | `286010dd` 删除无有效消费链的 Codecov 外部上传；CI 31061044971 完整成功，`Run Tests` 生成并通过 `upload-artifact@v7` 上传 `coverage.out`，三应用制品也全部成功 |

### 7.2 仍开放目标

| ID | 级别 | 目标 | 当前证据 | 退出条件 | Owner / 期限 |
| --- | --- | --- | --- | --- | --- |
| FINAL-P2-001 | P2 | Interpretation presentation/template 收敛 | 36,860 个 artifact 缺 profile，4 个 published template 为 `legacy-v1` | 回填、回放和模板升级计划；若不纳入本版本则书面接受 | Interpretation / 负责人确认 |
| FINAL-P2-004 | P2 | 执行兼容退役观察 | 5 个具名候选已进入 `compat-retirement-2026-09`；首次观察的 6 个指标可见增量均为 0，但无 30 天锚点 | 每日 workflow 连续成功到 2026-09-05；命中则对应项重新计时；窗口完成后仍需按项批准删除 | Actor / Statistics / Interpretation / Survey / Evaluation / 2026-09-05 |

当前已无未物理完成的 P1。剩余开放项是不阻断定档的历史展示治理和兼容观察；观察窗完成不会自动产生新的删除授权。

## 8. 验证入口与证据缺口

| 门禁 | 当前结果 |
| --- | --- |
| 本地 Go 门禁 | `go test ./internal/pkg/configcontract ./internal/pkg/migration`、`actionlint` 和 `git diff --check` 通过；全仓、integration-tag 与三制品结果见 31070641073 |
| `make lint-boundaries` | 通过，0 issues |
| `make docs-verify` | 通过；apiserver 174、collection 45 paths |
| `make docs-hygiene` / `make docs-facts` | 通过；150 篇现行 Markdown、7 个业务模块、6 个核心事件 |
| `k6 inspect scripts/perf/k6/mixed.js` | 通过，当前主压测入口可解析 |
| 当前基线 CI | 31070641073、31070640718、31070641067 完整成功；全量测试、迁移集成、E2E、部署契约和三制品全绿 |
| 当前基线 Deploy | 31071074962 实际部署 `6f08e511` 全部三服务成功，三个 worker 治理端点反向验证通过 |
| 部署后巡检 | 31071529902 成功；API、Nginx/DNS、collection 2/2 和 worker 3/3 健康；Runner `2.336.0` 正常接单 |
| Attention dry-run | 31051404012 成功；3 个串行成功轮次，均 scanned=41、missing=33、existing=8、mismatched=0、created=0 |
| Attention 跨库只读分类 | `go test ./internal/pkg/configcontract`、actionlint、脚本契约和临时 MongoDB 7 / MySQL 8 演练均通过；31060860200 生产复跑成功：gap reports/Testees/Assessments = 33/33/33，missing/deleted/cross-org/relation mismatch 均为 0，already/needs key focus = 0/33，全程只读 |
| Attention apply 与收敛后对账 | 31065753675 成功；3 worker、精确 created=33、error=0、mismatched=0。31065753703 成功；projection succeeded=184、missing/duplicate=0，high/severe coverage missing=0；`1532b30a` 已关闭临时任务 |
| 兼容接口与数据回退观察 | `go test ./internal/pkg/configcontract`、actionlint 和脚本正反例通过；31062020491 生产只读运行成功，六个指标均有 current series、可见增量均为 0、30 天 anchor 均缺失；公开接口窗口从 2026-08-06 09:04 CST 开始，数据回退窗口从 09:12 开始；AnswerSheet lookup 仅作流量观测，hit 才是旧数据依赖信号 |
| 生产数据盘点 | 31065753703 成功；Attention projection 184 条全成功且缺口/重复为 0。Statistics 结算 31063053544 与独立 MySQL 对账 31063110773 均成功，原六条 running 已收敛为 `failed` |
| Mongo 备份验证 | 31045715549 第二次成功；28 个归档集合 dry-run 0 failed |
| 物理退役备份 / 执行 / 独立对账 | 31070267072 / 31071105270 / 31071330424 均成功；5 张 MySQL 表和 11 个 MongoDB 集合已退役，migration 清洁且残留清单为空 |
| 静态 deadcode | 工具不可用；未下载未固定版本的 latest 工具，候选改由责任链与零调用搜索逐项确认 |

## 9. 破坏性操作边界

本台账不授权任何新的生产 `DROP`、`DELETE`、Mongo collection 删除、Redis 批量清理或未列明的 ledger 状态修改。Attention 33 个缺口的 apply 授权与本次 5+11 物理退役授权均已一次性使用并闭环，不得复用或扩展。

1. MySQL 物理删除前必须具备当前 schema/row count、备份、恢复命令、审批、执行窗口和执行后 migration/schema 核验。
2. 本次退役备份集必须按运维保留策略保管；`mongorestore --dryRun` 证明归档可解析，不等于新库实际导入的完整灾备演练。
3. Redis 清理必须精确到 DB、family、namespace 和 key pattern；先证明旧实例退出并等待最长 data TTL。无 TTL token 只能单独审批，禁止宽泛通配删除。
4. Statistics 六条结算授权已在 31063053544 一次性使用并闭环；不得复用为其他 Run 的批量状态修改授权。
5. Attention reconcile 授权已在 31065753675 使用，31065753703 对账后由 `1532b30a` 停用临时事实恢复任务；后续重新启用需新的对象清单、dry-run 和明确授权。

## 10. 定档签署结果

当前版本已从“阻断”升级为“有条件通过”，并达到代码定档与部署确认：

- 当前生产 `main@6f08e511` 的全仓测试、必跑 CI、三制品、实际部署和部署后巡检已闭环；`84b61b30` 已删除一次性清理入口。
- Statistics 六条陈旧 running ledger 已精确结算并独立对账；Attention 33 个缺口已精确收敛、跨库归零并停用临时任务。
- 七个业务模块与十二角度已无红项；兼容项已分为当前真实契约/数据依赖与具名退役候选，不进行整体直删。
- 已批准的 MySQL 5 表与 MongoDB 11 集合已备份、删除并独立对账；该授权已结束，其他字段、集合或缓存不因本次定档自动获得删除授权。

生产确认仍需持续跟踪两类黄色项：Interpretation 历史 presentation/template 治理，以及 5 个具名兼容退役候选按日观察至 2026-09-05。兼容指标出现命中、巡检中断或当前红项重现时，台账必须立即恢复对应阻断结论。

目标模式继续按“只读验收 → 小批修复 → 聚焦测试 → Conventional Commit 类型前缀 + 中文主题 → push → CI/CD → 运行证据 → 更新台账”维护。每个可独立验收阶段立即提交，不跨阶段积攒；commit 或 push 本身不提升验收状态。
