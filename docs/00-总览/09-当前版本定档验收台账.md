# 当前版本定档验收台账

> 当前结论：**阻断，尚未达到定档签署条件。** 本台账绑定已验证的业务代码与生产运行基线 `main@7f84b29bf3dab74cb3f71357612850d9a4954d82`，事实刷新时间为 2026-08-06 07:43 CST。七个业务模块、十个技术专题和十二个验收角度均已进入统一台账；该基线的 CI/CD、运行指纹、生产巡检、Attention dry-run 和数据库只读复核已闭环。剩余阻断项是 33 个高风险 Attention 投影缺口的处置决策、Statistics 六条陈旧运行台账的结算、活跃兼容项的风险接受，以及 ServerD 自托管 Runner 在 2026-08-10 前完成升级。生产物理退役对象保持待审批，不纳入自动删除。验收口径见[项目完成画像与验收标准](./08-项目完成画像与验收标准.md)。

> 本文只记录当前有效结论。绿色 workflow、候选构建、静态搜索和本地测试均不能替代原始 job 日志、实际 `DEPLOY_SHA`、运行进程指纹和生产只读证据。生产数据库与 Redis 的破坏性操作不在本台账授权范围内。

## 1. 基线、证据与当前结论

| 项目 | 当前事实 | 判定 |
| --- | --- | --- |
| 分支 / 验收输入 | `main@7f84b29bf3dab74cb3f71357612850d9a4954d82` | 当前已验证业务代码、验收自动化与生产运行基线 |
| 当前基线 CI | [CI 31056398040](https://github.com/FangcunMount/qs-server/actions/runs/31056398040) 完整成功；[CodeQL 31056396517](https://github.com/FangcunMount/qs-server/actions/runs/31056396517) 与 [SonarQube 31056398258](https://github.com/FangcunMount/qs-server/actions/runs/31056398258) 成功 | E5 已取得；race、integration-tag、E2E、部署契约、安全、维护性和三制品全部通过；上传与建置相关 check annotations 均为 0 |
| 当前实际全服务部署 | [Production Deploy 31056961683](https://github.com/FangcunMount/qs-server/actions/runs/31056961683) 原始日志确认 `DEPLOY_SHA=7f84b29bf3dab74cb3f71357612850d9a4954d82`，计划并实际部署 apiserver、collection、worker；worker 3/3、collection 2/2、40 次请求覆盖两副本，三个 worker 治理端点反向验证成功 | E6 已取得；三个镜像构建 job 的 Node 20 兼容 annotation 均为 0 |
| 本地门禁 | `go test ./...`、`make lint-boundaries`、`make docs-verify`、`k6 inspect scripts/perf/k6/mixed.js` 通过 | 当前 HEAD 的 E2 证据 |
| API 文档 | apiserver 174 paths、collection 45 paths，与当前生成结果一致 | 当前静态契约一致 |
| 部署后生产巡检 | [Ping Runner 31057268188](https://github.com/FangcunMount/qs-server/actions/runs/31057268188) 成功：ServerA API/Nginx/DNS、collection 2/2、ServerD worker 3/3、三 worker 治理端点均通过；ServerD job 为 14 秒 | 当前 SHA 部署后的独立运行证据；唯一 annotation 仍是 Runner `2.334.0` 的 2026-08-10 停止接单时限 |
| Attention dry-run | [Attention Audit 31051404012](https://github.com/FangcunMount/qs-server/actions/runs/31051404012) 成功：三实例目标镜像和配置一致，3 个串行成功轮次均扫描 41、已有 8、缺失 33、不一致 0、创建 0、连续失败 0 | dry-run 与零写入指标成立；证明当前是轮次互斥而非常驻单 leader；不授权 apply |
| 生产数据盘点 | [Data Status 31051481975](https://github.com/FangcunMount/qs-server/actions/runs/31051481975) 成功，覆盖 MySQL、MongoDB、Redis、Outbox 与模块一致性；Attention projection 仍为 117，缺口仍为 33 | E7 时点证据，交叉证明当前基线 dry-run 未创建投影；不授权写入或删除 |
| Mongo 备份恢复预检 | [Backup Validate 31045715549](https://github.com/FangcunMount/qs-server/actions/runs/31045715549) 第二次执行成功；28 个归档集合均被读取，dry-run 0 restored / 0 failed | 证明归档可读，不等于实际恢复演练 |

当前基线已取得 E5、E6、部署后巡检与 E7。版本仍为“阻断”不是因为交付流水线失败，而是两个生产状态处置需要显式授权、活跃兼容观察窗尚未形成风险接受，并且 ServerD Runner 仍有明确停服时限。

## 2. 七个业务模块完成画像

| 类型 | 模块 | 状态 | 当前成立事实 | 未关闭项 / 退出条件 |
| --- | --- | --- | --- | --- |
| 核心 | Survey | 有条件通过 | 问卷发布、精确版本答卷、隐藏题拒绝、可靠提交、资源归属和摘要列表主链成立；填写人展示身份已补齐 | `answersheet_submit_idempotency`、历史无 admission、`task_id` 等兼容需指标和观察窗；published-head fallback 因 6 个 head version 缺 snapshot 必须保留 |
| 核心 | ModelCatalog | 有条件通过 | 25 个 head、24 个 active snapshot；canonical identity、DefinitionV2 与 active 唯一性检查均通过 | 49 条记录仍持久化旧 identity 字段；`valid/errors` 外部响应兼容仍有调用方 |
| 核心 | Evaluation | 有条件通过 | Assessment、Run、Outcome、checkpoint 与 durable event 主链成立；五项生产一致性查询为 0；非 canonical run identity 为 0 | 保留历史读归一化与治理入口；补长期观察窗和真实并发运行证据 |
| 核心 | Interpretation | **阻断** | 212,423 个 generation/run/artifact/catalog 生命周期一致；退役集合不存在；当前 worker 的 dry-run 已实际扫描 41、已有 8、缺失 33、创建 0 | 33 个高风险 projection 仍缺失，apply 未获授权；共享租约保证轮次互斥，但短轮次释放后同一周期可能由不同实例串行复扫 |
| 辅助 | Actor | 有条件通过 | IAM、Operator、Clinician、Testee、ProfileLink 责任链已装配；旧角色投影、旧受试者统计代码已退出 | 三个 Testee 旧物理列仍存在；`tags`、`org_id` 和默认组织仍是活跃线协议/单租户契约 |
| 辅助 | Plan | 有条件通过 | 286,552 个 task 均有 revisioned schedule fact；legacy fulfillment fallback 为 0；计划、入组、任务链可回放；当前 HEAD 三进程和治理巡检通过 | 缓存、通知、失败恢复与容量仍缺长期观测和故障演练 |
| 辅助 | Statistics | **阻断** | 849,692 条 assessment fact 已 frozen；最新 publish 成功且 snapshot 对齐；v2 Redis key 已出现 | 六条陈旧 running ledger 均无 `data_committed_at`；只读前提已满足，但状态处置未获授权 |

## 3. 十个技术专题

| 技术专题 | 状态 | 当前判定 |
| --- | --- | --- |
| Event / Outbox / MQ | 黄 | 审计窗内 Mongo `answersheet.submitted`、`interpretation.report.generated` 各 768 条 published；MySQL `evaluation.requested`、`evaluation.outcome.committed` 各 768 条 published；无未知活动类型；当前 HEAD 消费端健康，仍缺长期窗口 |
| Cache / Signal / Warmup | 黄 | 当前 key Builder、Signal、Warmup 责任链成立；Statistics v2 已有真实 key，Assessment List v2 尚无真实 data key，旧 token 和兼容 key 不能提前清理 |
| Rate limit / Gate / Backpressure | 黄 | 限流、队列、payload gate 和 Outbox relay 均有边界与测试；生产 inflight、等待时长和下游慢场景证据不足 |
| LockLease / Leader / Duplicate suppression | 黄 | 三 worker 治理能力健康，Attention dry-run 三个轮次均成功且无并发写；当前实现是短轮次互斥，不是跨 interval 的常驻 leader，同周期三个实例会串行复扫。事实恢复与常规失败重试共用同一租约键，不能在未重构协调器前把其中一条直接改成长驻持锁；apply 依赖幂等 projector 收敛，完成后应关闭临时事实恢复任务 |
| Retry / Manual governance | 黄 | 业务重试、Outbox、MQ NACK、hold 与人工动作已分层；当前窗口无 hold/dead-letter，仍缺长期窗口和人工恢复演练 |
| Data / Migration / Retirement | 黄 | MySQL migration 66、Mongo migration 21 均 clean；候选对象已分类；破坏性删除均未授权 |
| IAM / Security / Tenant | 黄 | JWT 校验、authz snapshot、角色投影和资源归属链唯一；外部 IAM capability fallback 与当前单租户默认组织仍需保留 |
| Runtime / Scheduler / Shutdown | 黄 | 当前基线三进程已部署，apiserver、collection 2/2、worker 3/3 和治理端点均健康；Statistics 台账仍未结算；ServerD Actions Runner 必须在 2026-08-10 前滚动升级 |
| Observability / Governance | 黄 | cache、resilience、outbox、worker governance 与兼容命中均有指标入口；多个退出项仍缺完整观察窗 |
| Delivery / Performance | 黄 | 当前基线的 CI、三制品、实际全服务部署、运行指纹和部署后巡检已闭环；自托管 Runner 版本仍阻断可持续交付；当前 k6 主入口可解析，但本轮未重新执行容量与故障压测 |

## 4. 十二角度统一矩阵

颜色含义：绿为当前基线主体成立；黄为有 owner 或退出条件但仍需补证；红为定档阻断。矩阵只做导航，具体结论以本台账其他章节为准。

| 验收角度 | Survey | ModelCatalog | Evaluation | Interpretation | Actor | Plan | Statistics |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1. 职责与边界 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 2. 代码与装配 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 3. 领域与不变量 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 4. REST / gRPC / Event | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 5. 数据与迁移 | 黄 | 黄 | 黄 | **红** | 黄 | 绿 | **红** |
| 6. 状态与一致性 | 绿 | 绿 | 绿 | **红** | 绿 | 绿 | **红** |
| 7. 安全与隐私 | 绿 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 8. 缓存与并发 | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 9. 测试与质量门禁 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 10. 可观测与运维 | 黄 | 黄 | 黄 | **红** | 黄 | 黄 | **红** |
| 11. 性能与容量 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 12. 遗留与兼容 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |

## 5. 遗留代码与中间态兼容分类

### 5.1 已确认并删除的代码中间态

| 批次 | 代表提交 | 已关闭内容 |
| --- | --- | --- |
| 数据退役审计 | `0d7249f9` | 固化退役 MySQL 字段、表和索引只读审计 |
| 部署环境 | `63dbf946` | 删除已失效的部署环境变量兼容 |
| 路由契约 | `98b707bf` | `/practitioners` 降为仅运行时兼容，移出当前 OpenAPI |
| 接口 facade | `6c092b67` | 删除零调用 facade 中间层 |
| 管理接口 | `51e73892` | 删除永久 501 占位管理接口和空 registrar |
| 上下文语义 | `dbb39911` | 删除生产代码中的 `context.TODO()` 中间态 |
| 答卷展示 | `08bd5e30` | 补齐活跃消费方所需的 `filler_name` 身份解析，失败时回退用户 ID |
| 领域契约 | `b760116b` | 删除未生效的停用原因参数、虚假未来事件 TODO 和过期重构注释 |
| Attention 演练 | `b6a01ba2` | 为生产 worker 启用只读 reconcile dry-run，固定起始时间与配置契约 |
| 脚本入口 | `d2482f27` | 删除旧 Swagger 路径 fallback 与 k6 兼容包装脚本 |

当前生产 Go 代码已无 `TODO`、`FIXME`、`XXX`、`HACK`。静态遗留关键词和零调用搜索已经饱和：剩余命中均能映射到历史读、当前线协议、外部 IAM 能力、指标观察窗、迁移审计或部署回滚边界；不能仅凭 `legacy`、`v1`、`deprecated` 命名删除。

### 5.2 活跃兼容，不得删除

| 兼容项 | 保留理由 | 退出证据 |
| --- | --- | --- |
| `/practitioners` | `/clinicians` 已是 canonical，但旧路由仍有运行时计量入口 | 指标 `qs_actor_deprecated_practitioner_route_total` 连续 30 天为 0，调用方确认后删除 |
| ModelCatalog `valid/errors` | qs-operating-system 仍会从 `passed/issues` 回退读取 | 前端发布、观察窗归零后退出 |
| Statistics REST `validate_only` | 仓内 CLI 已迁移 `mode`，外部调用仍需指标证明 | deprecated 参数命中 30 天为 0 |
| AnswerSheet `task_id` / `origin_ref` | collection 与 seeddata-runner 等调用链仍使用 | 跨仓迁移、流量归零、历史事件回放通过 |
| Testee `tags`、Actor/Clinician `org_id` | 当前线协议仍公开且有消费者 | 调用方版本窗口与契约升级完成 |
| Collection `wait-report` / `interpreted` | 当前产品流程仍使用 | 新状态协议完成迁移并经过观察窗 |
| `GenerateReportFromAssessment` | 静态跨仓无消费者，但生产观察窗不足 | deprecated RPC 指标连续 30 天为 0 |
| `PayloadGateLegacyIncomplete` | 历史事件可能缺少新字段 | 历史积压清空、兼容命中归零 |
| Evaluation / Interpretation 历史 identity 归一化 | 不可变历史事实读取仍需要 | 历史事实回填或生命周期自然退出 |
| Questionnaire published-head / exact-version fallback | 生产有 6 个 head version 缺 snapshot | 完整性查询归零且 fallback 指标连续归零 |
| IAM 通用 claim key 与 service-token fallback | 外部 IAM 契约与能力协商仍活跃 | IAM 统一升级并完成跨服务验收 |
| 默认 `org_id=1` | 当前单租户运行契约，不是未实现 TODO | 多租户契约落地后显式迁移 |
| ModelCatalog 旧 identity 持久化字段 | 49 条生产记录仍包含旧字段 | 新写切断、回填/归档、读指标归零 |
| `answersheet_submit_idempotency` | 当前 0 文档，但嵌入幂等 miss 时仍有 fallback 读取 | 30 天 miss/fallback 为 0，删除演练通过 |
| Redis Statistics 旧 12 键 | v2 仅有 3 个真实键，旧代实例退出未证明 | 旧实例退出、最长 TTL 过去、单独批准 |
| Redis Assessment List 39,656 个旧 token | v2 data key 尚未真实出现，旧 token 无 TTL | v2 真实命中、旧实例退出、单独清理批准 |
| `qs_cache_lock_*` 指标 | 基础设施 Grafana 仍依赖 | 仪表盘迁移并完成观察窗 |

### 5.3 高置信退役候选，生产删除未授权

| 对象 | 生产证据 | 当前代码责任链 | 当前动作 |
| --- | --- | --- | --- |
| MySQL 4 个 `seed_orphan_*_bak_hist_v1*` 表 | 精确计数均为 0 | 无运行时读写 | 等 MySQL 备份、保留期限和明确 DROP 批准 |
| MySQL `analytics_scan_watermarks` | 精确计数为 0 | 仅 migration 37，无运行时 owner | 同上 |
| MySQL `testee.total_assessments` / `last_assessment_at` / `last_risk_level` | 64,676 个 active testee 均为默认值；其中 35,574 条与 canonical summary 不一致 | 当前代码不再读写 | 等备份、批准、DROP COLUMN 与回滚验证 |
| Mongo `assessment_models_legacy_20260717_084636` | 28 个文档 | 无当前运行时 owner | 已归档；仍需保留期限与明确删除批准 |
| Mongo 10 个 `__seed_orphan_backup_hist_v1...` 集合 | 每个 269 个文档 | 无当前运行时 owner | 同上 |

### 5.4 高置信但生产观察不足

| 对象 | 当前证据 | 仍缺证据 |
| --- | --- | --- |
| deprecated `GenerateReportFromAssessment` | 本仓及已检跨仓静态零调用，短窗口指标为 0 | 连续 30 天生产指标、所有外部消费者确认 |
| `/practitioners` | canonical `/clinicians` 已被当前前端和 seeddata-runner 使用 | 连续 30 天旧路由指标 |
| 兼容字段与 fallback | 均有明确 current owner 或历史读入口 | 独立指标、Owner、观察窗和退出版本 |

## 6. 数据库与缓存当前画像

### 6.1 MySQL

- MySQL 8.0.36，隔离级别 READ-COMMITTED，migration 66，`dirty=false`。
- 四个 seed backup 表和 `analytics_scan_watermarks` 均精确为空。
- 64,676 个 active testee 的三个旧统计列全部为默认值，且各有 35,574 条与 canonical summary 不一致；这些列不能作为回滚数据源。
- Evaluation 五项一致性检查均为 0，非 canonical run identity 为 0。
- Statistics 最新 publish run `631524273684492846` 成功，`as_of=2026-08-05`，generation 15，snapshot 对齐。
- 六条陈旧 running ledger 均满足只读处置前提：`data_committed_at IS NULL`、标准 rebuild 进程/容器为 0、Redis DB 6 的 `cache:lock:statistics:1` TTL 为 -2、年龄超过 30 分钟。修改状态仍需显式批准。

### 6.2 MongoDB

- MongoDB 7.0.31，migration 21，`dirty=false`；共 29 个集合、1,490,461 个对象、逻辑数据约 2.787 GB。
- 29 个集合精确分类为：17 个 canonical 运行时/迁移集合、11 个明确备份集合、1 个系统集合 `system.profile`。`system.profile` 不是遗留业务集合。
- `answersheet_submit_idempotency` 为 0 文档但存在活跃 fallback owner，不能按空集合删除。
- ModelCatalog 共 49 条：25 个 head、24 个 snapshot、24 个 active snapshot；canonical identity、DefinitionV2 和 active duplicate 检查均无异常。
- Questionnaire 共 136 条：42 个 head、94 个 snapshot、38 个 active snapshot；角色、状态和重复检查无异常，但有 6 个 head version 缺 snapshot。
- Interpretation 十个当前集合均存在；generation、run、artifact、query catalog 各 212,423 条，生命周期一致性检查均为 0；四个已退役集合均不存在。
- 36,860 个 active artifact 缺 `presentation_profile`，4 个 published template 均为 `legacy-v1`，属于已知后续治理项，不等同于可删除遗留集合。
- 最新归档 `qs_mongodb_backup_20260806_021432.archive.gz` 约 254 MiB；dry-run 读取 28 个归档集合成功，未执行实际导入。

### 6.3 Redis

- Redis 7.0.15，used memory 约 178.48 MiB，maxmemory 256 MiB，策略 `allkeys-lru`。
- 旧候选计数器在 DB 0–8 均为 0；这只能证明采样时点，不等于观察窗完成。
- 最新只读时点，DB 2 有 74 个 questionnaire、19 个 model 当前 key；DB 3 扫描到 32,134 个 assessment 当前 key。缓存计数会随 TTL、流量和 SCAN 并发变化，不能当成持久数据总量。
- DB 4 有 3 个 Statistics 当前 key 和 12 个兼容 key；DB 7 有 39,656 个 assessment-list 兼容 token 与 2 个 hotset；DB 8 有 321,868 个当前 rank/other family key。
- Redis 是运行时状态，不是迁移表；任何清理必须精确到 DB、family、namespace、TTL、实例退出和审批。

## 7. 当前阻断与目标队列

### 7.1 本轮已关闭门禁

| ID | 已关闭目标 | 证据 |
| --- | --- | --- |
| FINAL-P1-001 | 当前基线完整 CI | 31056398040、31056396517、31056398258 均成功；race、integration-tag、E2E、lint、边界、部署契约和三制品通过 |
| FINAL-P1-002 | 当前基线实际部署与运行指纹 | 31056961683 实际部署全部三服务；31057268188 独立巡检通过 |
| FINAL-P1-003A | Attention dry-run 演练 | 31051404012 验证三实例目标 SHA、有效 dry-run 配置和三个串行轮次，均扫描 41、缺失 33、创建 0、失败 0；31051481975 复核 projection 仍为 117 |

### 7.2 仍开放目标

| ID | 级别 | 目标 | 当前证据 | 退出条件 | Owner / 期限 |
| --- | --- | --- | --- | --- | --- |
| FINAL-P1-003B | P1 | 处置 33 个 Attention 高风险缺口 | dry-run 与零写入证据已完整，缺口确定为 33 | 用户决定“本版 apply”或形成正式风险接受；若 apply，需改配置、部署、归零对账并验证无重复副作用 | Interpretation + 运维 / 定档前 |
| FINAL-P1-004 | P1 | 结算六条 Statistics running ledger | 六条均未提交；无进程、无锁、超时四项只读前提再次成立；2026-08-06 负责人确认六条均无需继续，已准备 `scripts/oneoff/settle_statistics_stale_runs.sql` | 执行前重做无进程/无租约只读复核；精确更新 6 行为 `failed`，完成事务后对账，不删除幂等 Fact | Statistics + 运维 / 定档前 |
| FINAL-P1-005 | P1 | 固化兼容观察窗 | 多项只有短时点指标或静态证据 | 每项具有指标、Owner、起止时间、退出版本；今天无法归零的项目形成正式风险接受 | 各模块 / 定档签署前 |
| FINAL-P1-006 | P1 | 升级 ServerD 自托管 Runner | 31057268188 再次观测到 `2.334.0`；GitHub 明确提示该版本 2026-08-10 后停止接单，当前正式版为 `2.336.0` | 三实例逐个滚动升级到不低于 `2.336.0`，版本和 systemd 状态一致，组织端在线，升级后 Ping Runner 全绿 | 运维 / 2026-08-09 前 |
| FINAL-P2-001 | P2 | Interpretation presentation/template 收敛 | 36,860 个 artifact 缺 profile，4 个 published template 为 `legacy-v1` | 回填、回放和模板升级计划；若不纳入本版本则书面接受 | Interpretation / 负责人确认 |
| FINAL-P2-002 | P2 | 生产物理遗留对象退役 | 已有对象计数与 Mongo 归档，尚无删除批准 | 按对象审批、备份、演练、执行、验证和回滚；不得自动应用破坏性 migration | 数据 Owner + 运维 / 独立窗口 |
| FINAL-P2-003 | P2 | 退出 GitHub Actions Node 20 强制兼容并恢复真实覆盖率上传 | 31057564067 暴露 `codecov-action@v5` 间接调用 Node 20 `github-script`，且 tokenless 上传被静默拒绝；31058349927 证明 v7 OIDC token 已签发但 Codecov 端返回 `Repository not found`。仓库无 Codecov secret、配置与有效消费链，确认该外部上传为失效步骤 | 删除 Codecov Action，改由 `upload-artifact@v7` 保留 14 天 `coverage.out`并对缺失 fail-close；候选 CI 上传成功且 Run Tests annotation 为 0 | Delivery / 当前批次 |

P1 只能通过符合标准的正式风险接受降级；风险接受必须写明 Owner、期限、指标阈值和触发重新决策的条件，“以后处理”不构成定档。

## 8. 验证入口与证据缺口

| 门禁 | 当前结果 |
| --- | --- |
| 本地 Go 门禁 | 业务代码 `d2482f27` 的 `go test ./...` 全仓通过；`2f7ae468` 的部署契约测试、脚本正反例和 actionlint 通过；当前基线完整 CI 另见下列原始运行 |
| `make lint-boundaries` | 通过，0 issues |
| `make docs-verify` | 通过；apiserver 174、collection 45 paths |
| `make docs-hygiene` / `make docs-facts` | 通过；150 篇现行 Markdown、7 个业务模块、6 个核心事件 |
| `k6 inspect scripts/perf/k6/mixed.js` | 通过，当前主压测入口可解析 |
| 当前基线 CI | 31056398040 完整成功；CodeQL 31056396517、SonarQube 31056398258 成功；Node 24 上传 Action 相关 annotations 为 0 |
| 当前基线 Deploy | 31056961683 实际部署 `7f84b29b` 全部三服务成功；非 plan-only；Node 24 Docker Action 相关 annotations 为 0 |
| 部署后巡检 | 31057268188 成功；API、Nginx/DNS、collection 2/2、worker 3/3 和三治理端点健康；ServerD job 14 秒，Runner 版本时限仍是 P1 |
| Attention dry-run | 31051404012 成功；3 个串行成功轮次，均 scanned=41、missing=33、existing=8、mismatched=0、created=0 |
| 生产数据盘点 | 31051481975 成功；projection 仍为 117、缺口仍为 33，六条 Statistics running 与只读处置前提未变化 |
| Mongo 备份验证 | 31045715549 第二次成功；28 个归档集合 dry-run 0 failed |
| 静态 deadcode | 工具不可用；未下载未固定版本的 latest 工具，候选改由责任链与零调用搜索逐项确认 |

## 9. 破坏性操作边界

本台账不授权任何生产 `DROP`、`DELETE`、Mongo collection 删除、Redis 批量清理、Attention apply 或 Statistics ledger 状态修改。

1. MySQL 物理删除前必须具备当前 schema/row count、备份、恢复命令、审批、执行窗口和执行后 migration/schema 核验。
2. Mongo 候选集合已有正式 archive 与 `mongorestore --dryRun` 证据，但仍需保留期限、精确对象清单和删除批准；备份成功不能替代实际恢复演练。
3. Redis 清理必须精确到 DB、family、namespace 和 key pattern；先证明旧实例退出并等待最长 data TTL。无 TTL token 只能单独审批，禁止宽泛通配删除。
4. Statistics running ledger 禁止批量改状态或先删锁；当前只读前提成立只意味着可以制定逐条处置方案。
5. Attention reconcile 当前只授权 dry-run；目标 SHA、三实例配置、扫描清单和零写入证据已取得。`dry-run=false` 仍需用户显式批准。

## 10. 定档签署条件

当前版本只有满足以下条件后才能从“阻断”升级：

- 全部 FINAL-P1 条目关闭，或形成包含 Owner、期限、指标和阈值的正式风险接受。
- 当前 HEAD 的全仓测试、必跑 CI、制品、实际 `DEPLOY_SHA` 和三进程运行指纹一致。
- Attention dry-run 在实际三 worker 环境完成并经原始指标/日志验收；apply 是否纳入本次定档由负责人显式决定。
- 六条 Statistics 陈旧 running ledger 逐条形成并执行获批处置，不留下无法解释的运行中状态。
- ServerD 三个自托管 Runner 在 2026-08-10 停服时限前完成滚动升级，升级后生产巡检通过。
- 每个活跃兼容项都有明确指标、Owner、观察窗与退出版本；生产物理候选保持“待批准”或在独立窗口完成退役闭环。
- 两份定档文档只保留当前有效事实，且与注册表、配置、组合根、迁移、API 生成结果、测试和生产证据一致。

目标模式继续按“只读验收 → 小批修复 → 聚焦测试 → Conventional Commit 类型前缀 + 中文主题 → push → CI/CD → 运行证据 → 更新台账”维护。每个可独立验收阶段立即提交，不跨阶段积攒；commit 或 push 本身不提升验收状态。
