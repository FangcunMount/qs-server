# 当前版本定档验收台账

> 当前结论：**阻断，尚未达到代码定档标准。** 本台账以 `main@279f0d3dfb2b488fb54c288176c8ebeb7c5a89af` 为本轮业务代码起始基线，当前业务验收 SHA 为 `76f49ebc0b5795efd3826e6045a92fa7bd94bfd4`；Survey 两项 P1 已关闭，但仍有 2 个 P0，验收日期为 2026-08-05（Asia/Shanghai）。验收口径见[项目完成画像与验收标准](./08-项目完成画像与验收标准.md)。

## 1. 基线与证据边界

| 项目 | 当前事实 |
| --- | --- |
| 分支 / 本批远端 SHA | `main` / `76f49ebc0b5795efd3826e6045a92fa7bd94bfd4`（生命周期实现 `786b6913`，验收修复 `cf893386`、`76f49ebc`） |
| 初始工作区 | `main...origin/main`，验收开始时无未提交修改 |
| 当前 CI | [CI run 31007884611](https://github.com/FangcunMount/qs-server/actions/runs/31007884611) 成功；全量 race tests、lint、边界、构建、迁移、runtime closure 与 Survey Mongo 三项契约均通过 |
| 当前部署 | [Production Deploy run 31008436198](https://github.com/FangcunMount/qs-server/actions/runs/31008436198) 第一次因 GitHub secondary rate limit 在 GHCR push 处 403；失败作业重跑后 apiserver 部署成功，容器 healthy，运行镜像为完整 SHA `76f49ebc0b5795efd3826e6045a92fa7bd94bfd4` |
| 条件门禁 | 当前 CI 的 Docs & API Sync 因 `vars.DOCS_VERIFY != true` 跳过，不能计为通过证据 |
| 本轮本地证据 | Survey 聚焦包单元/架构测试通过；integration build-tag 编译通过；文档门禁见本文 §9 |
| 尚缺证据 | 生产数据库只读盘点、30 天接口/兼容流量、Survey 专项故障与容量证据 |

当前 CI 已证明该 SHA 的普通 Go test（含 race）、lint、边界、构建、Survey Mongo 集成契约和 runtime closure job 通过；当前 Deploy 进一步证明 apiserver 制品、部署、健康与运行镜像 SHA 一致。二者仍不证明生产对象、数据、流量、容量和长期行为。

## 2. 全局完成度

### 2.1 业务模块

| 顺序 | 模块 | 状态 | 本轮结论 |
| --- | --- | --- | --- |
| 1 | `survey` | **阻断** | 已完成十二角度首轮验收；2 个 P1 已关闭，仍存在 2 个 P0 与其他 P1 |
| 2 | `modelcatalog` | 未验收 | 等待 Survey 结论讨论后开始 |
| 3 | `evaluation` | 未验收 | 不继承历史清单结论 |
| 4 | `interpretation` | 未验收 | 不继承历史清单结论 |
| 5 | `actor` | 未验收 | 需独立核对主体、ProfileLink、租户与资源访问 |
| 6 | `plan` | 未验收 | 需独立核对计划、入组、任务、调度与完成语义 |
| 7 | `statistics` | 未验收 | 需独立核对 v2 事实、重建、发布与 v1 退出 |

### 2.2 技术模块

| 专题 | 状态 | 当前证据边界 |
| --- | --- | --- |
| Event / Outbox / MQ | 未验收 | Survey 链局部核对，不代表全局事件面通过 |
| Cache / Signal / Warmup | 未验收 | Survey questionnaire 局部核对 |
| Rate limit / Gate / Backpressure | 未验收 | Survey 提交入口局部核对，未完成全局容量画像 |
| LockLease / Leader / Duplicate suppression | 未验收 | `answersheet_processing` 局部核对 |
| Retry / Manual governance | 未验收 | 尚未全局串联业务、Outbox、MQ 和人工终态 |
| Data / Migration / Retirement | 未验收 | Survey Mongo 对象已盘点，生产对象未盘点 |
| IAM / Security / Tenant | 未验收 | Survey 已发现资源归属 P0 |
| Runtime / Scheduler / Shutdown | 未验收 | 当前 apiserver 部署与运行 SHA 已闭环，不等于三进程专题验收完成 |
| Observability / Governance | 未验收 | Survey 指标存在，但未完成告警与运行验证 |
| Delivery / Performance | 未验收 | 当前 SHA 的 CI、apiserver 制品、部署、健康和运行镜像已闭环；生产容量和恢复未确认 |

## 3. Survey 完成画像

### 3.1 入口与责任链

```text
Questionnaire authoring
  apiserver REST
  -> survey/questionnaire LifecycleService
  -> questionnaires head / published_snapshot
  -> cache invalidation + best-effort signal

AnswerSheet acceptance
  collection REST POST /api/v1/answersheets
  -> envelope + published questionnaire validation
  -> ProfileLink / testee ownership
  -> apiserver gRPC SaveAnswerSheet
  -> AnswerSheet + embedded submit_meta + domain_event_outbox transaction
  -> 202 Accepted

Async derivation
  answersheet.submitted
  -> worker duplicate-suppression LockLease
  -> Journey Ensure
  -> base scoring
  -> independent questionnaire ends, or bound questionnaire creates/submits Assessment
```

### 3.2 十二角度结论

| 角度 | 颜色 | 结论与证据 |
| --- | --- | --- |
| 职责与边界 | 黄 | Questionnaire / AnswerSheet 边界清晰，跨模块编排归 Journey；旧构造和 legacy admission 仍存在 |
| 代码与装配 | 绿 | Survey module、binding resolver、Mongo transaction、collection BFF 和 worker handler 均进入生产组合根 |
| 领域与不变量 | 红 | 聚合拒绝重复题并冻结提交上下文，但共享校验器不会拒绝当前不可见题答案 |
| 接口契约 | 红 | `GET /api/v1/answersheets/{id}` 缺少 writer/testee 资源归属校验；gRPC 详情读取明确不鉴权 |
| 数据与迁移 | 黄 | `questionnaires` 与 `answersheets` 为当前集合；嵌入幂等已完成，旧独立集合仍双读 |
| 状态与一致性 | 绿 | 可靠受理事务成立；独立 Questionnaire 的 Publish / Unpublish / Archive 已进入 Mongo transaction，三类部分失败回滚在真实 Replica Set 中通过 |
| 安全与隐私 | 红 | 已认证用户可按 AnswerSheet ID 读取包含原始答案的他人答卷，未发现路由级资源授权兜底 |
| 缓存与并发 | 黄 | L1/L2、信令、限流、coalescer、唯一索引和 worker LockLease 均存在；专项生产容量未确认 |
| 测试与门禁 | 黄 | 聚焦单元/架构测试通过；durable submit 与 questionnaire CAS 已进入强制 Replica Set CI；两个 P0 负向契约仍缺失 |
| 可观测与运维 | 黄 | 提交阶段、幂等、readiness、锁降级已有指标/结构化日志；独立计分完成状态不可观测 |
| 性能与容量 | 黄 | 有提交降级/限流脚本和有界超时设计，本轮没有当前生产压测与故障恢复证据 |
| 遗留与兼容 | 黄 | 已识别旧集合、published-head、live binding、task_id、旧构造和废弃 gRPC；删除均需分级处理 |

**Survey 最终状态：阻断。** 最高证据覆盖 E6（当前 SHA CI/CD）和局部 E4（current runtime closure）；未取得 Survey 专项 E7。P0 关闭并重新验收前，不能进入 `modelcatalog` 的正式通过判定，也不能把当前项目标记为代码定档。

## 4. Survey 阻断与目标队列

| ID | 级别 | 问题 | 影响 | 退出条件 |
| --- | --- | --- | --- | --- |
| SUR-P0-001 | P0 | 答卷详情缺少资源归属授权 | 登录用户可按 ID 读取他人原始答案、分数和主体标识 | REST/应用/gRPC 责任边界确定；正向、同租户越权、跨租户越权测试通过；生成契约同步 |
| SUR-P0-002 | P0 | 不可见题答案未被拒绝 | 客户端可持久化并参与计分当前不可见的答案，破坏发布问卷作答契约 | 共享校验器按最终提交计算可见性并拒绝隐藏答案；BFF/apiserver 一致；负向测试进入门禁 |
| SUR-P1-003 | P1（已关闭，E6） | 独立 Questionnaire 完整生命周期原为非事务多步写 | `786b6913` 已统一使用 Mongo transaction，事件与缓存失效在 commit 后；[CI 31007884611](https://github.com/FangcunMount/qs-server/actions/runs/31007884611) 三类真实 Replica Set 故障注入均回滚，[Deploy 31008436198](https://github.com/FangcunMount/qs-server/actions/runs/31008436198) 运行 SHA 已确认 | 已满足；保留事务故障测试、架构边界与 post-commit 副作用约束 |
| SUR-P1-004 | P1 | 基础计分无显式完成状态 | `score=0` 无法区分未计分与真实零分；独立 readiness 无法证明 worker 已完成计分 | 明确产品契约并建模可查询状态/时间，或正式确认计分状态不属于 readiness 并提供替代证据 |
| SUR-P1-005 | P1（已关闭，E5） | Survey integration tests 原未属于当前 CI 必跑范围 | `a8199a15` 已加入强制 Replica Set job；[CI 31005796084](https://github.com/FangcunMount/qs-server/actions/runs/31005796084) 首次证明两项测试 PASS，[CI 31007884611](https://github.com/FangcunMount/qs-server/actions/runs/31007884611) 继续证明扩展后的三项契约通过 | 已满足；保留该强制 job，后续变更不得退回 skip/advisory |
| SUR-P1-006 | P1 | 旧 Mongo 幂等集合仍被运行时读取 | 无法直接删除 `answersheet_submit_idempotency`，相关脚本仍依赖该集合 | 生产计数、引用完整性、观察窗流量、回填/归档和回滚证据齐备后删除双读与集合 |
| SUR-P2-007 | P2 | 保留的 `SaveAnswerSheetScores` gRPC 永久返回 Unimplemented | proto、ACL 和 worker client 维持一个无可用行为的接口 | 确认跨仓无消费者后删除 proto/client/ACL/实现并完成兼容发布 |
| SUR-P2-008 | P2 | 多条中间态兼容分支没有统一退出台账 | 新旧语义长期并存，后续无法判断可删时间 | 为每条兼容建立 owner、生产指标、观察窗和退出版本 |

## 5. Survey 已成立的关键能力

### 5.1 可靠受理

- collection 只在 gRPC 返回非零 AnswerSheet ID 后返回 `202 Accepted`。
- BFF 强制精确 `questionnaire_code + questionnaire_version`、安全幂等键、ProfileLink 和 testee 访问校验。
- apiserver 在同一 Mongo transaction 中写 AnswerSheet 与 `domain_event_outbox`；`submit_meta` 嵌入 AnswerSheet。
- `(submit_meta.writer_id, submit_meta.idempotency_key)` 局部唯一索引是最终并发裁决；Redis 只负责合并压力。
- commit 结果未知时使用 detached readback，只有读到相同已提交事实才返回成功。

### 5.2 异步终点

- `answersheet.submitted` 在 [`configs/events.yaml`](../../configs/events.yaml) 中声明为 `durable_outbox`。
- worker 使用 `answersheet_processing` LockLease 做 best-effort 重复抑制，Redis 故障时降级开放，最终幂等由 Journey / Evaluation 事实保护。
- 新 AnswerSheet 冻结 admission；无 AssessmentModel binding 时标记 `independent_questionnaire`，Journey 完成基础计分后不创建 Assessment。
- 历史无 admission 事件仍走 live binding，因此“独立问卷终止”已对新写成立，但遗留入口尚未退出。

### 5.3 Questionnaire 发布与缓存

- head 与 immutable published snapshot 共用 `questionnaires`，正式 migration 保护 head、snapshot version 和 active code 唯一性。
- 已绑定问卷通过 Assessment Release 的 Mongo transaction 联合发布。
- apiserver Redis cache、collection L1、cache signal 和 warmup 已装配；缓存不是发布事实源。
- 独立 Questionnaire 的公开 `Publish` / `Unpublish` / `Archive` 使用 Mongo transaction；事件发布、缓存失效与信令在 commit 后执行。

## 6. Survey 数据对象盘点

### 6.1 MongoDB

| 集合 | 分类 | 当前写入 | 当前读取 | 结论 |
| --- | --- | --- | --- | --- |
| `questionnaires` | 当前业务事实 | 是 | 是 | 保留；包含 head 与 published snapshots |
| `answersheets` | 当前业务事实 | 是 | 是 | 保留；包含原始答案、派生题分、admission、attribution 和 `submit_meta` |
| `domain_event_outbox` | 当前技术恢复事实 | 是 | 是 | 保留；跨模块基础设施，不是 Survey 专属集合 |
| `answersheet_submit_idempotency` | 高置信遗留候选、生产未证实 | 否 | 是，嵌入元数据 miss 时 fallback | 不可直接删除；先完成生产盘点与迁移窗口退出 |

### 6.2 MySQL

当前源码与 migration 未发现 Survey 自有 Questionnaire/AnswerSheet MySQL 表。Survey 提交会读取 Actor/ProfileLink、Plan attribution，并向 Evaluation 派生事实；这些 MySQL 表属于对应模块，不能按 Survey 遗留表删除。

### 6.3 Redis / 临时事实

Questionnaire cache、提交 coalescer/完成信号和 `answersheet_processing` lease 都是可丢失的运行时机制，不是业务事实。删除或清空 Redis 不能替代 Mongo 幂等、Outbox、权限或迁移处理。

## 7. Survey 兼容代码分类

| 兼容点 | 分类 | 当前理由 | 退出所需证据 |
| --- | --- | --- | --- |
| 旧幂等集合 fallback | 活跃兼容 | 旧 key 仍可能只存在独立集合 | 文档数、孤儿引用、fallback 命中率和观察窗归零 |
| published-head fallback | 活跃兼容 | 历史 Questionnaire 可能没有 snapshot | 生产 snapshot 完整性与 fallback 命中率 |
| 无 admission 时 live binding | 活跃兼容 | 历史事件/AnswerSheet 可能没有冻结意图 | 旧事件积压、旧 AnswerSheet 数和 gate 指标归零 |
| `task_id -> origin_ref(plan_task)` | 活跃兼容 | 当前 proto 仍公开 task_id 过渡语义 | 所有调用方迁移、流量归零、proto 兼容发布 |
| attribution resolver nil fallback | 测试/隔离环境兼容 | 生产组合根会注入 MySQL resolver | 架构测试固定生产注入；决定是否改为测试专用构造 |
| `NewAnswerSheet` 旧构造 | 高置信代码候选 | 标记 Deprecated，新提交使用 `Submit` | 全仓/跨仓调用确认和删除后表征测试 |
| `SaveAnswerSheetScores` | 高置信接口候选 | 服务永久返回 Unimplemented，主链内部直接计分 | 跨仓 gRPC 使用与运行流量确认 |
| 独立 Publish 的 binding version sync | 高置信代码候选 | 已绑定问卷已被入口拒绝 | 证明无隐式 side effect 后移除并补回归测试 |

## 8. Survey 测试与运行证据

### 8.1 本轮通过

以下聚焦包使用 `go test -count=1` 通过：

```text
internal/pkg/surveyvalidation
internal/collection-server/application/answersheet
internal/collection-server/transport/rest/handler
internal/apiserver/application/survey/answersheet
internal/apiserver/application/survey/questionnaire
internal/apiserver/domain/survey/answersheet
internal/apiserver/domain/survey/questionnaire
internal/apiserver/infra/mongo/answersheet
internal/apiserver/infra/mongo/questionnaire
internal/apiserver/application/journey/assessmentintake
internal/apiserver/transport/grpc/service
internal/worker/handlers
```

这些测试同时证明了当前测试集没有覆盖或阻止 SUR-P0-001 / SUR-P0-002；“测试全绿”不能反转源码责任链结论。

### 8.2 强制 CI、修复轮次与部署验收

- `durable_submit_integration_test.go` 需要显式 Mongo URI 和 Replica Set；`cas_integration_test.go` 另由 `integration` build tag 纳入构建。
- `a8199a15` 已在现有 Replica Set job 中显式执行 `TestDurableSubmissionTransactionAgainstMongoReplicaSet` 和 `TestRepositoryConcurrentWritersUseRevisionCAS`。
- 该 job 本身是后续 build 的强制依赖；新步骤失败会阻断构建，不是 advisory。
- [CI 31005796084](https://github.com/FangcunMount/qs-server/actions/runs/31005796084) 的原始 job 日志记录两项测试分别在 Mongo 7 Replica Set 上 PASS；SUR-P1-005 因此关闭。
- `786b6913` 加入独立 Questionnaire 生命周期事务与三类故障注入；[CI 31006945607](https://github.com/FangcunMount/qs-server/actions/runs/31006945607) 暴露测试夹具状态和旧构造调用问题，由 `cf893386` 修复。
- [CI 31007261849](https://github.com/FangcunMount/qs-server/actions/runs/31007261849) 中三类 Mongo 回滚测试已通过，但全量架构守卫拒绝 application 测试直接依赖 infra；`76f49ebc` 将测试迁入 Mongo adapter 并收敛 CI 包清单，没有放宽架构规则。
- [CI 31007884611](https://github.com/FangcunMount/qs-server/actions/runs/31007884611) 完整成功；原始日志记录 Publish、Unpublish、Archive 三类部分失败回滚、AnswerSheet durable submit、Questionnaire CAS、seeddata retirement migration 和 current runtime closure 全部 PASS。
- [Production Deploy 31008436198](https://github.com/FangcunMount/qs-server/actions/runs/31008436198) 第一次在 GHCR push 遭遇 GitHub secondary rate limit 403，未发生远端部署；仅重跑失败作业后镜像发布与 apiserver 部署成功，ServerA 容器 healthy，日志记录运行镜像为完整 SHA `76f49ebc0b5795efd3826e6045a92fa7bd94bfd4`。
- 本地 Docker daemon 仍不可用；这是本地证据缺口，不会抹消本次真实 Actions 运行证据。

## 9. 文档与本轮门禁

| 门禁 | 结果 |
| --- | --- |
| Survey 聚焦 Go tests | 通过 |
| GitHub Actions 静态检查 | `actionlint .github/workflows/ci.yml` 通过 |
| Survey Mongo Replica Set integration | [CI 31007884611](https://github.com/FangcunMount/qs-server/actions/runs/31007884611) 真实执行 durable submit、Questionnaire CAS 与 lifecycle rollback 并通过；无 skip |
| 全量 CI | [CI 31007884611](https://github.com/FangcunMount/qs-server/actions/runs/31007884611) 的 race tests、lint、depguard、Tier 1、迁移、runtime closure 与三个应用构建全部通过 |
| apiserver 部署与运行指纹 | [Production Deploy 31008436198](https://github.com/FangcunMount/qs-server/actions/runs/31008436198) 失败作业重跑后通过；容器 healthy，运行镜像标签等于 `76f49ebc0b5795efd3826e6045a92fa7bd94bfd4` |
| `make docs-hygiene` | 通过；扫描 150 篇现行 Markdown |
| `make docs-facts` | 通过；150 篇现行文档、7 个业务模块、6 个核心事件 |
| `git diff --check` | 通过 |
| REST 生成契约 | 本轮未修改 REST 契约；SUR-P0-001 修复时必须执行 `make docs-verify` |

## 10. 下一步与停止点

本轮在 Survey 验收结论处停止，不开始 `modelcatalog` 验收。推荐下一批按顺序处理：

1. 讨论并确认 SUR-P0-001 的答卷可见性模型：writer、active ProfileLink、testee 本人/监护人、运营角色分别允许什么。
2. 讨论并确认 SUR-P0-002：隐藏题答案一律整单拒绝，还是存在明确、可审计的兼容例外。
3. 小批实现两个 P0，并补 REST/应用/gRPC 与共享校验负向测试。
4. 重新执行 Survey 十二角度验收；升级为无红项后，再开始 `modelcatalog`。

后续使用目标模式维护本台账：每次切换基线 SHA 都重新核对受影响责任链、更新证据和状态，不直接继承本次通过项。

目标模式允许围绕同一目标形成多轮小批 commit，并在每批门禁通过、提交范围干净且远端目标明确时自行 push。每次 push 后，本台账必须补充 commit/分支、CI/CD run、失败修复轮次和最终运行 SHA；提交或推送动作本身不提升验收状态。

每个可独立验收阶段完成后立即 commit，不跨阶段积攒；commit message 使用 `feat:`、`fix:`、`docs:`、`test:`、`ci:`、`refactor:`、`chore:` 等 Conventional Commits 类型前缀，前缀后的主题使用中文。
