# 当前版本定档验收台账

<!-- docs-facts: checkout_sha=git:HEAD last_ci_sha=8f663383e117f186865686c78c9dbcae7ed50a91 deployed_sha=8f663383e117f186865686c78c9dbcae7ed50a91 -->

> 仓库事实复核日期：2026-08-20 CST；CI、部署和生产证据截点仍为 2026-08-18 11:35 CST。本文把源码、CI、部署和生产观测分开记账；任何一列更新都不能自动提升其他列的状态。

> 基础设施生产测量的机器真值、失效期和限制统一维护在[基础设施生产证据台账](./10-基础设施生产证据台账.md)；本页只保留版本级投影，二者冲突时以机器台账和原始证据为准。

> 当前结论：2026-08-20 复核开始时，仓库 `main` 与 `origin/main` 均指向 `1afaae03d65a14bb9ca6b25696432a0a5cce78d1`，工作区为 clean；本轮主体文档随后形成本地提交 `df09c14c5ebdacc95f463af1b9a0ee55cff735d9`，`origin/main` 仍停留在 `1afaae03...`，最终门禁加固仍可能包含未提交差异。可引用的最后 CI 与三服务生产部署仍只覆盖 `8f663383e117f186865686c78c9dbcae7ed50a91`，2026-08-18 08:45 CST 的周期主机/服务巡检仍是历史时点证据，不能提升为当前 checkout 的 CI、部署或生产证据。Mongo consistency one-off 已完成七阶段生产只读扫描，但仍有 `outbox_missing_answersheet=269` 的历史 high drift；自动 scheduler 必须继续关闭，不能写成“生产一致性已清零”。验收口径见[项目完成画像与验收标准](./08-项目完成画像与验收标准.md)。

## 1. 四类版本与时间必须分开

| 事实层 | 当前记录 | 证据时间（CST） | 能证明什么 | 不能证明什么 |
| --- | --- | --- | --- | --- |
| 当前 checkout SHA | 当前 checkout 由页首 `checkout_sha=git:HEAD` 动态取值；复核起点为 `1afaae03...`，本轮主体文档提交为 `df09c14c5ebdacc95f463af1b9a0ee55cff735d9` | 起点 commit：2026-08-19 10:46:40；主体文档 commit：2026-08-20 23:06:53；当前 checkout/工作区以每次交付时 `git rev-parse HEAD` 与 `git status` 为准 | 复核起点、本轮已提交文档以及当前 checkout 的可机械解析口径 | 当前 checkout、主体文档提交或剩余本地差异已通过 CI、已部署或已取得生产证据 |
| 最后通过 CI 的 SHA | `8f663383e117f186865686c78c9dbcae7ed50a91`；[CI 31896282313](https://github.com/FangcunMount/qs-server/actions/runs/31896282313)、[CodeQL 31896282267](https://github.com/FangcunMount/qs-server/actions/runs/31896282267)、[SonarQube 31896282337](https://github.com/FangcunMount/qs-server/actions/runs/31896282337) 成功 | 2026-08-16 00:43:07～00:53:23 | 该提交的测试、lint、边界、部署契约、真实 Mongo runtime E2E 和应用构建通过 | 本轮未提交文档差异已通过；该 CI 的 Docs & API Sync job 为 skipped；生产进程正在运行该 SHA |
| 实际部署 SHA | `8f663383e117f186865686c78c9dbcae7ed50a91`；[Production Deploy 31896757704](https://github.com/FangcunMount/qs-server/actions/runs/31896757704) 的原始日志确认 apiserver、collection 2/2、worker 3/3 运行该 SHA 镜像 | 2026-08-16 00:53:25～00:59:33 | 三个生产服务完成精确 SHA 部署并通过该次部署 gate | 部署之后所有时刻都健康；当前工作区差异已部署 |
| 生产证据时间 | [Ping Runner 32085615223](https://github.com/FangcunMount/qs-server/actions/runs/32085615223) 成功：apiserver health、collection 2/2 serving-ready、worker 3/3 governance 和 K6 observability 反向检查通过 | 2026-08-18 08:43:50～08:45:08 | 该时点的主机、容器和服务探针通过 | 业务全链、数据一致性、容量或当前镜像 SHA 被重新完整验证 |
| Mongo consistency 生产证据 | 正式合并版本 one-off：七阶段扫描 347,494 个 anchor，25.43 秒；唯一 finding 为 `outbox_missing_answersheet=269`，其余 finding 为 0 | 2026-08-16 01:13:35 | 该时点、该扫描上界内的跨集合只读盘点结果 | 漂移已修复；scheduler 已启用；之后没有新漂移 |
| 最新兼容观察 | [Compatibility Observation 32055956851](https://github.com/FangcunMount/qs-server/actions/runs/32055956851) 失败：三个 current series 的 30 天 anchor 均缺失，且 `qs_answersheet_legacy_idempotency_fallback_lookup_total` 已不在当前 Prometheus snapshot | 2026-08-18 02:39:23～02:40:00 | 观察链当前不完整，需要修正指标清单/锚点策略 | 不能解释为兼容流量回升，也不能累计为连续 30 天成功证据 |

时间字段是证据有效期，不是永久保证。后续 checkout、CI、部署或生产观测任一变化时，只更新对应行，并重新判断四者是否仍同 SHA。

## 2. 机器契约与文档数量

| 契约 | 当前机器事实 | 当前状态 |
| --- | --- | --- |
| apiserver REST | `api/rest/apiserver.yaml` 与生成 Swagger 均为 **191 method-path operations**、**171 unique paths** | `scripts/compare_api_docs.py` 通过 |
| collection REST | `api/rest/collection.yaml` 与生成 Swagger 均为 **50 method-path operations**、**48 unique paths** | `scripts/compare_api_docs.py` 通过 |
| OpenAPI 质量 | operationId、path parameter、description、security、error response | 比较脚本全部通过 |
| 文档树 | 165 篇 primary active Markdown、30 篇 maintained sidecar、7 个业务模块；机器 Event Catalog 为 18 个事件、Signal Catalog 为 5 个信号 | 165 达到硬上限，具名例外与 exact coverage 以 `document-closure.json` 为准 |

`compare_api_docs.py` 原先把 method-path operation 数显示为 `paths`，本轮已修正标签。191/50 是 `(HTTP method, path)` 操作对，171/48 才是唯一 URL path 数；两组数字都不覆盖 gRPC/Event/Signal。数字变化必须先更新机器契约，再由比较脚本和文档事实门禁生成证据；不得手工调整 Swagger 来迎合台账。

## 3. 七个业务模块当前画像

| 模块 | 状态 | 当前成立事实 | 仍需保留的边界 |
| --- | --- | --- | --- |
| Survey | 有条件通过 | Questionnaire/AnswerSheet 发布、精确版本、可靠提交主链成立；Mongo audit 的 `answersheet_outbox` 阶段为 0 | 反向 `outbox_answersheet` 仍有 269 条历史孤立 Outbox；不得未经授权删除或伪造 AnswerSheet |
| ModelCatalog | 有条件通过 | 统一 head/snapshot、发布事务、revision CAS、DefinitionV2 hash 与 runtime materialization 已实现；生产 one-off 的两个 ModelCatalog 阶段 finding 为 0 | scheduler 默认关闭；一次成功 one-off 不等于周期治理已运行 |
| Evaluation | 有条件通过 | Assessment、Run、Outcome、checkpoint 与 durable event 主链成立 | 长期生产窗口、并发与故障恢复证据仍需持续积累 |
| Interpretation | 有条件通过 | Generation/Run/Artifact/Outbox 成功事务、Generation CAS、Report Catalog audit 已实现；生产 one-off 的三个 Interpretation 阶段 finding 为 0 | Mongo consistency scheduler 默认关闭；Report Catalog repair 仍必须独立 dry-run/计划/授权 |
| Actor | **安全阻断** | IAM、Operator、Clinician、Testee 与关系链已装配；已有 `testeeaccess.Authorizer` | 公开 Intake 未证明调用者对 Profile 的代表权；collection Testee GET/PUT/care-context 绕过 User -> Testee ownership gate，存在跨用户 IDOR 风险 |
| Plan | 有条件通过 | Plan、Enrollment、Task 调度链与 revisioned schedule fact 成立 | 缓存、通知和失败恢复仍缺长期故障演练 |
| Statistics | 有条件通过 | 唯一 nightly publish、Fact/Projection/SyncRun 边界明确，历史陈旧 running ledger 已按原授权闭环 | inactive clinician 仍可能经缓存读取 self overview；须前置 active identity 授权并补停用前缓存负测 |

“有条件通过”表示主链可用但仍有具名运维/兼容退出条件，不等于功能未实现。“安全阻断”表示当前源码已确认 P0 授权缺口，不能被历史 CI、部署或生产观测覆盖；它与机器台账中的 7×7 semantic signoff 使用同一口径。

## 4. 十个技术专题

| 技术专题 | 状态 | 当前判定 |
| --- | --- | --- |
| Event / Outbox / MQ | 黄 | 本地事务 Outbox 与 relay 主链成立；269 条历史 AnswerSheet 孤立 Outbox 尚未处置，不能写成跨集合全绿 |
| Cache / Signal / Warmup | 黄 | 权威库、版本 token、TTL、signal/warmup 边界明确；仍需按 cache family 验证真实命中和恢复 |
| Rate limit / Gate / Backpressure | 黄 | 进程内 limiter、collection gate 和 Outbox 并发预算有代码保护；生产慢依赖/恢复证据不足 |
| LockLease / Leader | 绿 | workload catalog 已包含 Mongo consistency audit；Redis 不可用时 runner fail-closed，不会退化为多实例无锁扫描 |
| Retry / Manual governance | 黄 | 业务 retry、transport redelivery、hold/manual action 已分层；长期窗口与人工恢复演练仍不足 |
| Data / Migration / Consistency | 黄 | MySQL 末端 migration 000070、Mongo 末端 migration 000033（audit index 为 000024）和七阶段 scanner 已实现；269 条 high drift 阻断 scheduler 启用；AI 解读 retention TTL、subject-export 与治理目录索引及其它 migration 尚无真实环境证据 |
| IAM / Security / Tenant | 黄 | JWT、authz snapshot 和资源归属链成立；单租户默认值与外部 IAM 能力边界仍需保留 |
| Runtime / Scheduler / Shutdown | 黄 | 九个 apiserver scheduler runner 已进入 composition root；Mongo audit、AI Prompt 评测 prepared lease recovery 与 Participant AI 解读 lease recovery 的生产配置明确关闭，既有 shutdown 风险仍未因本次文档修复消失 |
| Observability / Governance | 黄 | Mongo audit 指标、日志和 Prometheus 规则已存在；兼容观察 workflow 当前失败，连续窗口不能累计 |
| Delivery / Performance | 黄 | CI、精确 SHA 部署和部署后巡检分层可追溯；本轮未重跑生产 K6，不能把历史容量结果提升为当前工作区证据 |

## 5. 十二角度统一矩阵

颜色含义：绿为当前主体与证据成立；黄为主链成立但仍有具名退出条件、时点证据或周期治理缺口；红为当前源码已经确认的发布/安全阻断。Actor 两条 P0 授权缺口为红；Mongo audit scheduler 另有独立启用阻断，二者均不能被历史部署或其它绿项覆盖。

| 验收角度 | Survey | ModelCatalog | Evaluation | Interpretation | Actor | Plan | Statistics |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1. 职责与边界 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 2. 代码与装配 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 3. 领域与不变量 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 | 绿 |
| 4. REST / gRPC / Event | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 5. 数据与迁移 | 黄 | 黄 | 黄 | 黄 | 黄 | 绿 | 绿 |
| 6. 状态与一致性 | 黄 | 黄 | 绿 | 黄 | 绿 | 绿 | 绿 |
| 7. 安全与隐私 | 绿 | 黄 | 黄 | 黄 | **红** | 黄 | 黄 |
| 8. 缓存与并发 | 黄 | 黄 | 绿 | 黄 | 黄 | 黄 | 黄 |
| 9. 测试与质量门禁 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 10. 可观测与运维 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 绿 |
| 11. 性能与容量 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |
| 12. 遗留与兼容 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 | 黄 |

## 6. Mongo consistency audit 当前真值

### 6.1 能力边界

- scanner 对 Mongo 业务集合只有读方法，没有 repair port；“只读”专指不写 Mongo 业务事实。
- scheduler 会写 MySQL `mongo_consistency_audit_checkpoint`，并使用 Redis leader lease；不能把它描述为“对所有存储零写入”。
- one-off CLI 不启动 scheduler、不写 checkpoint，退出码 `0/2/1` 分别表示无漂移/发现漂移/执行失败。
- 七阶段固定为 `answersheet_outbox`、`outbox_answersheet`、`generation_run`、`generated_terminal`、`retry_outbox`、`model_release`、`published_model_runtime`。
- 每阶段冻结 upper bound，以稳定 cursor 分批推进；checkpoint 的 `revision` 通过 compare-and-swap 防止并发覆盖，完成后保存 last completed cycle 和 next cycle time。
- scheduler 以 `mongo_consistency_audit` workload 获取自动续租 leader lock；拿不到锁就跳过，Redis lock 不可用则不启动。

### 6.2 生产状态与启用门禁

`configs/apiserver.prod.yaml` 当前 `mongo_consistency_audit.enable: false`。现有 269 条 `high` finding 意味着启用门禁未通过；本次只补文档，不修改生产配置。

只有同时满足以下条件，才能另起受控变更把生产开关改为 `true`：

1. 确认 MySQL 000069 checkpoint 表、Mongo 000024 索引和目标部署 SHA 均已就绪；
2. 对正式部署版本执行全七阶段 one-off，扫描完整结束且不存在未处置的 high drift；
3. 对每个 finding 冻结清单、原因、owner 与处置决定；任何 repair/delete 都有独立审批和复验；
4. Redis leader lock、Mongo backpressure、Prometheus 指标和告警查询均可用；
5. 明确 batch/cycle 容量预算、回退方式和观察窗口，并取得生产配置变更授权。

当前第 2、3 项未满足，所以结论只能是“能力已实现、生产 scheduler 关闭、one-off 有 high drift”，不能写成“周期审计已上线”。

## 7. 当前开放项

| ID | 类型 | 当前事实 | 退出条件 |
| --- | --- | --- | --- |
| MONGO-AUDIT-GATE | scheduler 启用阻断 | 269 条历史 published Outbox 指向物理不存在的 AnswerSheet；未删除、未自动修复 | 独立维护方案完成原因/范围/授权/执行/复验，之后全七阶段无未处置 high drift |
| ACTOR-PROFILE-REPRESENTATION | P0 安全阻断 | public Entry Intake 只校验 Profile 存在，未证明调用者拥有该 Profile 的代表权 | 接入可信 User -> ProfileLink/代表权校验；无登录流程只建临时 Testee；补跨用户、枚举与错误脱敏负测 |
| ACTOR-TESTEE-IDOR | P0 安全阻断 | collection `/api/v1/testees/:id` GET/PUT/care-context 直接按裸 TesteeID 调 Actor gRPC，未使用已有 `testeeaccess.Authorizer` | 三个入口统一前置 User -> ProfileLink/关系 -> Testee gate；跨用户、跨机构、inactive link、IAM/cache 故障均 fail closed；增加架构守卫 |
| STATISTICS-INACTIVE-CLINICIAN | P1 授权缺口 | self overview 可先命中 operatorUserID query cache，未每次验证 active clinician identity | 每次请求先走非缓存 active identity 授权，再按 clinicianID 读统计；补停用前已预热缓存负测 |
| COMPAT-OBS-2026-09 | P2 观察 | 最新 workflow 因 anchor/指标清单不完整失败；没有观测到窗口增量，但也没有连续窗口证据 | 修正只读观察契约，逐项取得完整 30 天窗口；窗口完成后仍需单独批准退役 |
| DEFINITION-V2-FRONTEND | 跨仓 P2 | 后端严格版本路由已成立；前端显式 TemplateID/TemplateVersion 契约仍由跨仓任务跟踪 | 前端类型、发布映射和 characterization fixture 门禁通过 |

## 8. 验证入口与证据边界

| 门禁 | 当前结果/口径 |
| --- | --- |
| API 同步 | `python3 scripts/compare_api_docs.py`：apiserver 191 operations / 171 unique paths、collection 50 / 48，全部检查通过 |
| 文档卫生 | `python3 scripts/check_docs_hygiene.py`；必须在本轮修改后重新执行 |
| 文档事实 | `python3 scripts/check_docs_facts.py`；必须在本轮修改后重新执行 |
| Markdown diff | `git diff --check`；只证明补丁格式，不证明内容正确 |
| 最后主分支 CI | 31896282313；只覆盖 `8f663383...`，不覆盖之后的 `1afaae03...`、本轮 `df09c14c...` 主体文档提交或剩余本地差异 |
| 实际部署 | 31896757704；原始日志确认三个服务镜像 SHA，不由后续 Ping workflow 代替 |
| 最新周期巡检 | 32085615223；是 2026-08-18 08:45 CST 的主机/服务时点证据，不是数据审计 |
| Mongo consistency | 2026-08-16 01:13 CST one-off；退出码 2、finding 269，scheduler 仍关闭 |

## 9. 破坏性操作边界

本台账不授权任何新的生产 `DROP`、`DELETE`、Mongo collection 删除、Redis 批量清理、checkpoint 重置或业务 ledger 修改。

1. 269 条孤立 Outbox 只能在独立维护任务中处理；必须先冻结精确清单、说明业务与合规影响、准备备份/回滚和写后复验。
2. 不得为了让指标变绿而删除 finding、直接改 checkpoint、跳过阶段或降低 severity。
3. scheduler 的 `enable: true` 是独立生产配置变更，不随文档合并、CI 成功或一次 one-off 自动生效。
4. 兼容观察窗口完成只提供退役候选证据，不自动授权删除接口、字段、指标或历史数据。

## 10. 定档签署

当前可签署的结论仅为：页首机器元数据以 `git:HEAD` 表示当前 checkout，本轮主体文档与基础设施复核基线已前移到 `df09c14c...`，复核起点 `1afaae03...` 只保留为历史父基线；可引用的 CI、精确 SHA 生产部署和 2026-08-18 08:45 CST 服务巡检仍分别属于 `8f663383...`。REST 机器契约为 191/50 个 method-path operations（171/48 unique paths）；Mongo consistency audit 能力已实现并完成一次正式七阶段生产 one-off，但发现 269 条历史 high drift，生产 scheduler 保持关闭。Actor 的两条 P0 授权缺口和 Statistics inactive clinician 缺口是本轮源码语义审计结论，尚未修复，不能签为安全闭环。

本轮文档修复、机器门禁和语义签署在提交并取得对应 CI 前，只是本地工作区证据；它们记录风险，但不修复上述业务代码缺口。后续维护必须继续遵循“源码/机器契约 → 本地验证 → CI → 精确 SHA 部署 → 带时间的生产证据 → 更新本台账”，不得用后一层的成功反推前一层之外的结论。
