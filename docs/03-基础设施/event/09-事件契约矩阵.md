# 事件契约矩阵

本文是当前可运行 event 契约的基线。`configs/events.yaml` 决定 event type、delivery、Topic 与 handler；本表补充生产者、Outbox store、immediate、幂等和消费结算。任何新增或调整事件都必须同步更新本表、目录与测试。

## 1. 事件矩阵

| Event type | Owner | Producer | Delivery | Profile | Store | Immediate | Priority | Handler | Idempotency policy | Settlement policy | Handler failure behavior |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `questionnaire.changed` | `survey/questionnaire` | Questionnaire lifecycle 后置发布 | `best_effort` |  | `none` | `false` |  | `questionnaire_changed_handler` | `published-lifecycle-post-action` | `handler_error_nack` | 解析错误返回 handler error；发布后的附加动作失败只记录 |
| `assessment_model.changed` | `modelcatalog` | AssessmentModel lifecycle 后置发布 | `best_effort` |  | `none` | `false` |  | `assessment_model_changed_handler` | `published-model-post-action` | `handler_error_nack` | 解析错误返回 handler error；发布后的附加动作失败只记录 |
| `answersheet.submitted` | `survey/answersheet` | AnswerSheet 提交事务 | `durable_outbox` | `mongo_domain_events` | `Mongo domain_event_outbox` | `true` | `p0` | `answersheet_submitted_handler` | `answersheet-id-lease-and-ensure-assessment` | `handler_error_nack` | handler error NACK；成功或重复事件 ACK |
| `evaluation.requested` | `evaluation` | Assessment 创建事务 | `durable_outbox` | `assessment_mysql_events` | `MySQL domain_event_outbox` | `true` | `p0` | `evaluation_requested_handler` | `evaluation-run-state-claim` | `handler_error_nack` | 可重试执行失败 NACK；终态或无需评分 ACK |
| `evaluation.outcome.committed` | `evaluation` | Evaluation outcome 提交事务 | `durable_outbox` | `assessment_mysql_events` | `MySQL domain_event_outbox` | `true` | `p1` | `evaluation_outcome_committed_handler` | `report-business-key-run-claim-cas` | `handler_error_nack` | 可重试生成失败 NACK；终态生成结果 ACK |
| `evaluation.failed` | `evaluation` | Evaluation 失败事务 | `durable_outbox` | `assessment_mysql_events` | `MySQL domain_event_outbox` | `false` | `p1` | `evaluation_failed_handler` | `report-status-overwrite` | `handler_error_nack` | payload 或必要依赖错误 NACK；状态投影完成 ACK |
| `interpretation.report.generated` | `interpretation/report` | Interpretation terminal commit 事务 | `durable_outbox` | `mongo_domain_events` | `Mongo domain_event_outbox` | `false` | `p1` | `interpretation_report_generated_handler` | `repeatable-attention-projection` | `handler_error_nack` | payload 解析失败 NACK；attention 同步失败仅记录后 ACK |
| `interpretation.report.failed` | `interpretation/report` | Interpretation terminal commit 事务 | `durable_outbox` | `mongo_domain_events` | `Mongo domain_event_outbox` | `false` | `p1` | `interpretation_report_failed_handler` | `terminal-failure-fact` | `handler_error_nack` | payload 解析失败 NACK；失败事实记录完成 ACK |
| `task.opened` | `plan` | AssessmentTask 状态变更 | `best_effort` |  | `none` | `false` |  | `task_opened_handler` | `notification-event-metadata` | `handler_error_nack` | 通知失败仅记录后 ACK；返回的 handler error NACK |
| `task.completed` | `plan` | AssessmentTask 状态变更 | `best_effort` |  | `none` | `false` |  | `task_completed_handler` | `notification-event-metadata` | `handler_error_nack` | 通知失败仅记录后 ACK；返回的 handler error NACK |
| `task.expired` | `plan` | AssessmentTask 状态变更 | `best_effort` |  | `none` | `false` |  | `task_expired_handler` | `notification-event-metadata` | `handler_error_nack` | 通知失败仅记录后 ACK；返回的 handler error NACK |
| `task.canceled` | `plan` | AssessmentTask 状态变更 | `best_effort` |  | `none` | `false` |  | `task_canceled_handler` | `notification-event-metadata` | `handler_error_nack` | 通知失败仅记录后 ACK；返回的 handler error NACK |

`best_effort` 的发布失败不会形成 Outbox 重试；`durable_outbox` 的发布失败由 relay 回写为 `failed` 并重试。所有进入 worker 的消息仍按至少一次消费设计。

### 1.1 附加消费者

| Consumer ID | Event | Runtime | Topic | Channel | Idempotency policy | Settlement policy |
| --- | --- | --- | --- | --- | --- | --- |
| `modelcatalog.hot_rank_projection` | `answersheet.submitted` | `apiserver` | `qs.evaluation.lifecycle` | `qs-apiserver-modelcatalog-hot-rank-v1` | `redis-processed-key-by-event-id` | `handler_error_nack` |

## 2. footprint 禁用策略

`behavior_footprint` 不是 event catalog 的聚合投影：没有 `footprint.*` event、Topic、Outbox record 或 worker handler。`durable_outbox` 只保证表中列出的业务事实可靠出站，**不得**触发实时 behavior 投影。

行为轨迹的常规写入者是 `behavior_journey_scan`：它从入口、答卷、测评和报告事实表扫描重建 footprint、episode 和统计。`evaluation.failed` 只更新 report status，不再通过 worker 回调实时投影 footprint。历史 Outbox 中即使出现旧的 footprint event，也不受当前 worker 支持。

## 3. MQ 结算基线

| 输入分类 | 识别规则 | 处理 | 结算 |
| --- | --- | --- | --- |
| poison | metadata 没有 `event_type` 且 envelope 无法解析 | 记录 poison outcome，不调用 handler | ACK |
| unknown | 可取得 event type，但启动时没有对应 handler | warning，跳过 | ACK |
| handler failure | 已找到 handler 且返回 error | 记录 dispatch error | NACK，由 MQ 重试 |
| success | handler 返回 nil | 记录 consume outcome | ACK |

ACK/NACK 自身失败只记录专用 outcome；不能据此宣称消息已被可靠安置。当前没有通用 DLQ 或最大消费重试次数。

## 4. 信号矩阵

| Signal | Publisher | Subscriber | 保障 |
| --- | --- | --- | --- |
| `report_status_changed` | apiserver、worker | collection-server | Redis Pub/Sub 唤醒；订阅者重查状态 |
| `questionnaire_cache_changed` | apiserver | apiserver、collection-server | 本地缓存失效/预热；TTL 或下次查询兜底 |
| `scale_cache_changed` | apiserver | apiserver | 本地缓存失效/预热；TTL 或下次查询兜底 |
| `typology_model_cache_changed` | apiserver | apiserver、collection-server | 本地缓存失效/预热；TTL 或下次查询兜底 |

`configs/signals.yaml` 是声明清单而非运行时装载文件；`internal/pkg/signalcatalog` 的测试保证其与代码常量一致。

## 5. 工程化约束

本矩阵由 `EventSpec + EffectiveRegistry` 的同步测试保护。修改事件时必须同时更新 `events.yaml`、代码常量、EventSpec、worker registry 和本矩阵。

- Mongo/MySQL Store、relay、ready index、immediate 和 reconciler 由 EventSubsystem 按 profile 唯一持有。
- hot-rank 是同 Topic、独立 channel 的附加消费者，不再使用 publish hook。
- unknown event 产生独立 `unknown_acked` outcome，结算仍为 ACK。
- priority 只影响 claim 顺序，不改变 delivery 或 settlement。

整体结构和维护边界见 [10-事件工程化设计.md](10-事件工程化设计.md)。
