# Redis 整体架构

**本文回答**：`qs-server` 为什么把 Redis 分成 Foundation、Cache、Lock、Governance 四层，三进程分别使用哪些能力，当前包边界和设计模式是什么。

## 30 秒结论

| 层 | 核心职责 | 当前代码 |
| -- | -------- | -------- |
| Foundation | family、profile、namespace、runtime bundle | [redisplane](../../../internal/pkg/redisplane)、[redisbootstrap](../../../internal/pkg/redisbootstrap)、[rediskey](../../../internal/pkg/rediskey) |
| Cache | apiserver 读侧缓存、payload、query、hotset、SDK adapter | [cachebootstrap](../../../internal/apiserver/cachebootstrap)、[infra/cache*](../../../internal/apiserver/infra) |
| Lock | 三进程共享 Redis lease primitive | [redislock](../../../internal/pkg/redislock) |
| Governance | family 状态、manual warmup、hotset、repair complete | [cachegovernance](../../../internal/apiserver/application/cachegovernance)、[cacheobservability](../../../internal/pkg/cacheobservability) |

## 四层架构图

```mermaid
flowchart TB
    subgraph F["Foundation"]
        Plane["redisplane\nFamily + Route + Runtime"]
        Bootstrap["redisbootstrap\nRuntimeBundle"]
        Key["rediskey\nNamespace Builder"]
        Obs["cacheobservability\nFamily status + metrics"]
    end

    subgraph C["Cache"]
        Entry["cacheentry\nPayloadStore"]
        Object["infra/cache\nObject decorators"]
        Query["cachequery\nVersioned query"]
        Hotset["cachehotset\nRedis ZSet"]
        Target["cachetarget\nWarmupTarget model"]
        SDK["wechatapi\nSDK cache adapter"]
    end

    subgraph L["Lock"]
        Lock["redislock\nSpec + Lease + Manager"]
        Leader["scheduler leaderLock"]
        Submit["collection SubmitGuard"]
        Worker["worker duplicate gate"]
    end

    subgraph G["Governance"]
        Subsystem["cachebootstrap.Subsystem"]
        Coordinator["cachegovernance.Coordinator"]
        Status["cachegovernance.StatusService"]
        REST["transport/rest\ninternal governance API"]
    end

    Bootstrap --> Plane
    Plane --> Object
    Plane --> Query
    Plane --> Hotset
    Plane --> Lock
    Key --> Entry
    Obs --> Subsystem
    Entry --> Object
    Entry --> Query
    Target --> Hotset
    Target --> Coordinator
    Subsystem --> Coordinator
    Subsystem --> Status
    Status --> REST
    Lock --> Leader
    Lock --> Submit
    Lock --> Worker
```

## 三进程 Redis 角色图

```mermaid
flowchart LR
    Redis["Redis profiles"]

    subgraph API["qs-apiserver"]
        APIRuntime["process/resource_bootstrap"]
        Cache["Cache subsystem"]
        Scheduler["Schedulers"]
        SDK["WeChat SDK adapter"]
        Gov["Governance REST"]
    end

    subgraph COL["collection-server"]
        COLRuntime["process/resource_bootstrap"]
        Rate["Distributed limiter"]
        Guard["SubmitGuard"]
    end

    subgraph WORKER["qs-worker"]
        WRuntime["process/resource_bootstrap"]
        Gate["Answersheet lock gate"]
    end

    Redis --> APIRuntime --> Cache
    APIRuntime --> Scheduler
    APIRuntime --> SDK
    Cache --> Gov
    Redis --> COLRuntime --> Rate
    COLRuntime --> Guard
    Redis --> WRuntime --> Gate
```

| 进程 | 使用 family | 不承担 |
| ---- | ----------- | ------ |
| `qs-apiserver` | `static_meta/object_view/query_result/meta_hotset/sdk_token/lock_lease` | 不把 Redis 当主数据源 |
| `collection-server` | `ops_runtime/lock_lease` | 不做领域读缓存 |
| `qs-worker` | `lock_lease` | 不做 object/query cache，不做 Redis 统计增量写入 |

## 包依赖图

```mermaid
flowchart TD
    Process["process packages"] --> RedisBootstrap["internal/pkg/redisbootstrap"]
    RedisBootstrap --> RedisPlane["internal/pkg/redisplane"]
    RedisBootstrap --> RedisLock["internal/pkg/redislock"]
    RedisPlane --> RedisKey["internal/pkg/rediskey"]
    RedisPlane --> CacheObs["internal/pkg/cacheobservability"]

    APICache["apiserver/cachebootstrap"] --> RedisBootstrap
    APICache --> CachePolicy["infra/cachepolicy"]
    APICache --> CacheHotset["infra/cachehotset"]
    APICache --> CacheGov["application/cachegovernance"]

    CacheObj["infra/cache"] --> CacheEntry["infra/cacheentry"]
    CacheQuery["infra/cachequery"] --> CacheEntry
    CacheHotset --> CacheTarget["cachetarget"]
    CacheGov --> CacheTarget
```

## 设计模式视角

| 模式 | 当前落点 | 解决的问题 |
| ---- | -------- | ---------- |
| Facade / Composition Root | `cachebootstrap.Subsystem` | 把 runtime、policy、hotset、lock、governance 装配收口 |
| Adapter | `cacheentry.PayloadStore`、SDK cache adapter | 隔离 Redis entry 细节或第三方 SDK 接口 |
| Decorator | object repository cache | 不改 repository 接口增加缓存能力 |
| Template Method / Function Object | `ReadThroughRunner` | 固化 hit/miss/load/writeback 流程 |
| Versioned Key | `cachequery` | query/list 通过 bump token 失效 |
| Lease | `redislock` | 短期排他、选主、重复抑制 |

## Verify

- family 定义以 [redisplane/catalog.go](../../../internal/pkg/redisplane/catalog.go) 为准。
- 三进程 runtime 装配以各自 `process/resource_bootstrap.go` 为准。
- cache 子系统以 [cachebootstrap/subsystem.go](../../../internal/apiserver/cachebootstrap/subsystem.go) 为准。
- lock 语义以 [redislock/doc.go](../../../internal/pkg/redislock/doc.go) 和 [redislock/spec.go](../../../internal/pkg/redislock/spec.go) 为准。
