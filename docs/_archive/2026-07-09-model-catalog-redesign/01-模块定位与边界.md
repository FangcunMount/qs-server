# Assessment Model 模块定位与边界

## 1. 模块定位

Assessment Model 是测评模型资产层。它管理医学量表、人格模型、行为能力测评等模型资产的定义、版本、发布快照和绑定关系。

`scale` 不再是独立核心模块，而是 Assessment Model 下的一类模型资产或兼容能力。

---

## 2. 负责范围

| 范围 | 说明 |
| ---- | ---- |
| 模型身份 | `AssessmentKind`、模型编码、算法或子类型 |
| 模型定义 | 模型名称、状态、描述、payload |
| 模型发布 | 发布版本和冻结快照 |
| 模型绑定 | 问卷、入口、执行器、解释模型的关联 |
| 兼容归并 | `scale/typologymodel` 作为 legacy register name |

---

## 3. 不负责范围

| 不负责 | 归属模块 |
| ------ | -------- |
| 用户答卷事实 | `survey` |
| 一次测评执行状态 | `evaluation` |
| 最终报告实例 | `interpretation` |
| 任务调度 | `plan` |
| 指标聚合 | `statistics` |

---

## 4. 边界反例

- `AssessmentModel` 不是 `Questionnaire`。
- `AssessmentModelSnapshot` 不是 `EvaluationResult`。
- `Scale` 不是独立核心模块。
- 模型发布不是报告生成。
- 模型 payload 不是用户答卷事实。

---

## 5. 代码锚点

- 模型资产领域：[`internal/apiserver/domain/modelcatalog`](../../../internal/apiserver/domain/modelcatalog/)
- Scale 资产路径：[`internal/apiserver/domain/modelcatalog/scale`](../../../internal/apiserver/domain/modelcatalog/scale/)
- Personality 发布：[`internal/apiserver/domain/modelcatalog/personality`](../../../internal/apiserver/domain/modelcatalog/personality/)
- 容器装配：[`internal/apiserver/container/modules/modelcatalog`](../../../internal/apiserver/container/modules/modelcatalog/)
