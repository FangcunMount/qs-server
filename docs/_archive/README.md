# Archive

`docs/_archive` 用于存放历史设计稿、旧版迁移文档和已经退出当前真值层的材料。

当前状态：

- 该目录默认**不作为现行文档真值层**
- 现行文档写作、校验与对外引用应优先指向 `docs/00-06`
- 如果未来需要归档旧文档，可放在本目录下，并在文件开头明确标注“历史资料 / 非当前实现”

如果你在维护 `docs/`：

- 不要从 `_archive` 直接拷贝结论到现行文档
- 需要引用历史背景时，应先回到当前代码与契约重新核对
