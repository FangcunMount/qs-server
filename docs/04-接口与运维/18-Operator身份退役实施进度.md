# Operator 身份退役实施进度

本文件记录 2026-09-11 的本地实施状态，**不是生产退役完成证明**。IAM 完整赋权事实兼容版本已提交推送（`7eef4845`，SDK `v5.1.0`）；QS 与运营端最终版本尚未统一部署，生产医生身份未执行退出。此前独立发布的门店批量管理前端版本为 `6f2a93bbd8948cc157895f2b3e192e76acb4f1e5`，已核对部署流程及公开版本标识。

## 已实现并验证的代码

- Actor 下新增 Operator 退出领域状态、应用用例、持久化端口及 MySQL 实现；先停用，再撤销受管授权，确认策略后软删除。失败保留任务，可按原请求恢复。
- 退出任务与停用、本地删除与任务完成各自原子提交；授权调用位于 QS 事务之外。生产组合根对注册、启用、角色修改及持久化写入接入同一用户锁和退出状态检查。
- 新增 `/api/v1/operators` 管理路由；DELETE 使用 `expected_version`、`request_id`、`reason`，处理中返回 202；`/:id/retirement` 可查询已软删除目标的退出任务。
- 运营端改为“运营人员”，退出弹窗不把受理当完成，网络结果不明时要求先查询状态，再沿用原请求。旧 `/admin/staff` 跳转新地址。
- 删除 Clinician 后台绑定领域字段、绑定行为、个人工作台及身份探测调用链。总部医生管理、关系管理、二维码创建/解析/入组路径保留。
- 旧 Staff、医生个人及绑定接口返回不依赖数据库的 410；含兼容 practitioner 路由和个人统计路由。医生写入包含旧绑定字段（含 null）时明确拒绝。
- 活动医生、关系、入口及统计响应去除后台绑定字段；生成 REST 契约同步。
- 普通无业务范围的 Operator 继续被拒绝，不因删除医生绑定自动获得公司全部受试者范围。Scope 建设独立进行。
- 退出查询绕过进程缓存，同时保留共享策略版本水位检查。管理员通配权限在角色名被应用范围过滤时仍会阻止退出。

## 当前验证

- QS 整体 `go test ./...` 通过。
- 本轮 Actor、工作台、IAM 适配、REST 和快照加载相关静态检查：0 issues。
- 隔离 MySQL 退出事务专项通过，包括中断恢复、审计写入失败回滚、用户锁及退出后的写入阻断。CI 已配置专项必需 DSN 的检查。
- 运营端 89 个测试套件、295 项测试通过；全量 lint 和生产构建通过。
- 差异格式检查通过。

以上不替代完整新库/存量结构迁移、退役清单工具测试、生产人员核对或线上业务验收。

## 退役工具与结构迁移

`operator-retire preflight/apply/status/verify` 使用独立清单和原始绑定归档。必须明确选择 `legacy`（退役前 staff）或 `final`（改表后 operators）布局，不自动回退。IAM 必须支持完整赋权事实；其他应用、全局或受保护角色会阻止自动退出。

预演记录原角色、医生绑定、业务数据和 Operator 事实指纹。首次 Apply 重新预演；逐人调用 Actor 退出服务，失败保持停用，沿用原清单恢复。完成态重跑只验证，不重复撤权；后续事实变化报告 `applied_drifted`，不覆盖。归档与清单校验失败报告 `invalid`。状态查询成功不等于验收通过。

迁移 74 建立退出任务，75 建立独立执行清单及绑定归档，76 在退役/归档完成后物理改表、改索引并删除医生绑定列。76 的 down 只恢复结构及原绑定，不启用账号或重新授予权限；损坏归档阻止恢复。历史 SQL 不改写。

运行环境：

- `OPERATOR_RETIRE_ID`：本次固定迁移标识。
- `OPERATOR_RETIRE_LAYOUT=legacy|final`：明确存储布局。
- `OPERATOR_RETIRE_ACTOR_ID`：已有有效总部 Operator 对应 IAM UserID。
- `OPERATOR_RETIRE_REPORT`：受限目录中的全新报告路径；文件以 0600 创建，不覆盖旧报告。
- Apply 额外要求原报告 `fingerprint` 和 `OPERATOR_RETIRE_MAINTENANCE=true`。后者是维护措施已落实的声明，不会自动关闭业务写入。

CLI 复用 QS 配置参数与服务身份，禁止在日志或命令行传递明文密码；日志只记录摘要，异常人员及原因保存在受限报告。首次预演到切换期间应暂停后台人员、绑定和授权写入，并停止会刷新投影或变更核对事实的旧实例/任务。完整业务数据指纹包含医生、门店、关系、入口、受试者、测评及计划数据；发现变化必须重新核对。

## 发布顺序与证据

1. 发布 IAM 兼容依赖并核对运行版本；QS 引用发布的 SDK，不依赖本地工作区替换。
2. 保存完整数据库备份、当前版本与校验和。先执行 74、75，禁止提前执行 76。
3. 预演、核对异常，在维护窗口重新核对指纹后执行退出；使用 verify 验证业务数据保持、授权撤销及本地投影。
4. 完成归档后执行 76，统一部署 QS 和运营端，更新全部实例及任务；再次使用 final 布局 verify。
5. 普通已退出身份必须拒绝后台访问，总部身份必须保持可用；保存数据库 dirty 状态、部署 SHA、策略与投影收敛、业务数据和二维码未变的证据后恢复后台访问。

当前已完成本地完整迁移链、存量切换/恢复、失败恢复、重复执行及绑定/授权漂移专项；这些不替代生产执行和真实业务验收。生产只读核对（2026-09-11）为 schema 73、119 个医生绑定的有效后台身份、6 家门店、117 个医生配置、0 个调店失效入口。执行必须重新读取，不能用此快照直接迁移。

不要自动恢复已撤销授权，不通过恢复旧应用复活调店失效二维码。已发生退出后优先向前修复；回退需要兼容 Operator 禁用和二维码永久失效语义的版本。
