# ModelCatalog 设计问题与重构清单

> 状态：**未关闭项续期实施中**。截至 2026-07-22，MC-R001、R007、R008、R010、R011、R013、R014、R015、R016、R019 共 10 项已关闭；其余条目继续按仓库内契约、Replica Set 集成和维护窗口证据严格验收。本文是 ModelCatalog 的活动重构台账，每一项都区分“当前事实”“验收结论”与“处理方案”，不得把已提交代码直接等同于问题关闭。

## 1. 本文的用法

本文不是另一篇 ModelCatalog 架构说明，而是后续改进的统一入口。它解决三个问题：

1. 我们在深度分析中究竟发现了哪些问题；
2. 这些问题哪些会影响结果正确性，哪些是发布与扩展性债务；
3. 后续单独开启重构时，应从哪个编号开始，完成到什么程度才能关闭。

使用约定：

- 后续的分析报告、Issue、提交或 PR 优先引用稳定编号，例如 `MC-R002`；
- 实施前必须重新核对当时源码、数据和运行环境；
- 一次重构只承诺经过确认的有界范围；本轮 DefinitionV2 切换明确采用维护窗口和删除重建，不保留目标链路历史兼容；
- 历史 snapshot、Outcome、Norm 或已受理 Assessment 如不符合 current-only 契约，由运营备份后删除并重新导入/发布；
- 问题关闭后更新状态、证据、验收结果和剩余风险，不直接删除条目。

---

## 2. 30 秒结论

ModelCatalog 的主干设计已经成立：

```text
AssessmentModel head
  -> DefinitionV2
  -> Questionnaire + Model 联合发布
  -> 不可变 AssessmentSnapshot
  -> active admission / exact-version execution
  -> Calculation
  -> Evaluation Outcome
  -> Interpretation
```

当前问题不是“整个架构没实现”，而是五类已经可以具体改造的缺口：

1. **已关闭**：MC-R001、MC-R007、MC-R008、MC-R010、MC-R011、MC-R013、MC-R014、MC-R015、MC-R016、MC-R019 已满足条目级关闭条件；
2. **仓库内已推进但尚未关闭**：MC-R002、MC-R003、MC-R004、MC-R005，以及联动的 MC-R012、MC-R017、MC-R018 已补仓库内契约；MC-R005/MC-R012 的本地非生产 Replica Set G4 门禁与生产 cutover audit 已通过，剩余条目仍缺修复版本的全 family 部署 smoke；
3. **本轮已消除的明确缺口**：Interpretation current path 不再 raw-score rematch/soft default；Scale 非零 Constant 发布拒绝并按 SortOrders 稳定输出；统一输入守卫精确比较完整身份；cutover audit 已校验 Outcome/ReportInput/NormReference/Mongo migration 与 indexes；
4. **仅待目标环境判定**：MC-R006 的仓库内冻结、路由、fail-closed 与生产 inventory 已通过；最终结论只差修复版本的部署 smoke；
5. **业务决策缺口**：MC-R009、MC-R020 仍依赖 Questionnaire 深兼容或 Norm/认知任务产品语义，保持待业务决策。

治理原则：

> 先修正可能改变结果或违反业务契约的问题，再将错误前移到发布阶段，最后才做 handler 拆分、payload 收敛和兼容代码退出。

---

## 3. 优先级与状态语义

### 3.1 优先级

| 级别 | 含义 | 典型标准 |
| --- | --- | --- |
| P0 | 结果正确性、业务契约或持久化约束风险 | 可能选错常模、生成无意义 Assessment、临界分结果不确定、新环境无法重建正确 schema |
| P1 | 发布门禁、版本一致性和执行可追溯性 | 错误未在发布时拒绝，Worker 需运行时猜测 |
| P2 | 模块边界、扩展性与可维护性 | handler 职责膨胀、DTO 表达力不一致、多份兼容投影 |
| P3 | 由新业务需求驱动的能力扩展 | 多常模、Norm 独立生命周期、新认知任务 |

P0 不表示必须在一个 PR 中一次完成，只表示在开启低优先级架构清理之前，应优先关闭或建立可验证的风险防线。

### 3.2 状态

| 状态 | 含义 |
| --- | --- |
| 规划改造 | 问题已由当前源码或存储契约确认，尚未实施 |
| 待补证据 | 静态源码不足以确认生产现状，需要数据或部署证据 |
| 待业务决策 | 技术可以实现，但缺少必要的产品/领域语义 |
| 已实现待验收 | 代码已改，但尚缺少对应风险层级的验收证据 |
| 已确认不通过 | 二次复验已发现明确的代码、配置、测试、文档或验收工具缺口，不能仅靠补环境记录关闭 |
| 已关闭 | 代码、数据迁移、测试和文档均完成，且无剩余必做项 |

### 3.3 2026-07-22 严格验收基线

本轮按“条目自身的代码、相关测试、文档，以及条目明确要求的数据/部署证据全部齐全才关闭”的口径执行。验收链路为：

```text
DefinitionV2
  -> 发布校验
  -> AssessmentSnapshot
  -> Evaluation Input / exact Descriptor
  -> Calculation
  -> Outcome / ReportInput
  -> Interpretation
```

已完成的仓库内验证：

- ModelCatalog、Calculation、Evaluation、Interpretation 相关定向 `go test -count=1` 通过；
- `python3 scripts/check_docs_hygiene.py`、`python3 scripts/check_docs_facts.py` 与 `git diff --check` 通过；
- 本轮重新执行全仓 `go test -count=1 ./...` 已通过；此前记录的 statistics 测试桩签名漂移在当前工作树已不存在。

已完成的生产维护窗口证据：

- 历史 Assessment/Outcome/ReportInput/Checkpoint 已按授权清理，Model、Questionnaire、Norm 与报告模板资产保留；
- ModelCatalog cutover repair 后，`verify_definition_v2_cutover` 于 2026-07-22 返回 `findings=0`、退出码 0；
- 修复前 smoke 中 SNAP-IV（scale）通过；SPM Sensory 在 Interpretation 暴露旧 `Level` 被错误投影为 OutcomeCode；MBTI 在 Artifact 校验暴露模板 `ModelExtra.Kind=mbti` 与 canonical `personality_type` 不一致；后续九型人格 smoke 又暴露 `trait_profile` 被错误要求冻结 outcomes/profiles，以及 `trait_profile + mbti` 模板路由错配；
- 上述故障均已增加失败契约并完成仓库内修复，但修复版本尚未重新部署和执行全 family smoke，因此相关条目不得关闭。

指标判定：入口清晰度、依赖方向、统一身份守卫、四 family 边界、ReportMap 完整性、Scale 投影、cutover inventory，以及 MC-R005/MC-R012 的本地 Replica Set schema/CAS/rollback 契约为绿；MC-R002～MC-R006、MC-R012、MC-R017、MC-R018 的修复版本部署 smoke 仍为黄，必须保持未关闭。

### 3.4 “待补证据”二次复核（2026-07-22）

本轮没有把“环境不可访问”直接写成业务失败，而是先验证仓库内证据是否足以支持一次有效的环境验收。复核结果如下：

| 项目 | 仓库内补证结果 | 环境证据可用性 | 二次判定 |
| --- | --- | --- | --- |
| MC-R003 | BRIEF-2/SPM Sensory 正向不可变快照、发布校验、精确 Norm 与 Outcome 留痕契约已补；旧 `Level` 与 OutcomeCode 分歧的生产故障已补契约并修复 | cutover audit 退出 0；修复前 SPM Sensory smoke 失败，修复版本尚未复验 | **已实现待验收**，完成修复版本 smoke 后复验 |
| MC-R005 | fail-closed Replica Set harness、fresh/legacy、dirty@13、索引幂等、primary+snapshot+majority 和三类真实 rollback 测试代码已补 | 本地授权的单节点 Mongo 7 Replica Set 已无 SKIP 通过完整 G4；production audit 已核验 migration/index，仍缺部署 smoke | **G4/audit 通过，待 smoke** |
| MC-R006 | RuntimeIdentity 冻结、Mongo 映射、InputSnapshot、exact route 和 fail-closed 定向测试通过 | cutover audit 退出 0；只缺修复版本全 family smoke | **待目标环境判定**；inventory 已通过，等待 smoke |
| MC-R017 | audit 已校验 Outcome schema 2、ReportInput schema 3、NormReference、Mongo dirty/index；活动发布文档已改为 DefinitionV2-only | 历史数据清理后 audit 退出 0；修复版本 smoke 未取得 | **已实现待验收**，等待部署 smoke |
| MC-R018 | registry 数量 7、精确 route、fail-closed、proto reserved、源码 ratchet与活动文档均已对齐 canonical identity | canonical inventory 通过；修复前 MBTI Artifact 失败，修复版本消费方 smoke 未取得 | **已实现待验收**，等待修复版本 smoke |

本轮实际执行：

- 相关 ModelCatalog、Evaluation routing/runtime、Mongo/MySQL mapper、transaction options、migration/index、cutover audit、BRIEF-2/SPM seed 和 collection OpenAPI 定向 `go test -count=1` 全部通过；
- 使用仅绑定 `127.0.0.1:27018` 的本地 Mongo 7 单节点 Replica Set（`rs0`）执行 G4 四包命令，fresh install、兼容 down、不兼容 down 拒绝、dirty@13、模型/问卷 CAS 和 questionnaire/model/commit 三类 rollback 全部通过且无 SKIP；该证据验证 schema/事务契约，不替代目标环境数据、索引和部署 smoke；
- 完成授权的历史数据清理与 ModelCatalog cutover repair 后，生产只读 `verify_definition_v2_cutover` 返回 `findings=0`、退出码 0；该结果关闭 inventory 缺口，但不能替代修复版本的端到端 smoke。

MC-R006 的 inventory 条件已经满足；剩余环境复验固定为修复版本 REST/gRPC/Collection/Worker smoke 全绿。任何 family 失败都继续保持未关闭。

### 3.5 G5 生产 smoke 与故障收口（2026-07-22）

首轮 smoke 使用新答卷和新 Assessment，不回填或改写历史失败结果：

| Family | 首轮结果 | 确定性原因 | 仓库内处理 | 当前结论 |
| --- | --- | --- | --- | --- |
| scale / SNAP-IV | 通过 | exact identity、Outcome 与 Interpretation 链路正常 | 作为 scale 回归门禁保留 | 仅证明 scale family |
| behavioral / SPM Sensory | 失败 | `calcNormTables` 把旧 `ScoreRangeOutcome.Level=none` 投影为 runtime OutcomeCode，冻结 Assets 只存在 canonical `normal`，Interpretation 按约定 fail closed | runtime `TScoreRange.Level` 字段保留，但只承载 `OutcomeCode`；BRIEF-2/SPM Sensory 增加 DefinitionV2→runtime→Calculation→Outcome→Interpretation 契约 | 待修复版本 smoke |
| typology / MBTI | 失败 | MBTI 模板输出 `ModelExtra.Kind=mbti`，不满足 Artifact 的 `personality_type/trait_profile` canonical 契约 | MBTI/SBTI 改为 `personality_type`，Big Five 改为 `trait_profile`；增加三类 Builder→Artifact 契约，严格校验不放宽 | 待修复版本 smoke |
| typology / ENNEAGRAM_45 | 失败 | `trait_profile` 输出完整因子向量、不选择 OutcomeCode，但 ReportInput schema 3 错误地要求冻结 outcomes/profiles；活动模型还错误路由到 `mbti` 模板 | schema 3 按 DecisionKind 判定：trait profile 要求 report spec + FactorCatalog，类型选择决策仍要求 outcomes/profiles；冻结目录恢复因子名称；新增 `enneagram` 模板及 template/adapter 发布守卫；独立 oneoff 不依赖历史清零或 MySQL，仅在 `ENNEAGRAM_45@v16` 完整前值匹配时事务修正模板 | 待执行 oneoff、部署后新数据 smoke |

Executor 现按 `phase=builder` 与 `phase=artifact_validation` 分离记录内部原始错误；外部 Artifact 失败仍稳定返回 `invalid_artifact` 且为 terminal，不泄露内部校验细节。MC-R016 的 fail-closed 行为正是本次故障被及时暴露的保护机制，关闭结论不回退。

---

## 4. 重构总表

| ID | 优先级 | 问题 | 状态 | 验收结论 | 主要影响面 |
| --- | --- | --- | --- | --- | --- |
| MC-R001 | P0 | 独立 Questionnaire 仍创建空壳 Assessment | 已关闭 | 通过 | Survey → ModelCatalog → Evaluation 准入 |
| MC-R002 | P0 | NormSubject 未闭环且 Parametric Band 缺失信息时可误匹配 | 已实现待验收 | 不通过（待环境） | 行为评定结果正确性 |
| MC-R003 | P0 | `behavioral_rating` 没有强制 Norm 与 Primary NormConclusion | 已实现待验收 | 不通过（待环境） | 模型类型语义、发布与路由 |
| MC-R004 | P0 | ScoreRange、Level 与 OutcomeCode 没有统一判定契约 | 已实现待验收 | 不通过（待环境） | 四类模型结果正确性 |
| MC-R005 | P0 | 标准 Mongo migration/index 不能完整重建 unified schema | 已实现待验收 | 不通过（G4/audit 通过，待 smoke） | 发布事务、唯一性、新环境 |
| MC-R006 | P1 | 发布 snapshot 未冻结完整 RuntimeIdentity | 待补证据 | 待目标环境判定 | ModelCatalog → Evaluation 路由 |
| MC-R007 | P1 | Definition 发布完整性守卫不统一 | 已关闭 | 通过 | Factor、Decision、Question refs |
| MC-R008 | P1 | 首次发布后 Algorithm 和 Questionnaire code 仍可变更 | 已关闭 | 通过 | 模型身份连续性 |
| MC-R009 | P1 | 各模型类型 Questionnaire Binding Policy 强度不一 | 待业务决策 | 不通过 | 问卷–模型兼容性 |
| MC-R010 | P1 | 四方执行输入不变式未统一收敛 | 已关闭 | 通过 | Assessment/Model/AnswerSheet/Questionnaire |
| MC-R011 | P1 | Norm 跨资产兼容校验与 Raven 常模入口未闭环 | 已关闭 | 通过 | Calibration 发布门禁 |
| MC-R012 | P1 | 发布幂等、dirty state 与并发冲突语义不完整 | 已实现待验收 | 不通过（G4/audit 通过，待 REST smoke） | 运营编辑与联合发布 |
| MC-R013 | P2 | Definition handler 按 Kind 过度聚合责任 | 已关闭 | 通过 | 新 Algorithm 扩展成本 |
| MC-R014 | P2 | Strategy capability、FactorGraph 和 Scoring 静态校验未完全同源 | 已关闭 | 通过 | 配置可执行性；批次 1–5 已齐 |
| MC-R015 | P2 | ScaleSnapshot 扁平投影损失 DefinitionV2 表达力 | 已关闭 | 通过 | Measure 发布语义与稳定顺序 |
| MC-R016 | P2 | Decision 与 Interpretation 资产物理结构混合 | 已关闭 | 通过 | current-path 只消费冻结 OutcomeCode 与 Assets |
| MC-R017 | P2 | DefinitionV2、runtime payload 与 ReportInput 存在多份投影 | 已实现待验收 | 不通过（待环境） | DefinitionV2-only snapshot + ReportInput schema 3 |
| MC-R018 | P2 | 历史 Algorithm/default/legacy alias 仍参与正常路由 | 已实现待验收 | 不通过（待环境） | exact family+decision 路由；legacy decoder/alias 已删除 |
| MC-R019 | P2 | 部分纯计算仍位于 Evaluation application | 已关闭 | 通过 | 批次 1–4 + EV-R013；纯算主缺口收口 |
| MC-R020 | P3 | Norm 资产治理和更广泛认知任务能力尚未产品化 | 待业务决策 | 不通过 | usage 审计切片已落地；其余待业务 |

---

## 5. P0：正确性、业务契约与存储防线

### MC-R001：独立 Questionnaire 不应创建空壳 Assessment

**状态**：已关闭

**验收结果（2026-07-22）**：**通过**

- `TestEnsureUnboundAnswerSheetEndsWithoutCreatingAssessment` 证明未绑定时只保存基础题分并在 `find` 后结束，不进入 create/submit；
- `TestEnsureUnboundReplayReusesLegacyAssessmentWithoutSubmit` 与 `TestEnsureBoundAnswerSheetCreatesAndAutoSubmits` 分别保护重放幂等和既有绑定链路；
- gRPC readiness 将 `AssessmentID=0` 投影为 `no_assessment_required`，Worker 对成功响应不把零 ID 当失败，因而不会发出 `evaluation.requested`。

**关闭说明**：`bound=false` 的更细业务分类和存量空壳 Assessment 归档仍由 EV-R002/运营治理承接，不改变本项“独立问卷不得新建 Assessment”的关闭结论。

**当前事实（改造后）**

`assessmentintake.Service.Ensure` 在保存 AnswerSheet 基础分并解析绑定后：

- `bound=false` 且无已有 Assessment：直接成功返回（`AssessmentID=0`），不调用 `CreateForAnswerSheet`，不写 queued report-status，不匹配/完成 Plan；
- `bound=false` 但已有历史空壳 Assessment：幂等复用该记录，不自动 Submit；
- `bound=true`：创建（或复用）并自动提交 Assessment，行为不变。

**已确认业务边界**

> 独立 Questionnaire 可以作为信息收集器；未绑定 AssessmentModel 时，提交成功只产生 AnswerSheet 及基础题分，不应产生 Assessment。

**目标设计**

```text
AnswerSheet 已持久化
  -> 计算并保存基础题分
  -> 查询 active AssessmentBinding
      -> 未绑定：链路成功结束
      -> 已绑定：创建并提交 Assessment
```

**验收条件**

- 独立问卷提交保留 AnswerSheet 和基础题分；
- 不创建 Assessment，不设置 queued report status，不发出 `evaluation.requested`；
- `answersheet.submitted` 重放仍幂等；
- 已绑定问卷的现有自动提交链路不变。

**实施证据**

- `Ensure` 在 `!bound` 时提前返回 `no_assessment_required`；
- 单元测试：`TestEnsureUnboundAnswerSheetEndsWithoutCreatingAssessment`、`TestEnsureUnboundReplayReusesLegacyAssessmentWithoutSubmit`、`TestEnsureBoundAnswerSheetCreatesAndAutoSubmits`。

**剩余风险**

- `bound=false` 仍同时覆盖「合法独立问卷」与「本应有模型但 binding 缺失 / resolver 未装配」；细粒度分流见 Evaluation `EV-R002`；
- 存量 unbound pending Assessment 需另行审计与归档，本项不迁移历史数据；
- gRPC `EnsureAssessmentResponse.AssessmentId=0` 表示无需 Assessment，调用方若误判为失败需跟进（Worker 已按成功 ACK）。

**证据**

- [assessment intake](../../../internal/apiserver/application/journey/assessmentintake/service.go)
- [unbound intake test](../../../internal/apiserver/application/journey/assessmentintake/service_test.go)
- [已发布模型准入与执行输入](./31-关键链路-已发布模型准入与执行输入.md)
- 跨模块：Evaluation `EV-R002`

### MC-R002：补齐 NormSubject 并修正人口学匹配语义

**状态**：已实现待验收（与 Evaluation `EV-R005` 同批落地）

**验收结果（2026-07-22，G3 复验）**：**不通过（仓库内通过，待环境）**

**尚不能关闭的理由**：behavioral/cognitive 已形成真实 `ValidateImport → ValidateForPublish → AssessmentSnapshot → Input materialization → Calculation → Outcome` 契约，覆盖 specific、generic、年龄端点、missing subject、no cohort、raw 越界、invalid material、retained release、`isn:v2` 与 NormReference；cognitive canonical 投影也只在 NormTableVersion 与冻结 NormRef 精确一致时携带不可变 NormTables。生产存量清理和 cutover audit 已完成，剩余缺口是修复版本的 behavioral/cognitive 部署 smoke。

**处理方案与复验条件**：

1. 已完成 MySQL+Mongo 只读 cutover audit、历史数据清理与 canonical ModelCatalog repair，post-audit 退出 0；
2. 部署本次 OutcomeCode 投影修复，使用新答卷和新 Assessment 完成 BRIEF-2、SPM Sensory、cognitive SPM smoke；
3. 全部 smoke 全绿后关闭；不得复用修复前失败的终态报告。

**G3 进展（2026-07-22）**：BRIEF-2/SPM Sensory retained-release 契约已覆盖 Norm Import、发布校验、DefinitionV2 快照物化、exact-version runtime 解码与 Outcome NormReference；v2 已存在时，引用 v1 的执行仍固定读取 `norm-v1`。新增 behavioral resolution matrix 与 cognitive SPM 全链路证明 StandardScore、cohort 和精确常模版本无损；失败不产出部分 Outcome。

**当前事实（改造后）**

- `ResolveNormScore` 固定 `direct specific → direct generic → parametric specific → parametric generic`，并返回 `specific/generic` 匹配类型；
- `AgeMonths` 为显式可空值：`nil` 是未知，`&0` 是已知 0 月龄；年龄上下界均包含端点；
- 四类失败稳定为 `norm_subject_missing`、`norm_cohort_not_found`、`norm_raw_score_out_of_range`、`norm_invalid`，均为终态且不可自动重试；
- `Calibration.NormRefs` 投影为 Behavioral 必需因子；Cognitive SPM 的 total-factor NormRef 同样为必需。必需因子解析失败时不提交部分 Outcome；
- Behavioral 与 Cognitive 共用无损 Norm runtime assembler，年龄、性别、T 分、百分位、StandardScore 和指针值均完整复制；
- 生产 `BehavioralRating` / `Cognitive` input provider 通过 `NormSubjectReader` 从 Actor Testee 装配 `NormSubjectSnapshot`；
- `InputRef.AsOf` 只来自 `Assessment.SubmittedAt`，缺失时年龄保持未知，不再回退当前时间；
- 所有可执行尝试只接受 `isn:v2:<64-hex>`（摘要包含年龄是否存在、年龄值和性别）；旧 ref 直接拒绝，`evaluation.force_retry` 也只允许 v2→v2 修订；
- Outcome 保存实际命中的 NormReference；specific 保存 cohort，generic 保存零值 cohort，同时保留 TableVersion 和 FormVariant。

**目标设计**

1. 从 Actor/Testee 权威事实装配性别和出生日期；
2. 按测评发生时间计算 AgeMonths，不按当前时间；
3. Lookup 与 Band 采用同一严格规则：缺少必需人口学资料时不得命中专用层；
4. 区分 generic fallback、missing subject、no matching norm 和 invalid norm；
5. 把实际命中的 NormReference 保存到 Outcome 证据。

**验收条件**

- 覆盖年龄下界、上界、性别、信息缺失和 generic fallback 的 golden tests；
- Import → Publish → Input materialization → Calculation → Outcome 的端到端测试；
- 结果能回答“使用了哪个 Norm 版本和哪个人群层”。

**实施证据**

- `domain/calculation/norm`：`ResolveNormScore`、`ResolutionError`、必需因子原子 `Projection.Apply`；
- `port/modelcatalog/payload/normruntime`：Behavioral/Cognitive 共享无损转换；
- `infra/evaluationinput`：`NormSubjectReader` 装配 + `AgeMonthsAt`；
- `application/evaluation/execute`：精确 FailureKind、v2-only identity、重试漂移拒绝和 force v2 修订审计；
- Characterization：Behavioral scoped Norm、Cognitive SPM StandardScore、negative no-Outcome 与 force correction 链路；
- `scripts/oneoff/audit_norm_usage`：只读列出含人口学 Lookup 的 Norm 版本和已发布模型引用。

**剩余风险**

- 不回填历史 InputSnapshotRef，也不自动重算既有 Outcome；不符合 current-only 契约的 Assessment/Outcome 在维护窗口备份后删除并重建；
- `isn:v2` 只保存摘要，不持久化出生日期或完整人口学明文；
- 非 T-score 的结论判定策略仍归 MC-R004/MC-R011，本项只保证 StandardScore 无损传递。

**证据**

- [Norm lookup](../../../internal/apiserver/domain/calculation/norm/tables.go)
- [Evaluation input contract](../../../internal/apiserver/port/evaluationinput/input.go)
- [常模资产与校准](./24-核心设计-常模资产与校准.md)
- 跨模块：Evaluation `EV-R005`

### MC-R003：在发布时强制 behavioral_rating 的常模语义

**状态**：已实现待验收

**验收结果（2026-07-22，G3 复验）**：**不通过（仓库内通过，待环境）**

**尚不能关闭的理由**：BRIEF-2 与 SPM Sensory 已通过真实 Norm Import、发布校验、不可变快照、exact input 和原生 `factor_norm` Outcome 链路；canonical inventory 与 cutover audit 也已通过。首轮生产 SPM Sensory smoke 暴露 runtime 错把旧 `Level` 当作 OutcomeCode；仓库内已修正并补 BRIEF-2/SPM Sensory 全链路契约，但修复版本尚未部署复验。

**处理方案与复验条件**：部署修复版本后，对 BRIEF-2、SPM Sensory 分别用新答卷执行 REST/gRPC/Collection/Worker smoke；确认 `level.code` 为 canonical OutcomeCode、NormReference 精确且 Artifact 成功持久化，两条链路均通过后关闭。

**P0 阶段 C/G5 进展（2026-07-22）**：BRIEF-2 与 SPM Sensory 已新增正向不可变发布快照、精确常模解析和 Outcome 留痕契约；behavioral 与身份活动文档已改为 canonical Algorithm、强制 Norm、DefinitionV2-only。生产 audit 已退出 0；首轮 SPM Sensory smoke 失败对应的 OutcomeCode 投影缺陷已在仓库修复，等待重新部署 smoke。

**当前事实（改造后）**

- `behavioralDecisionKind` 仅返回 `norm_lookup`；无 NormRef / 无 NormConclusion / 非恰好一个 Primary 均发布失败；
- 新发布拒绝 `behavioral_rating_default` 与空 Algorithm，必须使用 `brief2` 或 `spm_sensory`；
- `ValidateForPublish` 额外校验：NormRef factor 必须存在于常模表；NormConclusion factor 必须有对应 NormRef；
- `MaterializeSnapshot` 不再生成 payload，也不静默回退 default algorithm。

**已确认领域边界**

> `behavioral_rating` 必须使用 Norm 校准；如果业务只需要原始因子分和区间结果，应建模为 `scale`。

**目标设计**

发布门禁同时要求：

- 至少一个 NormRef；
- 至少一个 NormConclusion；
- 有且仅有明确 Primary 结果；
- NormRef Factor、Norm table Factor 和 Conclusion Factor 一致；
- DecisionKind 只能为 `norm_lookup`；
- 新发布不允许 `behavioral_rating_default` 代替真实 Algorithm。

**验收条件**

- 无 Norm、无 Primary、多 Primary、Factor 不一致均在发布时拒绝；
- BRIEF-2 与 SPM Sensory 正常发布/执行回归通过；
- 旧 default/空 Algorithm snapshot 不可执行，运营侧删除并重新发布 canonical DefinitionV2。

**实施证据**

- [`decision.go`](../../../internal/apiserver/domain/modelcatalog/assessmentmodel/decision.go)
- [`behavioral_rating_handler.go`](../../../internal/apiserver/application/modelcatalog/definition/behavioral_rating_handler.go)
- 测试：`TestDecisionKindForDefinitionRejectsBehavioralWithoutNormSemantics`、`TestBehavioralValidateForPublish*`

**二次复核新增证据**

- 已通过：`TestBehavioralValidateForPublishAcceptsBrief2`、`TestBehavioralMaterializationUsesDefinitionV2Identity`、`TestBehavioralRuntimeIsBuiltDirectlyFromDefinitionAndNorm`、`TestPublishedBehavioralRatingCatalogDecodesBrief2Snapshot`、BRIEF-2/SPM seed tests；
- 已通过：behavioral 正向 Import→Outcome、retained release 与本次 `TestBehavioralOutcomeCodeSurvivesDefinitionToInterpretation`；
- 活动文档已移除无 Norm/default fallback 当前态。

**剩余风险**

- production cutover audit 已退出 0；
- BRIEF-2 / SPM Sensory 仍需使用修复版本在部署环境完成新数据 smoke。

**证据**

- [behavioral decision](../../../internal/apiserver/domain/modelcatalog/assessmentmodel/decision.go)
- [behavioral definition handler](../../../internal/apiserver/application/modelcatalog/definition/behavioral_rating_handler.go)
- [behavioral_rating 专题](./40-模型类型/30-behavioral-rating-行为评定.md)

### MC-R004：统一 DecisionResult、ScoreRange 和 OutcomeCode

**状态**：已实现待验收

**验收结果（2026-07-22，G1 复验）**：**不通过（仓库内通过，待部署 smoke）**

**尚不能关闭的理由**：scale、typology、behavioral、cognitive 的 minimum、相邻边界、maximum、gap、overlap 已形成共享发布/计算契约；nearest-pattern 也不再补 `3/5` 或 `0.6` 隐式默认值，LevelRule 与 fallback threshold 均须显式且有限。严格关闭仍要求部署 smoke 证明 OutcomeCode 与 code/label 分离在真实链路一致。

**处理方案与复验条件**：

1. 在部署环境执行 scale、typology、BRIEF-2/SPM Sensory、cognitive SPM 边界 smoke；
2. 核验持久化 `level_code` 始终为短 OutcomeCode，展示文案只进入 `level_label`/冻结 Assets；
3. smoke 与 post-audit 全绿后关闭。

**G1 进展（2026-07-22）**：current ReportInput 解释路径只允许 `OutcomeCode → frozen InterpretationAssets`；四 family 边界矩阵已补。typology nearest-pattern 发布要求显式合法 LevelRule；存在 fallback 时 threshold 必须在 `(0,1]` 且不得孤立配置，runtime 不再补隐式默认。

**G5 复验进展（2026-07-22）**：生产 smoke 中 SNAP-IV 通过；SPM Sensory 的 legacy Level 投影、MBTI 的非 canonical Artifact kind，以及九型 trait profile 的冻结条件/模板错配分别被严格契约拒绝。确定性缺陷均已补契约并修复，未放宽类型选择决策的 outcomes/profiles 要求；仍需修复版本覆盖 scale、typology、behavioral、cognitive 的全 family smoke。

**当前事实（改造后）**

- 发布校验：Risk/Norm/Ability 的 ScoreRange 必须有 `OutcomeCode`，且引用顶层 Outcomes registry；
- 发布校验：默认半开 `[min,max)`；**仅末段**可设 `max_inclusive` 或 `unbounded_max`；拒绝重叠/空洞/非末段端点标志；
- 运行时匹配同源：`conclusion.MatchBounds` / `MatchScoreRangeOutcomes`（无显式端点标志时，旧 snapshot 末段 max-inclusive 兼容）；
- scale 运行时不再用硬编码绝对分阈值兜底风险等级（无规则命中 → `none`）；
- interpretation current path 只接受 OutcomeCode→冻结 Assets；不再 score-range rematch、生成 soft default 或用 RiskLevel 替代 OutcomeCode；
- `outcome.DecisionResult` + `DecisionResultFromExecution` 落地；cognitive `AbilityConclusion.Primary`（或 `Role=total`）提升为 `Execution.Level`；
- 临界值 golden：validate endpoint、ability boundary/unbounded、interpretation no-fallback、scale/norm Matches。

**仍待后续（非本批阻塞）**

- ScoreRangeOutcome 上 Title/Summary/Description 从判定事实彻底剥离到 InterpretationAssets；
- Import → Publish → Outcome 端到端各 family 全链路 golden（当前以单元/机制级 golden 为主）；
- 旧 snapshot 兼容窗口的可观测指标与下线计划。

**目标设计**

```text
Score fact
  -> DecisionSpec
  -> DecisionResult
       OutcomeCode
       LevelCode?
       PrimaryScore?
       Dimension facts
  -> InterpretationAssets
       title / summary / description / suggestions
```

新发布契约：

- 区间端点语义统一（半开 + 显式 `max_inclusive` / `unbounded_max`）；
- 区间不重叠；
- 对完整合法输入域不存在意外空洞；
- 所有可选结果引用顶层 OutcomeRegistry；
- 正式 scale 发布不使用默认风险阈值；
- cognitive 明确 primary ability dimension 并投影主 Outcome。

**验收条件**

- 每个 family 均有 minimum/boundary/maximum/gap/overlap golden cases；
- 持久化 code 与 label/prose 分离；
- 历史 OutcomeCode 不被新版本或文案修改改写；
- current-only 链路不存在 score/identity/payload fallback；历史不兼容 snapshot 通过维护窗口删除重建。

**实施证据**

- [`conclusion/scorerange.go`](../../../internal/apiserver/domain/modelcatalog/conclusion/scorerange.go)：共享端点契约
- [`definition/validate.go`](../../../internal/apiserver/domain/modelcatalog/definition/validate.go)：`endpoint.required` / `endpoint.non_last` / overlap / gap
- [`scoring/risk.go`](../../../internal/apiserver/domain/calculation/scoring/risk.go)：移除硬编码 default risk
- [`interpretation/scoring/scale_interpret.go`](../../../internal/apiserver/domain/interpretation/scoring/scale_interpret.go)：去掉末段 fallback
- [`outcome/decision_result.go`](../../../internal/apiserver/domain/evaluation/outcome/decision_result.go) + [`task_performance/projection.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/task_performance/projection.go)：DecisionResult / primary ability
- 测试：`TestValidateRequiresOutcomeCodeAndRejectsOverlapOrGap`、`TestApplyAbilityConclusionsBoundaryGolden`、`TestInterpretScaleFactorNoLastRuleFallback`、`TestDecisionResultFromExecution`

**证据**

- [Conclusion/Outcome](../../../internal/apiserver/domain/modelcatalog/conclusion/conclusion.go)
- [Definition validation](../../../internal/apiserver/domain/modelcatalog/definition/validate.go)
- [cognitive ability projection](../../../internal/apiserver/application/evaluation/registry/mechanisms/task_performance/projection.go)
- [结果判定、Outcome 与解释边界](./25-核心设计-结果判定、Outcome与解释边界.md)

### MC-R005：让标准 migration 成为 unified schema 的真正部署入口

**状态**：已实现待验收（本地非生产 Replica Set G4 与 production audit 已通过，待部署 smoke）

**验收结果（2026-07-22，G4/G5 复验）**：**不通过（G4/audit 通过，待部署 smoke）**

**尚不能关闭的理由**：本地授权的 Mongo 7 单节点 Replica Set 已完成一次真实无 SKIP G4，证明动态数据库、Replica Set fail-closed、fresh install、兼容/不兼容 down、索引 reconcile 幂等、dirty@13、模型/问卷 CAS 与 questionnaire/model/commit 三类 rollback 契约可执行，事务 runner 固定 primary + snapshot + majority。production cutover audit 已核验目标环境 required/forbidden indexes 与 migration state；仍缺修复版本部署 smoke，因此不能严格关闭。

**处理方案与复验条件**：保留本次 G4 无 SKIP 与 production audit 日志；部署修复版本后完成全 family smoke，并复跑只读 audit。不得用生产库跑动态集成测试。

**G4 进展（2026-07-22）**：首次真实执行发现并修复两处问题：问卷 command projection 漏投影 `revision`，导致两个 CAS 写者都冲突；fresh-install fixture 在验证 unified head+snapshot 共存后未恢复 legacy-compatible 数据，导致成功 down 场景误失败。修复后增加 command projection 契约和“不兼容 head+snapshot 必须拒绝 down”负向测试；G4 四包在本地 `rs0` 全部通过且无 SKIP。

授权环境复验命令固定为：

```bash
QS_SERVER_TEST_MONGO_REQUIRED=1 \
QS_SERVER_TEST_MONGO_URI='mongodb://...' \
QS_SERVER_TEST_MONGO_DB_PREFIX='qs_modelcatalog_contract' \
go test -tags=integration -count=1 \
  ./internal/pkg/migration \
  ./internal/apiserver/infra/mongo/modelcatalog \
  ./internal/apiserver/infra/mongo/questionnaire \
  ./internal/apiserver/container/modules/modelcatalog
```

**当前事实（改造后）**

- `000013_unified_modelcatalog_schema`：**仅** `createIndexes`（role-based partial unique + `assessment_norms.table_version` unique）；**不再**在 JSON 里 `dropIndexes`（已 cutover 环境 IndexNotFound → dirty）；
- 启动时：migration 成功后执行 `ReconcileUnifiedModelCatalogIndexes`（幂等 drop legacy + ensure）再 `VerifyUnifiedModelCatalogIndexes`；
- 契约/集成测试覆盖 create-only migration + Go reconcile。

**生产 dirty@13 恢复（当前阻塞）**

1. mongosh `qs`：`db.schema_migrations.find()` → 确认 `version:13, dirty:true`；
2. 对照 `RequiredUnifiedIndexNames` / `ForbiddenLegacyIndexNames` 检查三集合 `getIndexes()`；
3. **required 齐全且无 legacy** → 清 dirty 后重启：
   `db.schema_migrations.updateOne({}, {$set:{version:NumberLong(13), dirty:false}})`；
4. **缺 required**（drop 失败导致 create 未跑）→ 手工补齐或清 dirty 到 `version:12, dirty:false` 后部署含本修复的包再启（会重跑 13 + reconcile）；
5. **切勿**在未核验索引前盲目 `force 13`。

**仍待部署核验 / 后续**

- 生产/测试环境实际索引清单对照；
- Mongo read/write concern 配置固化；
- 维护窗口内目标环境索引、migration state 与部署 smoke 证据。

**目标设计**

1. 增加不回改历史文件的后续 migration；
2. 清理冲突旧索引，建立 role-based partial unique indexes；
3. 为 Norm table version 建立唯一约束；
4. 增加 fresh database migration test 和 index contract test；
5. 启动时验证关键索引，缺失时拒绝写流量或产生高优先级告警；
6. 使用非生产、可重置的 Replica Set 数据库验证联合发布。

**关闭前必须补的部署证据**

- 生产/测试环境实际索引清单；
- Mongo read/write concern 配置；
- fresh install 和已有数据升级两种路径；
- commit failure、model save failure、questionnaire save failure 的 rollback 证据。

**二次复核新增证据**

- `TestMongoUnifiedModelCatalogSchemaMigrationContract`、`TestRequiredUnifiedIndexNamesContract` 和普通 package tests 通过；
- 新增 integration harness 强制 `QS_SERVER_TEST_MONGO_URI`、动态数据库和 Replica Set `hello.setName`，缺失或 standalone 直接失败，不再静默 SKIP；
- `TestMongoReleasePairRollsBackEveryFailureBoundary`、模型/问卷 CAS、dirty@13、compatible down 和 incompatible down rejection 已在本地 `rs0` 真实执行通过；
- 本地单节点 Replica Set 证明事务与 schema 契约，不证明生产拓扑故障切换，也不替代目标环境 inventory 和部署 smoke。

**实施证据**

- [`000013_unified_modelcatalog_schema.up.json`](../../../internal/pkg/migration/migrations/mongodb/000013_unified_modelcatalog_schema.up.json)
- [`indexes.go`](../../../internal/pkg/mongodb/indexes.go)：`ReconcileUnifiedModelCatalogIndexes` / `VerifyUnifiedModelCatalogIndexes`
- [`bootstrap/database.go`](../../../internal/apiserver/bootstrap/database.go)：migration 后 reconcile + verify
- 测试：`unified_modelcatalog_schema_contract_test.go`、`unified_modelcatalog_schema_integration_test.go`

**证据**

- [Mongo migrations](../../../internal/pkg/migration/migrations/mongodb/)
- [startup index manager](../../../internal/pkg/mongodb/indexes.go)
- [数据存储与一致性](./26-核心设计-数据存储与一致性.md)
- [模型创建、编辑与联合发布](./30-关键链路-模型创建编辑与联合发布.md)

---

## 6. P1：将错误前移到发布阶段

### MC-R006：发布时冻结完整 RuntimeIdentity

**状态**：待补证据

**验收结果（2026-07-22，二次复核）**：**待目标环境判定（仓库内证据通过）**

**尚不能关闭的理由**：DefinitionV2 → RuntimeIdentity 冻结、Mongo mapper、InputSnapshot 传递、七条 exact descriptor 注册和 fail-closed 路由的代码与定向测试均通过；生产 cutover audit 也已在历史数据清理和 ModelCatalog repair 后退出 0。当前唯一未满足的严格关闭条件是修复版本的端到端 smoke。

**处理方案与复验条件**：部署修复版本后执行 REST/gRPC → InputSnapshot → Outcome 的全 family smoke，确认 RuntimeIdentity 精确一致；smoke 通过且复跑 audit 仍退出 0 时改为已关闭。

**二次复核新增证据**

- 通过：`TestResolveRuntimeIdentityFreezesCompatibleRoute`、`TestFrozenRuntimeIdentityDoesNotSilentFallback`、`TestDescriptorKeyFromRouteRequiresExactFrozenIdentity`、`TestDefaultRuntimeDescriptorRegistryCoversMaterializePaths`；
- 通过：published Mongo mapper、behavioral/cognitive catalog、ReportInput 与 Outcome codec 相关定向包；
- 环境审计已完成：历史数据清理和 ModelCatalog repair 后，只读 cutover audit 返回 `findings=0`、退出码 0。

**当前事实（改造后）**

- Publish：`MaterializeSnapshot` 由 DefinitionV2 解析并校验 AlgorithmFamily + DecisionKind，写入不可变 AssessmentSnapshot；
- `InputSnapshot.ModelSnapshot` 携带完整 AlgorithmFamily、DecisionKind 与 DefinitionV2；缺少任一项时 fail closed；
- `ModelRouteFromInput` / `DescriptorKeyFromRoute` 只接受冻结字段，并按 family+decision 精确命中；
- RuntimeResolver 禁止回退 Assessment ModelRef，compatibility resolver 与相关指标已删除；
- gRPC/BFF 只透传 ModelIdentity、AlgorithmFamily、DecisionKind，`payload_format` 原字段号保留为 reserved。

**切换边界**

- 旧 snapshot 不做 backfill；维护窗口删除并从 canonical DefinitionV2 重新发布；
- MySQL migration `000053` 删除 `evaluation_outcome.payload_format`，down 只恢复 nullable 列，不恢复历史值。

**目标设计**

```text
Publish
  Identity + Definition + CompatibilityMatrix
  -> ResolvedRuntimeIdentity
       AlgorithmFamily
       Algorithm
       DecisionKind
  -> freeze in AssessmentSnapshot

Evaluation
  -> validate frozen runtime identity
  -> resolve exact descriptor
  -> execute
  -> never choose another family/decision silently
```

**迁移边界**

- 新 snapshot 必须完整，缺失即发布失败；
- 旧 snapshot 不推导、不兼容，审计后删除重建；
- current-only 违规项必须由 cutover audit 可计数、可审计，并在切换前删除重建；
- 不得在历史 Assessment 上直接改写 Algorithm 并伪装成原始事实。

**验收条件**

- snapshot、Mongo PO、REST/gRPC 读模型、InputSnapshot 和 Outcome RuntimeIdentity 完整传递；
- 运行时不使用 identity/default/format fallback；
- 错误 family/decision 组合在发布或 descriptor 解析时拒绝；
- 旧 snapshot 由 cutover audit 列出并删除重建。

**实施证据**

- [`identity/resolve_runtime.go`](../../../internal/apiserver/domain/modelcatalog/identity/resolve_runtime.go)
- [`publication/publisher.go`](../../../internal/apiserver/application/modelcatalog/publication/publisher.go)
- [`routing/resolve.go`](../../../internal/apiserver/domain/evaluation/routing/resolve.go)
- [`execute/runtime_resolver.go`](../../../internal/apiserver/application/evaluation/execute/runtime_resolver.go)
- 测试：`resolve_runtime_test.go`、`runtime_identity_freeze_test.go`、`routing_test.go`

**证据**

- [published snapshot contract](../../../internal/apiserver/port/modelcatalog/catalog.go)
- [evaluation input contract](../../../internal/apiserver/port/evaluationinput/input.go)
- [runtime materialization matrix](../../../internal/apiserver/application/evaluation/runtime/materialization_specs.go)
- [模型身份、算法绑定与执行路由](./21-核心设计-模型身份、算法绑定与执行路由.md)

### MC-R007：建立统一发布完整性守卫

**状态**：已关闭

**验收结果（2026-07-22，G2 复验）**：**通过**

`Definition.Validate` 现使用真实 `Measure.Factors` 校验 `factor_scores.SourceRefs`：显式空列表仍表示隐藏全部因子；空字符串、重复引用、未知 Factor 和多个 `factor_scores` section 分别返回稳定 issue code。typology preview 与正式发布复用同一 `ValidateForPublish` 入口，冻结 InterpretationAssets 保留合法 SourceRefs。

**关闭说明**：Questionnaire type、复用规则和 CorrectOption 题型兼容继续归 MC-R009；它们不再阻断 ReportMap 完整性本身。

**当前事实（改造后）**

- `ValidateMeasureSpecParts`：Factors 为空返回 `measure.factors.required`；
- `DecisionKindForDefinition`：scale 要求 RiskConclusion，cognitive 要求 AbilityConclusion；typology/behavioral 保持强语义；
- scale/cognitive/behavioral `ValidateForPublish` 共用 `validateDefinitionQuestionnaireRefs`：对照已发布问卷版本校验 scoring question/option 与 SPM `QuestionCode`/`CorrectOptionCode`；
- 共享索引：`domain/modelcatalog/questionnaireref`；加载：`loadPublishedQuestionnaire`（typology 发布加载也复用）；
- 稳定 code：`question_mapping.question_not_found` / `option_not_found` / `binding.questionnaire.*`；
- 预览与发布仍共用同一 `ValidateForPublish` 契约。
- `factor_scores` 额外拒绝 `report_section.source_ref.required` / `duplicate` / `not_found` 与 `report_section.factor_scores.multiple`；ReportMap 未配置和显式空映射保持不同语义。

**验收后仍划给后续项**

- 问卷 **type / 复用 / Bind vs Publish 分策**、CorrectOption 题型兼容 → **MC-R009**；
- 独立 Validator 包拆分与 Strategy capability → **MC-R013/R014**；
- ReportMap 只引用真实 Outcome 事实的加强守卫；
- typology 贡献映射的存在性检查可逐步改调共享 Index（当前仍走 RuntimeSpec validator，语义已对齐）。

**已确认领域规则**

> Factor 必需，Norm 按模型类型决定是否必需，Decision 必需。只需收集答案或展示基础题分时，应使用独立 Questionnaire，不应发布不完整 AssessmentModel。

**目标设计**

- `DefinitionValidator`：内部引用、code、graph、scoring 结构；
- `QuestionnaireMeasureValidator`：QuestionCode、OptionCode、题型与已发布问卷版本；
- `ModelSemanticValidator`：Factor/Norm/Decision 的 family 完整性；
- `AlgorithmBindingValidator`：Algorithm、ExecutionSpec、Strategy capability；
- `ReportProfileValidator`：ReportMap 只引用真实产生的 Outcome 事实。

**验收条件**

- 四个 family 共享统一基线，再追加专属校验；
- “可编辑但不可执行”的配置不能发布；
- 校验错误使用稳定 code，运营端可定位到具体字段；
- 发布预览与真实执行复用同一契约。

**实施证据**

- [`factor/measure_validate.go`](../../../internal/apiserver/domain/modelcatalog/factor/measure_validate.go)
- [`assessmentmodel/decision.go`](../../../internal/apiserver/domain/modelcatalog/assessmentmodel/decision.go)
- [`questionnaireref/index.go`](../../../internal/apiserver/domain/modelcatalog/questionnaireref/index.go)
- [`definition/questionnaire_publish.go`](../../../internal/apiserver/application/modelcatalog/definition/questionnaire_publish.go)
- 测试：`measure_validate_test.go`、`decision_test.go`、`questionnaireref`、`scale_handler_test.go`、`cognitive_handler_test.go`、`behavioral_rating_handler_test.go`
### MC-R008：保护首次发布后的模型身份连续性

**状态**：已关闭

**验收结果（2026-07-22）**：**通过**

- `evolution.Policy` 始终读取 retained release history 的最早发布身份，不依赖可变 head status；
- `UpdateBasicInfo`、`BindQuestionnaire` 和两个发布入口均执行对应守卫，错误稳定映射为 409；
- 同 Questionnaire code 升版允许，Algorithm/code 变更拒绝，恢复 draft 或归档后重编辑也无法绕过 retained history。

**关闭说明**：本项不修改旧 snapshot 或已受理 Assessment，只保护首次发布后的身份连续性，验收条件已齐。

**当前事实（改造后）**

- `evolution.Policy` 以 retained release history（`ListPublishedReleaseHistory`，取最早一条）判定是否已首次发布，不依赖 head.status；
- 编辑守卫：`UpdateBasicInfo` → `GuardAlgorithmChange`；`BindQuestionnaire` → `GuardQuestionnaireCodeChange`（同 code 升 version 允许）；
- 发布最终守卫：`release.PublishRelease` / `publication.Publish` → `GuardPublishIdentity`；
- 冲突返回 `ErrConflict`（409）。

**验收条件**

- 编辑 API 和发布 API 都有最终守卫；
- 禁止修改时返回清晰领域错误；
- 恢复 draft、归档后重编辑等路径不能绕过规则；
- 旧 snapshot 和已受理 Assessment 不被修改。

**实施证据**

- [`evolution/policy.go`](../../../internal/apiserver/application/modelcatalog/evolution/policy.go)
- [`management/service.go`](../../../internal/apiserver/application/modelcatalog/management/service.go)
- [`release/service.go`](../../../internal/apiserver/application/modelcatalog/release/service.go)
- 测试：`evolution/policy_test.go`、`management/service_test.go`

**证据**

- [AssessmentModel editing](../../../internal/apiserver/domain/modelcatalog/assessmentmodel/model.go)
- [management service](../../../internal/apiserver/application/modelcatalog/management/service.go)
- [问卷绑定与发布版本](./22-核心设计-问卷绑定与发布版本.md)

### MC-R009：补齐各模型类型的 Questionnaire Binding Policy

**状态**：待业务决策（存在性基线已齐；type/唯一绑定/题型深兼容仍缺产品口径）

**验收结果（2026-07-22）**：**不通过**

**不通过理由**：registry 覆盖和 fail-closed 基线已实现，但各 family 可绑定 Questionnaire type、是否允许复用、CorrectOption 对题型的要求以及 Bind/Publish 分策尚未形成业务矩阵，无法验证“专属 policy 完整”。

**处理方案与复验条件**：先由产品/领域方确认四 family × Questionnaire type × 题型 × 复用 × 生命周期矩阵；再把矩阵固化为 policy、错误码和契约测试。业务决策与实现均完成前保持待业务决策。

**业务决策矩阵（G6，当前只记录待定项，不改变运行时代码）**：

| Family | 当前已执行基线 | Questionnaire type 待定 | 题型 / CorrectOption 待定 | 复用 / 独占待定 | Bind / Publish 时机待定 | 批准后的验收 |
| --- | --- | --- | --- | --- | --- | --- |
| scale | MedicalScale policy、精确已发布版本、唯一绑定 | 是否只允许医学量表 type | radio/checkbox/number 与 option score 能力白名单；无 CorrectOption | 同一问卷能否被多个 scale release 复用 | Bind 做结构预检；Publish 是否重跑全部题型契约 | policy 错误码、正反向矩阵、历史 retained release 不漂移 |
| typology | 已发布且非空；贡献映射校验 | survey / personality 专属 type 是否区分 | contribution 支持的题型与多选组合规则；无 CorrectOption | 是否允许同问卷承载多个 typology model | Bind 校验存在性还是完整贡献映射 | preview/publish 一致、贡献映射与正式 Outcome 一致 |
| behavioral_rating | 已发布且非空；question score / option score source 存在性 | clinical rating / observer form 是否单列 | reporter 题型、缺失答案与 option score 白名单；无 CorrectOption | reporter/form variant 是否要求独占 | FormVariant 在 Bind 还是 Publish 锁定 | BRIEF-2/SPM Sensory 各自契约与 retained form 回归 |
| cognitive | 已发布且非空；SPM Question/Option 存在性 | cognitive task 专属 type 是否必需 | CorrectOption 只允许单选还是允许其他任务题型；计时题语义 | item bank 是否允许跨 Algorithm/release 复用 | Bind 做题型预检；Publish 冻结答案键与时限 | 正确答案、题型、时限和 exact item set 全链路 |

所有“待定”必须由产品/领域方明确批准；未批准前保留现有最小基线，不推断兼容范围。

**当前事实（改造后）**

- Registry 覆盖四 family：Scale（MedicalScale + 唯一绑定 + BeforePublish 同步）、Typology / Behavioral / Cognitive（`PublishedQuestionnairePolicy`：已发布 + 非空题）；
- `Policies.Validate` / `BeforePublish`：**无匹配 policy 时拒绝**（不再默认放行）；
- Typology `BeforePublish` 与 Bind 对齐存在性校验；
- 题/选项存在性发布对照已由 **MC-R007** 覆盖；CorrectOption 题型兼容与问卷 type 产品规则仍待后续。

**目标设计（剩余）**

每个 family 明确：可绑定 Questionnaire type、题型能力、是否允许复用、Bind vs Publish 分策。

**验收条件**

- policy registry 覆盖所有可创建 family；
- 没有专属 policy 的 family 不再默认通过关键发布检查；
- 正确答案、题目贡献和因子 source 都核对精确问卷版本（存在性已齐；深兼容后续）。

**实施证据**

- [`binding/published_questionnaire_policy.go`](../../../internal/apiserver/application/modelcatalog/binding/published_questionnaire_policy.go)
- [`binding/model_policies.go`](../../../internal/apiserver/application/modelcatalog/binding/model_policies.go)
- [`lifecycle_wiring.go`](../../../internal/apiserver/container/modules/modelcatalog/lifecycle_wiring.go)
### MC-R010：建立跨 family 的统一执行输入不变式

**状态**：已关闭

**验收结果（2026-07-22，P0 阶段 B 复验）**：**通过**

公共守卫现在要求 Assessment ModelRef、冻结 ModelSnapshot 和 DescriptorKey 均存在，并精确比较 kind、subkind、algorithm、code、version。`factor_scoring`、`factor_norm`、`task_performance` 与 typology/SPM 路径均传递真实 descriptor key；required/mismatch 错误码稳定，错误上下文保留 model、questionnaire、answersheet 与 descriptor。

**关闭说明**：`inputinvariant` 单元矩阵、全部 mechanisms 测试以及更大范围 Evaluation/infra input 测试均通过；运行时不使用 latest、默认 Algorithm 或兼容身份回退。

**当前事实（改造后）**

- 新增 `inputinvariant.Validate`：统一校验 Assessment / AnswerSheet / QuestionnaireSnapshot / Model questionnaire binding 的 code+version，以及 Assessment.ModelRef ↔ Input.Model；
- 失败返回稳定 `inputinvariant.Error`（含 model ref、questionnaire ref、answersheet id、descriptor key），**不回退 latest**；
- Scale / Behavioral / Cognitive（非 SPM）经 `DefaultInputValidator` 委托公共守卫；Typology 与 Cognitive SPM 在 Calculator 入口显式调用。

**验收条件**

- 四个 family 进入 Calculation 前均经过同一公共守卫；
- 任意一个 code/version/algorithm 不一致都产生稳定 validation failure；
- 不回退 latest，不尝试“找一个能跑的版本”；
- 失败记录 model ref、questionnaire ref、answersheet id 和 descriptor key。

**实施证据**

- [`inputinvariant/validator.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/inputinvariant/validator.go)
- [`scoring/input_validator.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/scoring/input_validator.go)
- [`typology/pipeline_components.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/typology/pipeline_components.go)
- [`task_performance/pipeline_components.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/task_performance/pipeline_components.go)

### MC-R011：完成 Norm 跨资产兼容校验与 Raven 入口

**状态**：已关闭

**验收结果（2026-07-22，P0 阶段 C 复验）**：**通过**

behavioral 现已用生产发布物化、Published catalog、input provider 和原生 `factor_norm` pipeline 形成 Import→Snapshot→Input→Calculation→Outcome 正向链路；BRIEF-2/SPM Sensory 均证明 v2 存在时 retained v1 仍读取 `norm-v1` 并写入 Outcome。缺人口学资料的负向链路返回稳定 `norm_subject_missing` 且不产生部分 Outcome。cognitive SPM Import-to-Outcome、StandardScore 与兼容拒绝矩阵继续通过。

**关闭说明**：引用不存在 Factor、错误 Algorithm/FormVariant、不可产生 ScoreBasis 均在发布阶段拒绝；behavioral/cognitive 均有正向 Outcome 留痕，retained release 精确 NormTableVersion 回归已成立。

**当前事实（改造后）**

- `norm.ValidateImport` 受控扩展：允许 `cognitive + spm`（仍拒绝其他 Kind/Algorithm 组合）；
- `CheckNormCompatibility`：发布时校验 Model/Norm 的 Kind、Algorithm、FormVariant、Factor，以及 ScoreBasis 可产生性（含 SPM 禁 `t_score`、缺 `standard_score` 拒发）；
- Behavioral / Cognitive 发布路径经 `ValidateDefinitionV2ForPublishWithModel` 加载常模后执行兼容检查；
- SPM Outcome 命中常模后写入 `NormReference`（TableVersion/FormVariant/cohort）；
- Import→Outcome 回归：cognitive SPM 留痕 + behavioral ScoreBasis 门禁单测。

**验收条件**

- 引用不存在 Factor、错误 Algorithm、错误 FormVariant 或无法产生的 ScoreBasis 时拒绝发布；
- behavioral 和 cognitive 各有 Import-to-Outcome 回归测试；
- Outcome 保留实际 NormReference；
- 历史 release 继续精确读取旧 NormTableVersion。

**实施证据**

- [`norm/validate.go`](../../../internal/apiserver/domain/modelcatalog/norm/validate.go)
- [`definition/norm_compatibility.go`](../../../internal/apiserver/application/modelcatalog/definition/norm_compatibility.go)
- [`definition/definition_validation.go`](../../../internal/apiserver/application/modelcatalog/definition/definition_validation.go)
- [`task_performance/spm_calculator.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/task_performance/spm_calculator.go)
- 测试：`norm_compatibility_test.go`、`import_to_outcome_test.go`、`spm_calculator_test.go`

### MC-R012：强化发布幂等、dirty state 和并发冲突

**状态**：已实现待验收

**验收结果（2026-07-22，G4/G5 复验）**：**不通过（G4/audit 通过，待 REST smoke）**

**尚不能关闭的理由**：模型 draft 与 questionnaire head 的真实 repository 双写者 CAS 已在本地 `rs0` 通过，均满足仅一人成功、另一人 revision conflict、刷新后可重试；应用层 409 映射有直接测试。联合发布 pair 的 questionnaire/model/commit 三类真实 Mongo transaction 故障注入也全部通过且无半发布 pair。production audit 已核验目标环境 migration/index；仍需部署后 REST 冲突 smoke，故暂不关闭。

**处理方案与复验条件**：保留本次 G4 无 SKIP 与 production audit 日志；部署后核对真实 REST 冲突响应为 409，通过后关闭。

**当前事实（改造后）**

1. 幂等发布：`IsPublished` 短路前校验 active AssessmentSnapshot + published Questionnaire 完整配对；半边缺失返回 `ErrConflict`（`release.pair.incomplete` / `release.questionnaire.not_active`）；
2. dirty state：`HasUnpublishedChanges` 叠加问卷 head 的 `ReleaseState.HasUnpublishedChanges` / version 差；
3. Model draft：`ErrRevisionConflict` → `MapDraftWriteError` → 409；
4. Questionnaire head：`revision` CAS（无 upsert）；冲突 → 409；删除后恢复走 `Create`；
5. 图片上传：只存对象+返回 URL，**不再** fork published head（URL 写入 Definition 才算模型编辑）。

**验收条件**

- 重复发布只在完整 postcondition 成立时返回成功；
- 任一半边缺失/归档都返回可治理错误；
- 两个运营者并发编辑时，后提交者收到 conflict 并能刷新；
- 联合事务失败不留下半发布 pair。

**实施证据**

- [`release/service.go`](../../../internal/apiserver/application/modelcatalog/release/service.go)
- [`query/service.go`](../../../internal/apiserver/application/modelcatalog/query/service.go)
- [`domain/modelcatalog/errors.go`](../../../internal/apiserver/domain/modelcatalog/errors.go)
- [`infra/mongo/modelcatalog/draft_repo.go`](../../../internal/apiserver/infra/mongo/modelcatalog/draft_repo.go)
- [`infra/mongo/questionnaire/repo.go`](../../../internal/apiserver/infra/mongo/questionnaire/repo.go)
- [`assets/service.go`](../../../internal/apiserver/application/modelcatalog/assets/service.go)

---

## 7. P2：模块边界与扩展性

### MC-R013：拆分按 Kind 膨胀的 Definition handler

**状态**：已关闭（批次 1–4 全部完成）

**验收结果（2026-07-22）**：**通过**

- 四 family 通过 `PublicationComposer` 组合共享发布校验，Registry 按完整 `AlgorithmBinding` 解析；
- `RuntimeMaterializer` 只证明 DefinitionV2 能物化 family 临时运行 DTO，不持久化 compatibility bytes；Typology preview 独立于发布编排；
- 通用 validator 无 Mongo/HTTP 依赖，新增 Algorithm 只需扩展对应 binding/handler/descriptor，不要求改动无关 family。

**关闭说明**：原文中的 `CompatibilityPayloadProjector` 已随 current-only 切换失效，本次将证据名称校正为当前的 `RuntimeMaterializer`。

**当前事实（改造后 · 批次 1–4）**

- 抽出无外部依赖校验：`ValidateAlgorithmBinding`、`ValidateBehavioralSemantic`、`AppendDecisionKindIssues`、`ValidateDefinitionForPublish`、`ValidateQuestionnaireMeasure`（`validators.go`）；
- `PublicationComposer`（`ComposePublishValidation`）：四 family handler 组合配置，不再各自复制 Kind 校验树；
- `ValidateAlgorithmBinding` 按 Algorithm + Family 分派，不再 `switch Kind`；
- `RuntimeMaterializer` / `TypologyPreviewService` 与发布编排分离，前者只产生临时运行 DTO/冻结身份，不产生 compatibility payload；
- Registry 按完整 `AlgorithmBinding` 解析；`CompatibleAlgorithmBinding` 拒绝跨 Kind 算法误匹配；
- Norm 兼容：`effectiveNormAlgorithm` / SPM score_basis 守卫改走 Family/Algorithm，不再 Kind 分支。

**验收条件**

- 新增 Algorithm 不需修改不相关 Kind 的 handler；
- 发布错误 code 和行为不因结构拆分而改变；
- 通用 validator 无 Mongo/HTTP 依赖；
- payload projector / preview 不承担发布编排；
- publication 对外仍保持一个原子用例。

**实施证据**

- [`publication_composer.go`](../../../internal/apiserver/application/modelcatalog/definition/publication_composer.go)
- [`validators.go`](../../../internal/apiserver/application/modelcatalog/definition/validators.go)
- [`payload_projector.go`](../../../internal/apiserver/application/modelcatalog/definition/payload_projector.go)（当前 `RuntimeMaterializer`）
- [`typology_preview.go`](../../../internal/apiserver/application/modelcatalog/definition/typology_preview.go)
- [`algorithm_binding.go`](../../../internal/apiserver/application/modelcatalog/definition/algorithm_binding.go) / [`handler.go`](../../../internal/apiserver/application/modelcatalog/definition/handler.go)
- [`identity.CompatibleAlgorithmBinding`](../../../internal/apiserver/domain/modelcatalog/identity/identity.go)
- 测试：`validators_test.go`、`handler_test.go`、`compatible_binding_test.go`、`payload_projector_test.go`、`norm_compatibility_test.go`

### MC-R014：让 Strategy capability 与运行时执行能力共享一个事实源

**状态**：已关闭（批次 1–5：catalog + 四 path 发布 + Graph + Role 可执行 Scoring + missing-answer + ops strategies + OpenAPI/ruleengine 对齐）

**验收结果（2026-07-22）**：**通过**

- 四条 execution path 的发布能力、missing-answer policy 和运营 options 共同读取 `domain/calculation/capability`；
- FactorGraph、重复 Scoring、Role 可执行性和 strategy path 均有稳定错误码及直接测试；
- OpenAPI/Go enum/ruleengine 的声明集合有契约测试，Calculation 包没有反向依赖 ModelCatalog。

**关闭说明**：本项只关闭 capability/validation 单一事实源；Scale Measure 字段投影完整性由 MC-R015 独立验收。

**当前事实（改造后 · 批次 1–5）**

- `domain/calculation/capability`：ExecutionPath × Usage 真实策略矩阵；四 family 发布接入；
- 同 Factor 仅一条 Scoring（`scoring.factor.duplicate`）；
- FactorGraph 结构校验：roots 有效、单 parent、边去重、自环禁止、edges 可达、Graph↔factor-source children 一致；
- 扁平 scale（仅 Roots、无 Edges）仍合法；有 Edges 时必须声明 Roots；
- FactorRole 强制可执行 Measure Scoring（`factor.scoring.executable_required`）；cognitive 的 `total`/`ability_domain` 例外（走 SPM ExecutionSpec）；
- `MissingAnswerPolicyFor(path, usage)`：question_aggregation=skip，typology leaf/composite 与 composite projection=fail；与 scale collect / typology scoreLeaf 行为对齐；
- 运营 `OptionsResult.ScoringStrategies` 按 path 从 catalog 子集暴露（`AuthoringStrategyCodes`）；
- OpenAPI / `factor.ScoringStrategy` / wire enums 收敛为 `DeclaredAuthoringStrategyCodes` 并集（去掉 max/min，补 none/lookup/custom）；path 子集仍由发布校验强制；
- `ScaleFactorScorer` 经 `capability.Canonical(scale, question_aggregation)` 路由；公开 registry 仅 sum/avg/cnt；legacy max/min/first/last/weighted_sum 仅保留为测试 helper。

**稳定错误 code（Graph + Scoring）**

- `factor_graph.roots.required` / `root.not_found` / `root.duplicate` / `root.is_child` / `root.invalid`
- `factor.parent_code.multiple` / `self` / `cycle` / `not_found`
- `factor_graph.edge.duplicate` / `edge.invalid`
- `factor_graph.node.unreachable`
- `factor_graph.children.mismatch`
- `strategy.unsupported_for_path` / `scoring.factor.duplicate` / `factor.scoring.executable_required`

**目标设计**

建立由 runtime/Calculation 注册的 capability catalog，供运营选项、编辑提示、发布校验、preview、descriptor resolver、contract tests 同源消费。

**验收条件**

- 四 path 发布与 catalog 支持集同源；
- 未支持 strategy / 非法 Graph / 缺可执行 Scoring 在发布期失败，code 稳定；
- 同一 Factor 仅一条 Scoring；
- Calculation 不依赖 modelcatalog；
- ops `/options` 的 scoring_strategies 与 catalog path 子集同源；
- OpenAPI ScoringStrategy 枚举 = `DeclaredAuthoringStrategyCodes`；
- 公开 `ScaleFactorScorer` 与 scale question_aggregation catalog 同源（含 avg↔average、cnt↔count 别名）。

**实施证据**

- [`capability/catalog.go`](../../../internal/apiserver/domain/calculation/capability/catalog.go)
- [`capability/policy.go`](../../../internal/apiserver/domain/calculation/capability/policy.go)（`DeclaredAuthoringStrategyCodes`）
- [`factor/strategy_capability.go`](../../../internal/apiserver/domain/modelcatalog/factor/strategy_capability.go)
- [`factor/executable_scoring.go`](../../../internal/apiserver/domain/modelcatalog/factor/executable_scoring.go)
- [`factor/scoring.go`](../../../internal/apiserver/domain/modelcatalog/factor/scoring.go)
- [`factor/measure_validate.go`](../../../internal/apiserver/domain/modelcatalog/factor/measure_validate.go)
- [`query/options.go`](../../../internal/apiserver/application/modelcatalog/query/options.go)
- [`response/openapi_wire.go`](../../../internal/apiserver/transport/rest/response/openapi_wire.go)
- [`infra/ruleengine/scoring.go`](../../../internal/apiserver/infra/ruleengine/scoring.go)
- 测试：`capability/*_test.go`、`strategy_capability_test.go`、`executable_scoring_test.go`、`scoring_declaration_test.go`、`measure_validate_test.go`、`options_test.go`、`ruleengine/scoring_test.go`
- 契约产物：`make docs-rest` 已重生 `api/rest/apiserver.yaml` / swagger
### MC-R015：消除 ScaleSnapshot 的扁平表达损失

**状态**：已关闭（长期中性 DTO 收敛继续由 EV-R013 承接）

**验收结果（2026-07-22，P0 阶段 A 复验）**：**通过**

Scale 发布现在以稳定错误码 `factor.scoring.constant.unsupported` 拒绝非零 `Scoring.Constant`；`FactorGraph.SortOrders` 在发布时拒绝未知、非正数或重复值，并在 DefinitionV2 → Calculation → Outcome 投影中稳定保留。已配置项按升序，未配置或同序项保持 Definition 原始顺序。

**关闭说明**：canonical Measure、composite、option override、Constant 拒绝与 SortOrders 投影测试均通过；不存在已允许发布但在 factor_scoring 消费时被静默丢弃的已知字段。

**当前事实（改造后）**

- `ScaleSnapshot` 只是由 DefinitionV2 临时物化的 runtime DTO，不持久化或暴露为发布 payload；
- `ScaleSnapshotFromDefinition` 深拷贝 Measure，并投影临时 flat Factors；反向 `DefinitionFromScaleSnapshot` 已删除；
- `modelFromSnapshot` 只接受 canonical Measure，投影为 `calcscoring.Factor` 的 `Contributions` / `ChildCodes` / `ChildWeights`；
- `calcscoring.Evaluator`：先算 leaf（sign/weight/option_override），再按依赖序算 composite（sum/avg/weighted_sum）；
- 缺少 DefinitionV2/Measure 时不从 flat payload 重建。

**目标选择**

| 方案 | 状态 |
| --- | --- |
| DefinitionV2 → 临时 ScaleSnapshot/Calculation DTO | **已落地** |
| runtime DTO → DefinitionV2 反向导入 | **已删除** |

**验收条件**

- DefinitionV2 中发布允许的 Measure 语义均能被 factor_scoring 消费；
- 不再存在“可保存/可发布，但投影后丢失”的字段（canonical Measure 路径）；
- 缺少 canonical Measure 的输入 fail closed；
- 代表性 DefinitionV2 量表回归不变。

**实施证据**

- [`payload/scale/payload.go`](../../../internal/apiserver/port/modelcatalog/payload/scale/payload.go)
- [`payload/scale/definition.go`](../../../internal/apiserver/port/modelcatalog/payload/scale/definition.go)
- [`mechanisms/scoring/converters.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/scoring/converters.go)
- [`calculation/scoring/collect.go`](../../../internal/apiserver/domain/calculation/scoring/collect.go) / [`pipeline.go`](../../../internal/apiserver/domain/calculation/scoring/pipeline.go)
- 测试：`measure_semantics_test.go`、`converters_measure_test.go`

### MC-R016：逻辑与物理分离 DecisionSpec 和 InterpretationAssets

**状态**：已关闭

**验收结果（2026-07-22，G2 复验）**：**通过**

DecisionSpec、InterpretationAssets、ReportInput schema 3 与 OutcomeCode 主路径均已落地；current Interpretation 不再按 raw score 重匹配或生成 `soft_default`。G2 又补齐 ReportMap SourceRefs 深校验及 preview/publish 一致性，关闭了本项最后一个发布完整性阻断。

**关闭说明**：缺 Assets、OutcomeCode 或 presentation 均返回可分类 fail-closed 错误；文案变化不改写旧 Outcome 事实。新的 Questionnaire 深兼容决策继续归 MC-R009。

**当前事实（改造后）**

- 新增 `domain/modelcatalog/decision` / `interpretationassets` 及 `definition.DecisionSpecFrom` / `InterpretationAssetsFrom` 投影；
- 表征 + ReportMap↔DecisionKind 发布校验（批次 1）；
- Norm/Cognitive/Scale 判定路径仅写 OutcomeCode；Interpretation 优先 OutcomeCode→Assets（批次 2）；
- `Definition.DecisionSpec` / `Definition.InterpretationAssets` 由 `MaterializeLayers` 在 Save/Publish 时形成并随 DefinitionV2 冻结；
- Evaluation commit 只写 ReportInput schema 3，冻结 assets、ModelRef、FactorCatalog 与 family 必需的 Typology/Norming 信息；不写完整 ModelPayload；
- Interpretation 只解码 current ReportInput，并严格按 OutcomeCode 读取冻结 Assets；payload-only/旧 schema decoder、score rematch、soft default 与 upgrade/backfill 已删除。

**目标设计**

```text
DefinitionV2
├── Measure
├── Calibration
├── DecisionSpec
├── OutcomeRegistry
└── InterpretationAssets
    ├── OutcomePresentation
    ├── TypeProfiles
    └── ReportSpec
```

第一阶段仍在一个模型 release 中联合发布，但使用独立类型、validator 和 package。

**验收条件**

- Calculation/Evaluation 不消费长文案来做判定；
- Interpretation 不重新计分或反推 OutcomeCode；
- 修改解释文案需要新 release，但不改变旧 Outcome 事实；
- 发布时完整校验 ReportMap source/adapter/template 与 DecisionKind 兼容性。

**实施证据（批次 1–4）**

- [`decision/spec.go`](../../../internal/apiserver/domain/modelcatalog/decision/spec.go)
- [`interpretationassets/assets.go`](../../../internal/apiserver/domain/modelcatalog/interpretationassets/assets.go)
- [`definition/definition.go`](../../../internal/apiserver/domain/modelcatalog/definition/definition.go)（DecisionSpec / InterpretationAssets 字段）
- [`definition/layers.go`](../../../internal/apiserver/domain/modelcatalog/definition/layers.go)（MaterializeLayers / Resolved*）
- [`definition/validate.go`](../../../internal/apiserver/domain/modelcatalog/definition/validate.go)（ReportMap↔DecisionKind）
- [`infra/mongo/modelcatalog/definition_layers_po.go`](../../../internal/apiserver/infra/mongo/modelcatalog/definition_layers_po.go)
- [`port/evaluationinput/report_input.go`](../../../internal/apiserver/port/evaluationinput/report_input.go)（ReportInput schema 3）
- [`calculation/norm/projection.go`](../../../internal/apiserver/domain/calculation/norm/projection.go)
- [`interpretation/scoring/scale_interpret.go`](../../../internal/apiserver/domain/interpretation/scoring/scale_interpret.go)
- 测试：`layers_test.go`、`definition_layers_po_test.go`、`report_input_test.go`、`outcome_presentation_test.go`

### MC-R017：治理 DefinitionV2、compatibility payload 与 ReportInput 多份投影

**状态**：已实现待验收（production audit 已通过，等待修复版本 smoke）

**验收结果（2026-07-22，G5 前置复验）**：**不通过（仓库内通过，待环境）**

**尚不能关闭的理由**：PO、ReportInput schema 3、Outcome schema 2、fail-closed decoder、DefinitionV2-only 文档和架构测试均已对齐；历史数据清理与 ModelCatalog repair 后，生产 `verify_definition_v2_cutover` 已退出 0。当前只缺修复版本部署 smoke。

**处理方案与复验条件**：部署修复版本，以新答卷验证新 Outcome schema 2、ReportInput schema 3 且文档不含 payload/payload_format/definition_payload；smoke 全绿并复跑 audit 仍退出 0 后关闭。

**P0 阶段 E/G5 进展（2026-07-22）**：cutover audit 已解析并校验 Outcome schema 2、ReportInput schema 3 与 normative Outcome 的 NormReference，同时检查 Mongo migration dirty 状态及 unified ModelCatalog required/legacy indexes；生产 post-repair audit 已退出 0。活动发布文档已改为 DefinitionV2-only 与临时 runtime DTO。尚缺修复版本 smoke，故不关闭。

**当前事实（改造后）**

- `AssessmentModel`、draft PO 与 published PO 只保存 `DefinitionV2`；`DefinitionPayload`、payload bytes、payload format、projection hash/replay 均已删除；
- family handler 的 `MaterializeSnapshot` 从 DefinitionV2 校验并产生 Kind、SubKind、Algorithm、AlgorithmFamily、DecisionKind、Version，不序列化运行时 payload；
- published snapshot 冻结 DefinitionV2、精确运行时身份、Questionnaire 引用与 Definition 内容摘要；四类 provider 都从 DefinitionV2 物化临时 Calculation DTO；
- `ReportInput` 只接受 schema 3，并冻结 InterpretationAssets、ModelRef、FactorCatalog、TypologyRouting/Source 或 Norming；缺失冻结材料时提交失败；
- Outcome schema 只接受 current schema 2；schema 0/1 decoder、compat metrics、current-catalog/scale fallback 和 backfill 工具均已删除；
- `verify_definition_v2_cutover` 是只读切换审计入口，不执行删除或回填；当前覆盖 Outcome schema 2、ReportInput schema 3、normative NormReference、Mongo dirty 状态和 required/legacy indexes。

**验收条件**

- 新 snapshot/Mongo 文档不含 payload、payload_format、definition_payload；
- 缺少 DefinitionV2、AlgorithmFamily 或 DecisionKind 时 fail closed；
- ReportInput 非 schema 3、Outcome 非 schema 2 均拒绝；
- 运行时临时 DTO 只能由冻结 DefinitionV2 物化，不能从历史 payload 反解。

**实施证据**

- [`definition/handler.go`](../../../internal/apiserver/application/modelcatalog/definition/handler.go)（`MaterializeSnapshot`）
- [`assessmentmodel/model.go`](../../../internal/apiserver/domain/modelcatalog/assessmentmodel/model.go)（DefinitionV2-only 聚合）
- [`infra/mongo/modelcatalog/definition_po.go`](../../../internal/apiserver/infra/mongo/modelcatalog/definition_po.go)
- [`port/evaluationinput/report_input.go`](../../../internal/apiserver/port/evaluationinput/report_input.go)（current schema 3）
- [`port/evaluationfact/codec/codec.go`](../../../internal/apiserver/port/evaluationfact/codec/codec.go)（current Outcome schema 2）
- [`verify_definition_v2_cutover`](../../../scripts/oneoff/verify_definition_v2_cutover/main.go)

**二次复核新增证据**

- 通过：`TestMarshalReportInputEmitsOnlyCurrentSchemaWithoutPayload`、`TestReportInputRejectsNonCurrentSchemas`、`TestDecodeExecutionRejectsNonCurrentSchemas`、DefinitionV2-only architecture ratchet；
- 已补：`auditMySQL` 校验 Outcome schema 2、解析 ReportInput schema 3 并核对 normative NormReference；Mongo 审计覆盖 migration dirty 与 required/legacy indexes；
- 已补：活动发布链路统一描述 DefinitionV2-only snapshot 与临时 runtime DTO。

### MC-R018：有计划地退出历史身份与运行时 fallback

**状态**：已实现待验收（canonical inventory 已通过，等待修复版本 smoke）

**验收结果（2026-07-22，G5 前置复验）**：**不通过（仓库内通过，待环境）**

**尚不能关闭的理由**：canonical Algorithm、registry 数量 7、精确 `(AlgorithmFamily, DecisionKind)` route、reserved proto 字段、源码 ratchet 与活动文档均已对齐 current-only，生产 canonical inventory 也已通过。生产 smoke 暴露 MBTI Artifact kind 不规范，以及九型 `trait_profile` 冻结条件和模板路由错配；仓库内均已修正并补契约，修复版本尚未执行 REST/gRPC/Collection/Worker smoke。

**处理方案与复验条件**：先用 `repair_enneagram_report_template` 的 dry-run/apply 精确修正 `ENNEAGRAM_45@v16` 模板并复跑 audit；该工具不要求再次删除已产生的 smoke 数据。部署修复版本后验证 API/proto 不暴露 `payload_format`、七条合法 route 可执行且非法组合终态失败，并以 MBTI、SBTI、Big Five、九型人格新数据完成 REST/gRPC/Collection/Worker smoke；复跑 audit 仍退出 0 后关闭。

**P0 阶段 E/G5 进展（2026-07-22）**：身份路由和 behavioral 活动文档已移除 `behavioral_rating_default` 与 runtime payload 当前态；oneoff README 将历史脚本明确列入“已退役”。canonical inventory 已通过；剩余复验仅为修复版本 API/gRPC/Worker 消费方 smoke。

**当前事实（改造后）**

- RuntimeIdentity、ModelRoute、Outcome、EvaluationFact、worker/gRPC/REST/collection DTO 已删除 `PayloadFormat`；proto 原字段号保留为 `reserved`；
- Descriptor Registry 只按 `(AlgorithmFamily, DecisionKind)` 精确注册七条 route，不再按 format 或 family fallback；
- canonical Algorithm 仅保留 `personality_typology`、`brief2`、`spm_sensory`、`spm`；retained alias、空 Algorithm invent、default mapping 与 compatibility resolver 已删除；
- Behavioral/Cognitive/Typology provider 只接受精确 canonical Algorithm；缺少冻结身份或 DefinitionV2 时终态失败；
- ruleset v1 envelope、旧 Mongo ruleset repository、旧 payload decoder、退役门禁、backfill/soft-delete 工具源码已删除；oneoff README 将历史入口明确列为已退役。

**验收条件**

- 七个 family+decision 组合可精确命中，任一字段缺失或组合错误均不能路由；
- API/proto/OpenAPI 不再暴露 payload_format；
- 源码架构 ratchet 阻止 CompatibilityResolver、DefinitionPayload、PayloadFormat 与旧 decoder 回归；
- cutover audit 只报告 canonical contract 违规项，不提供自动修复。

**实施证据**

- [`runtime/descriptor/registry.go`](../../../internal/apiserver/application/evaluation/runtime/descriptor/registry.go)
- [`runtime_resolver.go`](../../../internal/apiserver/application/evaluation/execute/runtime_resolver.go)
- [`identity/identity.go`](../../../internal/apiserver/domain/modelcatalog/identity/identity.go)
- [`architecture_test.go`](../../../internal/apiserver/application/architecture_test.go)
- [`evaluation.proto`](../../../api/grpc/proto/evaluation/evaluation.proto)
- [`assessment_model_catalog.proto`](../../../api/grpc/proto/assessmentmodel/assessment_model_catalog.proto)

**二次复核新增证据**

- 通过：runtime registry `Len()==7`、exact resolve/fail-closed、canonical identity、proto `reserved "payload_format"`、DefinitionV2-only architecture ratchet；
- 文档：身份路由、behavioral 专题/README 与 oneoff README 已对齐 current-only；
- 环境缺口：canonical inventory 已通过；修复版本部署后二方/三方消费链 smoke 尚未取得。

### MC-R019：将纯计算从 Evaluation application 收敛到 Calculation

**状态**：已关闭（批次 1–4 + EV-R013；复扫确认无值得再开批次的纯算遗漏）

**验收结果（2026-07-22）**：**通过**

- SPM、AbilityConclusion、PoleMaxDeviation、similarity/match_percent 换算均由 `domain/calculation` 纯函数拥有；
- Evaluation 只保留输入适配、descriptor 编排和 Outcome 组装，`mechanisms` 复扫未发现新的 `math.*` 或百分比换算主路径；
- calculation、typology、task_performance、scoring、norming 与 descriptor 测试全部通过。

**关闭说明**：family adapter 的 DTO 投影和运行缺省仍属于 Evaluation 正常职责，不作为纯计算下沉的剩余必做项。

**当前事实**

- scale scoring、typology classification、常模数学已在 `domain/calculation`；
- **批次 1**：Raven SPM → [`ScoreSPM`](../../../internal/apiserver/domain/calculation/task_performance/score.go)；
- **批次 2**：AbilityConclusion → [`ApplyAbilityConclusions`](../../../internal/apiserver/domain/calculation/task_performance/ability.go)；
- **批次 3**：pole `MaxDeviation` → [`PoleMaxDeviation`](../../../internal/apiserver/domain/calculation/classification/pole_deviation.go)；删除 `ContextWithExecutionInput` / `ExecutionInputFromContext`；
- **批次 4**：similarity ↔ match_percent → [`DualScaleFromScore`](../../../internal/apiserver/domain/calculation/classification/similarity_scale.go) / [`MatchPercentPrefer`](../../../internal/apiserver/domain/calculation/classification/similarity_scale.go)；
- **EV-R013**：`Assemble(ExecutionInput)` + 四 family 读 `calcInput.Execution`；
- **复扫**：`mechanisms` 内无残留 `math.*` / `×÷100` 纯算；typology fallback threshold 已改为发布期显式配置，runtime 不再补 `0.6`。

**目标边界**

```text
ModelCatalog
  定义模型、算法绑定与参数

Calculation
  纯函数计分、校准与分类

Evaluation
  物化输入、选择算法、编排执行、保存事实
```

**验收关注点**

- family adapter（typology graph/decision 投影、scoring converters）留在 Evaluation，属正常边界，不作为本项阻断；
- 回归：typology / task_performance / scoring / norming / characterization 相关测试。

**实施原则**

- 先建 characterization/golden tests；
- 仅移动无 I/O、无应用状态的计算；
- 保留 InputAdapter 和 OutcomeAssembler 在 Evaluation；
- 不在这一步同时改变 Algorithm 标识或结果 schema。

**实施证据（批次 1–4 + EV-R013）**

- [`domain/calculation/task_performance/`](../../../internal/apiserver/domain/calculation/task_performance/)
- [`domain/calculation/classification/pole_deviation.go`](../../../internal/apiserver/domain/calculation/classification/pole_deviation.go)
- [`domain/calculation/classification/similarity_scale.go`](../../../internal/apiserver/domain/calculation/classification/similarity_scale.go)
- [`mechanisms/task_performance/`](../../../internal/apiserver/application/evaluation/registry/mechanisms/task_performance/)
- [`typology/runtime/configured/detail_assemblers.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/typology/runtime/configured/detail_assemblers.go)
- [`typology/outcome_assembler.go`](../../../internal/apiserver/application/evaluation/registry/mechanisms/typology/outcome_assembler.go)
- [`runtime/descriptor/contracts.go`](../../../internal/apiserver/application/evaluation/runtime/descriptor/contracts.go)
- [`execute/descriptor_executor.go`](../../../internal/apiserver/application/evaluation/execute/descriptor_executor.go)
---

## 8. P3：需要真实业务驱动的扩展

### MC-R020：Norm 资产治理与更广认知任务

**状态**：待业务决策（首切片 A：Norm usage 只读审计已通过；lifecycle / 多常模 / 新认知任务仍待业务决策）

**验收结果（2026-07-22）**：**不通过（切片 A 通过，整体未关闭）**

**不通过理由**：`audit_norm_usage` 已能只读输出 usage/dangling/unreferenced/multi_version，满足低频反向审计；但 Norm lifecycle、多 NormTableVersion、scale+factor_norm、服务端认知时限和更多 cognitive Algorithm 均没有已确认业务语义，本项整体仍是能力候选集合。

**处理方案与复验条件**：保留切片 A 的通过记录，不把引用列表反写 Norm 聚合；只有真实需求触发并完成相应产品决策时，才把候选能力拆成独立编号、定义验收条件并实施。当前整体保持待业务决策。

以下决策包只定义触发门槛与评审输入，不代表已经选择方案：

| 候选能力 | 真实需求触发 | 待批准状态机 / 选择规则 | 权限与审计 | 数据迁移 | 批准后的验收条件 |
| --- | --- | --- | --- | --- | --- |
| Norm draft/publish/archive/retired 生命周期 | 运营需要审核、回滚或禁用常模，Import 即 available 已不足 | `draft → published → archived/retired`；是否允许恢复、何时禁止新引用待定 | Import、Publish、Archive/Retire 分权；记录操作者、理由和引用数量 | 现有 Norm 默认映射到哪个状态、被 Assessment 引用时能否退役待定 | 状态转换矩阵、并发保护、retained release 仍精确读取、回滚 runbook |
| Norm usage 产品化查询 | 低频脚本不足，运营需要在线阻断删除/退役 | usage/dangling/unreferenced/multi_version 的分页与一致性口径 | 只读权限、导出脱敏、查询审计 | 无反向列表写回；必要时仅建派生索引 | API 与 `audit_norm_usage` 对账，删除/退役前置守卫 |
| 多 NormTableVersion / CalibrationProfile | 同一 release 的不同 Factor 确实需要不同常模 | Factor→NormRef 唯一性、profile 选择优先级与 cohort fallback 待定 | profile 编辑/发布权限与变更审计 | 单表 release 如何显式升级成 profile，禁止自动猜测 | 多 Factor 精确版本、retained release、gap/conflict 与 Outcome NormReference 矩阵 |
| scale + factor_norm | 真实模型业务语义仍是 scale 但必须常模化 | 新 Algorithm/Family 还是 scale capability 扩展待定；不得复用默认路由 | 模型发布与 Norm 发布联动权限 | 既有 scale 不自动升级 | exact identity、发布门禁、四方输入守卫、ReportInput 与解释全链路 |
| 服务端强制认知任务时限 | 超时影响结果有效性或计分 | start/pause/resume/offline/re-entry/timeout 状态与服务端时钟权威待定 | 开始/强制结束/人工豁免审计 | 既有 SPM Assessment 是否保持无服务端强制待定 | 时钟漂移、断网重连、幂等结束、超时 Outcome/FailureKind golden |
| 更多 cognitive Algorithm | 第二个真实任务需要反应时、遗漏/误报、自适应或新详情结构 | 每个任务独立 Algorithm；输入、计分、时限、终止与 Outcome schema 逐项定义 | 题库、答案键、算法发布权限 | current SPM 不改 identity，不用万能兼容 decoder | capability catalog、发布校验、exact descriptor、Calculation 与部署 smoke |

**已落地（首切片 A）**

- [`scripts/oneoff/audit_norm_usage/`](../../../scripts/oneoff/audit_norm_usage/)：以 published AssessmentSnapshot NormRef 为事实源，输出 usage / dangling / unreferenced / multi_version。

这一组其余项不作为近期技术清理，只保留决策门槛。

---

## 9. 建议的实施顺序

不建议按文件顺序或 package 顺序实施，而应按风险与依赖关系分批。

### 批次 0：建立现状证据

先进行只读审计：

- 有多少 `behavioral_rating` release 没有 NormRef/Primary NormConclusion；
- 现有 behavioral Outcome 有多少缺失 NormReference；
- 有多少 pending Assessment 没有 model ref；
- 非 canonical Algorithm、缺失 DefinitionV2/冻结身份和旧 payload 字段各有多少存量；
- 生产实际 Mongo 索引与 migration 有什么差异；
- DefinitionV2 内容摘要是否存在且运行时可物化。

本批次不修数据，只生成数量、样本和影响范围。

### 批次 1：关闭直接正确性缺口

```text
MC-R001 独立 Questionnaire 分流（已关闭）
MC-R002 NormSubject + strict demographic matching
MC-R003 behavioral_rating 发布强制 Norm
MC-R004 Decision/Outcome 边界正确性（已实现待验收）
```

MC-R004 已完成区间校验、OutcomeCode、max_inclusive/无上界、DecisionResult/cognitive primary、Interpretation fail-closed 与四 family golden；只待部署 smoke。

### 批次 2：补齐发布和存储门禁

```text
MC-R005 canonical migrations/indexes/integration tests
MC-R007 统一发布完整性
MC-R008 首次发布后身份不变式（已关闭）
MC-R009 Questionnaire Binding Policy
MC-R011 Norm compatibility
MC-R012 release/concurrency semantics
```

### 批次 3：让运行时严格消费发布事实

```text
MC-R006 freeze RuntimeIdentity
MC-R010 InputInvariantValidator
```

完成该批次后，新模型的 Worker 路径应已不需要默认 family/decision。

### 批次 4：在行为保护下调整代码边界

```text
MC-R013 handler responsibility split（已关闭）
MC-R014 capability/validation single source（已关闭）
MC-R015 scale canonical Calculation input（已关闭）
MC-R016 Decision/Interpretation logical split（已关闭）
MC-R019 pure calculation extraction（已关闭）
```

### 批次 5：切换到 DefinitionV2/current-only

```text
MC-R017 DefinitionV2-only + ReportInput schema 3
MC-R018 canonical identity + exact family/decision route
```

这一批采用维护窗口：备份 → `verify_definition_v2_cutover` → 删除不兼容数据 → 重新导入/发布 → 部署 smoke test；不执行 backfill 或双写。

### 2026-07-22 验收后的复验顺序

1. MC-R004 四 family 边界矩阵与 MC-R007/MC-R016 SourceRefs/解释边界已完成；MC-R005/MC-R012 的本地 Replica Set schema/CAS/rollback 门禁也已完成；
2. 下一步随维护窗口收集 MC-R002～MC-R006、MC-R012、MC-R017、MC-R018 的数据与部署证据；
3. MC-R010、MC-R011、MC-R015 已在本轮通过并关闭，不再重复叠加重构；
4. MC-R009、MC-R020 在业务矩阵或真实产品需求确认前不进入实现。

---

## 10. 每个重构项的开始与关闭门槛

### 10.1 开始前

- [ ] 重新阅读当前源码，不仅依赖本文；
- [ ] 确认工作树中用户未提交修改；
- [ ] 界定输入、输出、持久化和兼容契约；
- [ ] 完成影响数据审计；
- [ ] 补齐 characterization/golden tests；
- [ ] 明确是纯代码重构、行为改造还是数据迁移；
- [ ] 对需要业务决策的语义先得到确认。

### 10.2 关闭前

- [ ] 目标验收条件均有测试或部署证据；
- [ ] 历史 exact-version 执行与重试不漂移；
- [ ] 新发布契约已收紧，老数据兼容位于独立边界；
- [ ] 数据迁移可重跑、可审计、可回滚；
- [ ] 对应 ModelCatalog、Evaluation、Interpretation 文档已同步；
- [ ] `make docs-hygiene docs-facts` 通过；
- [ ] 相关领域、应用、基础设施和运行时测试通过；
- [ ] 对外 API/gRPC 契约变化已完成消费方迁移；
- [ ] 监控能证明旧 fallback 使用量下降或清零。

---

## 11. 不应被重构破坏的已确认决策

下列内容不是“待优化项”，而是重构时必须保护的领域决策：

1. Questionnaire 可以独立发布并产生 AnswerSheet；
2. Questionnaire 与 AssessmentModel 绑定后才构成可执行测评；
3. `AssessmentModel` 是可变工作头，`AssessmentSnapshot` 是不可变发布版本；
4. 新测评使用 active release，已受理测评和重试使用 exact retained release；
5. Algorithm 语义发生不兼容变化时新增 Algorithm identifier，当前不引入独立 AlgorithmVersion；
6. Factor 必需，Decision 必需；不完整模型不得发布；
7. `behavioral_rating` 必须常模化，无常模的原始分区间模型使用 `scale`；
8. `cognitive` 当前只宣称 Raven SPM 已实现，不宣称已建成通用认知任务平台；
9. ProductChannel 只用于产品分类，不用于 Evaluation 路由；
10. ModelCatalog 配置模型，Calculation 实现纯算法，Evaluation 编排执行，Interpretation 解释结果；
11. DefinitionV2 是唯一持久化模型定义；运行时 DTO 只能临时物化，不得保存为 compatibility payload；
12. current-only 切换前可备份后删除旧 Assessment/Outcome/Model/Norm；切换后已被有效 Assessment 引用的当前版本不得就地改写；
13. 该系统产生辅助测评信息，不产生医学诊断。

---

## 12. 维护规则

每次更新某个 `MC-Rxxx` 时，至少记录：

| 字段 | 要求 |
| --- | --- |
| 状态 | 规划改造 / 待补证据 / 待业务决策 / 已实现待验收 / 已关闭 |
| 当前证据 | 实际代码、数据、部署或测试，不只引用旧文档 |
| 决策 | 接受的方案、未采用方案及原因 |
| 实施范围 | 本次修改什么，明确不修改什么 |
| 数据迁移 | 是否需要 audit/backfill/rollback |
| 验收 | 测试、集成环境、生产观测或人工核验 |
| 剩余风险 | 关闭后仍保留的兼容面或未覆盖场景 |

当源码已修复某个问题，但本文尚未更新时，以源码与真实运行证据为准；修改者应随后将条目改为“已实现待验收”或“已关闭”。

---

## 13. 相关文档

- [ModelCatalog 总览](./README.md)
- [领域模型](./10-领域模型.md)
- [DefinitionV2 与模型扩展](./20-核心设计-DefinitionV2与模型扩展.md)
- [模型身份、算法绑定与执行路由](./21-核心设计-模型身份、算法绑定与执行路由.md)
- [问卷绑定与发布版本](./22-核心设计-问卷绑定与发布版本.md)
- [因子与计分模型](./23-核心设计-因子与计分模型.md)
- [常模资产与校准](./24-核心设计-常模资产与校准.md)
- [结果判定、Outcome 与解释边界](./25-核心设计-结果判定、Outcome与解释边界.md)
- [数据存储与一致性](./26-核心设计-数据存储与一致性.md)
- [模型创建、编辑与联合发布](./30-关键链路-模型创建编辑与联合发布.md)
- [已发布模型准入与执行输入](./31-关键链路-已发布模型准入与执行输入.md)
- [四种模型类型](./40-模型类型/README.md)
