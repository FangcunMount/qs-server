package modelcatalog

// Identity helpers for ModelSummary lived here historically (ProductChannel /
// AlgorithmFamily). Those fields were removed from the API summary contract;
// decision routing now uses DecisionKind as the single runtime key.
